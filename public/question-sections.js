/* eslint-disable */
/**
 * Interview question sections for the AllWeb runtime.
 *
 * This file consolidates the message queue and Spring datasets so the app
 * can load them with a single script tag.
 */

/**
 * Message Queues — Questions & Answers
 *
 * Keyed by the exact section heading from INTERVIEW-QUESTIONS.md.
 * Source of truth is still the .md file; this file is generated/maintained
 * alongside it so the web app can load the content without a fetch()
 * (works on file:// too).
 */
var MESSAGE_QUEUE_QUESTION_SECTIONS = {
  "Fundamentals": [
    "## Fundamentals",
    "",
    "### Q1: What is a message queue and why do we need one?",
    "",
    "A message queue is a middleman that sits between two parts of a system. Instead of Service A calling Service B directly and waiting for a response, Service A drops a message on the queue and moves on. Service B picks it up whenever it's ready.",
    "",
    "**Why we need it:**",
    "- **Decoupling.** Service A and Service B don't need to know about each other. Either one can be updated, restarted, or replaced without breaking the other.",
    "- **Handling traffic spikes.** If 10,000 requests arrive in one second, the queue absorbs them. Workers process them at their own pace. Without a queue, Service B would crash or reject requests.",
    "- **Reliability.** If Service B goes down, messages wait in the queue. When it comes back, it picks up where it left off. No data is lost.",
    "",
    "**Example:** When you place an order on Amazon, the website responds immediately with \"order confirmed.\" Behind the scenes, the order event sits in a queue. Separate services pick it up to charge your card, notify the warehouse, send a confirmation email, and update your order history -- all asynchronously.",
    "",
    "---",
    "",
    "### Q2: What is the difference between a message queue and a message broker?",
    "",
    "- A **message queue** is a specific data structure -- an ordered list of messages waiting to be processed. Think of it like a single pipe: messages go in one end, come out the other.",
    "- A **message broker** is the entire server/system that manages queues, routes messages, handles connections, and ensures delivery. Think of it like a post office that contains many mailboxes (queues), sorting rules (routing), and delivery guarantees.",
    "",
    "RabbitMQ is a message broker. It contains many queues inside it. SQS is both -- AWS manages the broker, and you interact with individual queues.",
    "",
    "---",
    "",
    "### Q3: What is the difference between a queue and a topic?",
    "",
    "- A **queue** follows the point-to-point pattern. Each message goes to exactly one consumer. If 3 workers are listening on the same queue, each message is delivered to only one of them. Think of a to-do list where each task is assigned to one person.",
    "- A **topic** follows the publish/subscribe pattern. Each message goes to all subscribers. If 3 services subscribe to a topic, all 3 get a copy of every message. Think of a radio broadcast -- everyone tuned in hears the same thing.",
    "",
    "**When to use which:**",
    "- Queue: background jobs, task processing (send email, resize image).",
    "- Topic: event notifications where multiple services need to react (user signed up, order placed).",
    "",
    "---",
    "",
    "### Q4: What is the difference between synchronous and asynchronous communication?",
    "",
    "- **Synchronous:** Service A calls Service B and waits for a response. Like a phone call -- you speak, you wait for the other person to reply, then you continue.",
    "- **Asynchronous:** Service A sends a message and moves on without waiting. Like a text message -- you send it and do other things. The reply comes later.",
    "",
    "Message queues enable asynchronous communication. The producer doesn't wait for the consumer to process the message.",
    "",
    "**Trade-off:** Synchronous is simpler and gives you an immediate answer. Asynchronous is more resilient and scalable, but you have to deal with the fact that you don't know when (or if) the work is done unless you build a way to check.",
    "",
    "---",
    "",
    "### Q5: What is the difference between push and pull delivery?",
    "",
    "- **Push:** The broker sends messages to the consumer as soon as they arrive. The consumer doesn't ask for them -- they just show up. RabbitMQ does this by default.",
    "- **Pull:** The consumer asks the broker \"do you have any new messages?\" at regular intervals. Kafka works this way -- consumers fetch batches of messages.",
    "",
    "**Push** is better for low-latency scenarios (real-time dashboards). **Pull** is better for high-throughput scenarios because the consumer controls how fast it processes (backpressure).",
    "",
    "---",
    "",
    "### Q6: What is a Dead Letter Queue (DLQ)?",
    "",
    "A DLQ is a special queue where messages go when they can't be processed successfully. Think of it as the \"undeliverable mail\" bin at a post office.",
    "",
    "**When messages end up in a DLQ:**",
    "- The consumer tried to process the message 3 times (or whatever limit you set) and failed every time.",
    "- The message expired (TTL ran out before anyone processed it).",
    "- The message format is invalid and no consumer can parse it.",
    "",
    "**Why it matters:** Without a DLQ, a \"poison message\" (one that always fails) would be retried forever, blocking the queue. With a DLQ, the bad message is moved aside, the main queue keeps flowing, and an engineer can inspect the DLQ later to figure out what went wrong.",
    "",
    "---",
    "",
    "### Q7: What are producers, consumers, and brokers?",
    "",
    "- **Producer:** The part of your system that creates and sends messages. Example: the Order Service that publishes \"order placed\" events.",
    "- **Consumer:** The part that reads and processes messages. Example: the Email Service that reads \"order placed\" and sends a confirmation email.",
    "- **Broker:** The server in the middle that receives messages from producers, stores them, and delivers them to consumers. Example: RabbitMQ, Kafka, SQS.",
    "",
    "The producer and consumer never talk directly. The broker is always in between."
  ].join("\n"),

  "Delivery Guarantees and Reliability": [
    "## Delivery Guarantees and Reliability",
    "",
    "### Q8: What are the three delivery guarantees? Explain each.",
    "",
    "- **At-most-once:** The message is delivered zero or one time. The broker sends it and forgets. If the consumer crashes before processing, the message is lost. Fastest, but unreliable. Good for non-critical metrics where losing a few data points is acceptable.",
    "- **At-least-once:** The message is delivered one or more times. The broker keeps retrying until the consumer acknowledges. If the consumer processes the message but the ACK is lost (network glitch), the broker resends it, causing a duplicate. Reliable, but you may see duplicates.",
    "- **Exactly-once:** The message is delivered exactly one time. This is the hardest to achieve. It requires coordination between the producer, broker, and consumer (idempotency, transactions, deduplication). Most expensive in terms of performance. Used for critical things like financial transactions.",
    "",
    "**Follow-up question interviewers love:** \"Is exactly-once really possible?\" The honest answer is: true end-to-end exactly-once across different systems is extremely hard. Kafka achieves it within its own ecosystem using idempotent producers and transactional APIs, but the moment you write to an external database, you need idempotency on the consumer side too.",
    "",
    "---",
    "",
    "### Q9: What does \"idempotent\" mean and why does it matter for message queues?",
    "",
    "An operation is **idempotent** if doing it once or doing it ten times produces the same result.",
    "",
    "**Example:** \"Set the user's email to alice@example.com\" is idempotent -- doing it twice doesn't change anything. \"Add $10 to the user's balance\" is NOT idempotent -- doing it twice adds $20.",
    "",
    "**Why it matters:** With at-least-once delivery, consumers may receive the same message more than once. If your consumer is idempotent, duplicates are harmless. If not, you'll charge a customer twice, send two emails, or create duplicate records.",
    "",
    "**How to make consumers idempotent:**",
    "- Store a unique message ID in your database. Before processing, check if you've seen this ID before. If yes, skip it.",
    "- Use database upserts (INSERT ... ON CONFLICT DO NOTHING) instead of plain inserts.",
    "- Design operations as \"set to X\" instead of \"add X.\"",
    "",
    "---",
    "",
    "### Q10: What is message acknowledgment (ACK/NACK)?",
    "",
    "- **ACK (Acknowledge):** The consumer tells the broker \"I'm done with this message, you can delete it.\" The broker removes it from the queue.",
    "- **NACK (Negative Acknowledge):** The consumer tells the broker \"I couldn't process this, put it back.\" The broker requeues the message for another attempt.",
    "- **Auto-ACK:** The broker considers the message delivered the moment it sends it. If the consumer crashes after receiving but before processing, the message is gone. Fast, but risky.",
    "- **Manual ACK:** The consumer explicitly sends the ACK after successful processing. Safer, because the message stays in the queue until it's definitely handled.",
    "",
    "**Interview tip:** Always mention that production systems should use manual ACK, because auto-ACK risks losing messages on consumer crashes.",
    "",
    "---",
    "",
    "### Q11: What is a poison message and how do you handle it?",
    "",
    "A poison message is a message that causes the consumer to fail every time it tries to process it. Maybe the JSON is malformed, a required field is missing, or the message triggers a bug in the consumer's code.",
    "",
    "**The danger:** Without protection, the broker keeps redelivering the poison message. The consumer keeps crashing. The queue is stuck -- no other messages behind it get processed.",
    "",
    "**How to handle it:**",
    "1. Set a **max retry count** (e.g., 3 attempts).",
    "2. After the max, move the message to a **Dead Letter Queue**.",
    "3. Alert the team so someone can inspect the DLQ.",
    "4. The main queue continues processing other messages normally.",
    "",
    "---",
    "",
    "### Q12: What is backpressure and how do you handle it?",
    "",
    "Backpressure happens when producers send messages faster than consumers can process them. The queue grows, memory fills up, and eventually the broker crashes or starts rejecting messages.",
    "",
    "**How to handle it:**",
    "- **Prefetch limit (RabbitMQ):** Tell the broker \"only send me 10 messages at a time.\" The broker won't send message #11 until one of the first 10 is ACKed.",
    "- **Consumer lag monitoring (Kafka):** Track how far behind each consumer group is. If lag grows above a threshold, alert the team to add more consumers.",
    "- **Queue length limits:** Set a max queue size. When reached, either reject new messages (tell the producer to slow down), drop the oldest messages, or route overflow to a different queue.",
    "- **Scale consumers:** Add more worker instances to drain the queue faster."
  ].join("\n"),

  "RabbitMQ": [
    "## RabbitMQ",
    "",
    "### Q13: What is an exchange in RabbitMQ? Name all four types.",
    "",
    "An exchange is a routing layer. Producers send messages to an exchange (never directly to a queue). The exchange looks at the message's routing key and decides which queue(s) should receive it based on bindings.",
    "",
    "**Four types:**",
    "1. **Direct:** Routes to queues where the binding key exactly matches the routing key. Routing key `order.created` goes only to queues bound to `order.created`.",
    "2. **Fanout:** Ignores the routing key entirely. Copies the message to every bound queue. Used for broadcasting.",
    "3. **Topic:** Pattern matching. Routing key `order.created` matches bindings like `order.*` (one word wildcard) and `#` (everything). Most flexible.",
    "4. **Headers:** Routes based on message header key-value pairs instead of routing key. Useful when routing depends on multiple attributes.",
    "",
    "---",
    "",
    "### Q14: What are quorum queues in RabbitMQ?",
    "",
    "Quorum queues are RabbitMQ's modern high-availability queue type. They use the **Raft consensus algorithm** to replicate messages across multiple nodes.",
    "",
    "**How they work:**",
    "- One node is the **leader** (accepts writes).",
    "- Other nodes are the **followers** (replicate data from the leader).",
    "- A message is only confirmed to the producer once a **majority** (quorum) of nodes have stored it.",
    "- If the leader dies, a follower is automatically elected as the new leader.",
    "",
    "**Why they matter:** Classic mirrored queues (the old HA approach) had split-brain problems and data loss edge cases. Quorum queues solve these with proper consensus, at the cost of slightly higher latency (because the majority must confirm before ACKing the producer).",
    "",
    "---",
    "",
    "### Q15: How does RabbitMQ handle message persistence?",
    "",
    "Two things must be true for a message to survive a broker restart:",
    "1. The **queue** must be declared as `durable: true`.",
    "2. The **message** must be published with `delivery_mode: 2` (persistent).",
    "",
    "If either is missing, the message lives only in memory and is lost on restart. Even with both set, there's a small window where a message is received but not yet flushed to disk. Quorum queues close this gap by requiring majority confirmation."
  ].join("\n"),

  "Apache Kafka": [
    "## Apache Kafka",
    "",
    "### Q16: What is a partition in Kafka and why does it matter?",
    "",
    "A partition is a slice of a topic. Each topic is divided into one or more partitions, and each partition is an ordered, append-only log stored on a broker.",
    "",
    "**Why it matters:**",
    "- **Parallelism.** Each partition can be consumed by a different consumer. 10 partitions = up to 10 consumers reading in parallel.",
    "- **Ordering.** Messages within a single partition are strictly ordered. Messages across partitions have no ordering guarantee. If order matters for a specific entity (e.g., all events for customer #42), use the entity ID as the partition key so they all land in the same partition.",
    "- **Scalability.** More partitions = more throughput. But too many partitions increases metadata overhead and rebalancing time.",
    "",
    "**Common interview follow-up:** \"Can you decrease the number of partitions?\" No. Once created, partitions can only be increased, never decreased. Choose wisely at topic creation.",
    "",
    "---",
    "",
    "### Q17: What is a consumer group in Kafka?",
    "",
    "A consumer group is a set of consumers that cooperate to read from a topic. Each partition is assigned to exactly one consumer in the group.",
    "",
    "**Example:** Topic with 3 partitions, consumer group with 2 consumers:",
    "- Consumer A reads partitions 0 and 1.",
    "- Consumer B reads partition 2.",
    "",
    "If Consumer A dies, its partitions are reassigned to Consumer B (rebalancing). If a third consumer joins, partitions are redistributed: one each.",
    "",
    "**Key rule:** You can never have more active consumers than partitions in a single group. If you have 3 partitions and 5 consumers, 2 consumers will sit idle.",
    "",
    "**Multiple groups:** Different consumer groups read the same topic independently. The analytics group and the search-indexer group each have their own offsets and don't interfere with each other.",
    "",
    "---",
    "",
    "### Q18: What is an offset in Kafka?",
    "",
    "An offset is a sequential number that identifies a message's position within a partition. The first message is offset 0, the next is 1, then 2, and so on.",
    "",
    "**How consumers use it:**",
    "- A consumer reads messages starting from its last **committed offset**.",
    "- After processing, it **commits** the new offset (\"I've processed up to offset 42\").",
    "- If it crashes and restarts, it resumes from the last committed offset (43).",
    "",
    "**Why it matters:**",
    "- **Replay:** Reset the offset to 0 and reprocess everything.",
    "- **Monitoring:** The difference between the latest offset and the committed offset is **consumer lag** -- how far behind the consumer is.",
    "",
    "---",
    "",
    "### Q19: What is log compaction in Kafka?",
    "",
    "Log compaction keeps only the **latest value for each key** in a topic, deleting older entries for the same key.",
    "",
    "**Example:** A topic tracking user addresses:",
    "- Offset 0: key=user-1, value=\"123 Main St\"",
    "- Offset 1: key=user-2, value=\"456 Oak Ave\"",
    "- Offset 2: key=user-1, value=\"789 Pine Rd\" (user-1 moved)",
    "",
    "After compaction: offset 0 is deleted (superseded by offset 2). Offsets 1 and 2 remain. The topic now acts like a database table -- one row per user with the latest address.",
    "",
    "**Use cases:** CDC (change data capture), configuration distribution, maintaining materialized views.",
    "",
    "---",
    "",
    "### Q20: What is the difference between Kafka and RabbitMQ?",
    "",
    "| Aspect | RabbitMQ | Kafka |",
    "|---|---|---|",
    "| **Model** | Message broker with queues | Distributed event log |",
    "| **After consumption** | Message is deleted | Message stays (retained for days/weeks) |",
    "| **Replay** | Not possible | Yes, seek to any offset |",
    "| **Routing** | Rich (4 exchange types) | None (topic + partition only) |",
    "| **Throughput** | ~50K msg/s | Millions msg/s |",
    "| **Ordering** | Per-queue FIFO | Per-partition FIFO |",
    "| **Best for** | Task queues, RPC, complex routing | Event streaming, log aggregation, event sourcing |",
    "| **Consumer model** | Broker pushes to consumers | Consumers pull from brokers |",
    "",
    "**Short answer for interviews:** \"RabbitMQ is a smart broker with dumb consumers (the broker decides where messages go). Kafka is a dumb broker with smart consumers (the broker just stores the log, consumers decide where to read from).\"",
    "",
    "---",
    "",
    "### Q21: How does Kafka achieve high throughput?",
    "",
    "Kafka is fast because of several design choices that work together:",
    "",
    "1. **Sequential disk I/O.** Messages are appended to the end of a file. Sequential writes are extremely fast -- even faster than random reads from RAM on modern disks.",
    "2. **Zero-copy transfer.** When a consumer reads, the OS sends data directly from the disk page cache to the network socket without copying it through application memory. This avoids two expensive memory copies.",
    "3. **Batching.** Producers batch multiple messages into a single network request. Consumers fetch in batches too. This amortizes the network overhead across many messages.",
    "4. **Partitioning.** Multiple partitions spread the load across multiple brokers and disks, allowing parallel writes and reads.",
    "5. **Compression.** Producers can compress batches (gzip, snappy, lz4, zstd) before sending, reducing network bandwidth."
  ].join("\n"),

  "AWS SQS / SNS": [
    "## AWS SQS / SNS",
    "",
    "### Q22: What is the difference between SQS Standard and SQS FIFO?",
    "",
    "| Feature | Standard | FIFO |",
    "|---|---|---|",
    "| **Throughput** | Nearly unlimited | ~3,000 msg/s (with batching) |",
    "| **Ordering** | Best-effort (may be out of order) | Strict FIFO within a message group |",
    "| **Delivery** | At-least-once (duplicates possible) | Exactly-once (within dedup window) |",
    "| **Use case** | High-volume work queues | Order-sensitive workflows |",
    "",
    "**When to use Standard:** You need high throughput and can handle occasional duplicates or out-of-order messages (e.g., sending notification emails -- getting two is annoying but not catastrophic).",
    "",
    "**When to use FIFO:** Order matters and duplicates would cause real problems (e.g., processing bank transactions -- \"deposit then withdraw\" must not become \"withdraw then deposit\").",
    "",
    "---",
    "",
    "### Q23: What is a visibility timeout in SQS?",
    "",
    "When a consumer receives a message from SQS, the message doesn't disappear -- it becomes **invisible** to other consumers for a period (default 30 seconds). This is the visibility timeout.",
    "",
    "**What happens next:**",
    "- If the consumer finishes processing and **deletes** the message before the timeout, it's gone permanently.",
    "- If the consumer crashes or the timeout expires before deletion, the message **becomes visible again** and another consumer can pick it up.",
    "",
    "**Why it matters:** This is how SQS handles retries without you writing any retry logic. If processing fails, the message automatically gets another chance.",
    "",
    "**Tuning tip:** Set the timeout longer than your expected processing time. If your job takes 2 minutes but the timeout is 30 seconds, the message will become visible again while the first consumer is still working on it, causing duplicate processing.",
    "",
    "---",
    "",
    "### Q24: What is the difference between SQS and SNS?",
    "",
    "- **SQS (Simple Queue Service)** is a queue. One message goes to one consumer (point-to-point). Think of it as a to-do list -- each item is handled by one worker.",
    "- **SNS (Simple Notification Service)** is a pub/sub system. One message goes to all subscribers (fan-out). Think of it as a newsletter -- every subscriber gets a copy.",
    "",
    "**Used together:** SNS for fan-out, SQS for reliable processing. Publish to an SNS topic, which copies the message to 5 SQS queues, each consumed by a different service. This is the most common AWS messaging pattern."
  ].join("\n"),

  "System Design and Architecture": [
    "## System Design and Architecture",
    "",
    "### Q25: How would you design a notification system using message queues?",
    "",
    "**Requirements:** When an event happens (new follower, new message, new like), send notifications through multiple channels (push notification, email, SMS, in-app).",
    "",
    "**Design:**",
    "",
    "1. **Event producers** (the Follow Service, Message Service, Like Service) publish events to a topic/exchange.",
    "2. **Notification Router** consumes events and decides which channels to use based on user preferences (user A wants push + email, user B wants SMS only).",
    "3. For each channel, it publishes a job to a channel-specific queue:",
    "   - `push-notification-queue`",
    "   - `email-queue`",
    "   - `sms-queue`",
    "4. **Channel workers** consume from their respective queues and deliver the notification.",
    "5. Each queue has a **DLQ** for failed deliveries.",
    "6. A **retry policy** with exponential backoff handles transient failures (email server temporarily down).",
    "",
    "**Why a queue?** If the email server is slow or down, the queue buffers thousands of pending emails. Users don't see errors. When the server recovers, emails are sent in order.",
    "",
    "---",
    "",
    "### Q26: How would you handle exactly-once processing in a payment system?",
    "",
    "**The problem:** A payment event arrives (\"charge customer $50\"). If it's processed twice, the customer is charged $100. If it's lost, the customer isn't charged at all.",
    "",
    "**Solution: idempotent consumer with a deduplication store.**",
    "",
    "1. Each payment message has a unique `paymentId`.",
    "2. Before processing, the consumer checks a database: \"Have I already processed paymentId=abc123?\"",
    "3. If yes, skip it (duplicate).",
    "4. If no, process the payment AND write `paymentId=abc123` to the deduplication table in the **same database transaction**.",
    "5. ACK the message.",
    "",
    "**Why this works:** Even if the message is delivered twice (network retry, consumer restart), the second attempt sees the ID in the deduplication table and does nothing. The database transaction ensures the payment and the deduplication record are saved atomically -- either both succeed or both fail.",
    "",
    "---",
    "",
    "### Q27: How would you design a system that processes 1 million events per second?",
    "",
    "**Answer: Kafka with horizontal scaling.**",
    "",
    "1. Create a topic with many partitions (e.g., 100-200).",
    "2. Run a cluster of Kafka brokers (e.g., 10-20) spread across multiple machines. Partitions are distributed across brokers.",
    "3. Producers batch messages and use compression (lz4 or zstd).",
    "4. Run a consumer group with as many consumers as partitions (e.g., 100-200 consumer instances).",
    "5. Each consumer processes its assigned partitions independently.",
    "6. Monitor consumer lag to detect if consumers are falling behind.",
    "7. If throughput isn't enough, add more partitions and consumers.",
    "",
    "**Why Kafka:** Sequential disk writes + zero-copy reads + batching + partitioning = designed from the ground up for this scale. RabbitMQ would struggle past ~50K msg/s per node. SQS Standard could handle the throughput but can't guarantee ordering or support replay.",
    "",
    "---",
    "",
    "### Q28: What is the Transactional Outbox pattern?",
    "",
    "**The problem:** Your service needs to update a database AND publish a message to a queue. If you do them as two separate operations, either one can fail:",
    "- DB write succeeds, message publish fails = the queue is out of sync.",
    "- Message publish succeeds, DB write fails = the queue has events for things that didn't happen.",
    "",
    "**The solution:** Write the message to an **outbox table** in the same database, inside the same transaction as the business data.",
    "",
    "1. In one transaction: INSERT the order into the `orders` table AND INSERT the event into the `outbox` table.",
    "2. A separate background process (or CDC tool like Debezium) reads the outbox table and publishes messages to the queue.",
    "3. After publishing, mark the outbox row as \"sent.\"",
    "",
    "**Why it works:** The database transaction guarantees both writes succeed or both fail. The outbox reader is idempotent -- if it crashes and retries, it just re-publishes (and the consumer handles duplicates via idempotency).",
    "",
    "---",
    "",
    "### Q29: What is the Saga pattern and how do message queues fit in?",
    "",
    "**The problem:** A business operation spans multiple services (order service, payment service, inventory service). You can't use a single database transaction because each service has its own database.",
    "",
    "**The Saga pattern** breaks the operation into a chain of local transactions, coordinated through messages:",
    "",
    "1. Order Service creates order (status: PENDING) and publishes `OrderCreated`.",
    "2. Payment Service hears `OrderCreated`, charges the customer, publishes `PaymentSucceeded`.",
    "3. Inventory Service hears `PaymentSucceeded`, reserves stock, publishes `StockReserved`.",
    "4. Order Service hears `StockReserved`, marks order as CONFIRMED.",
    "",
    "**If something fails (e.g., Payment fails):**",
    "- Payment Service publishes `PaymentFailed`.",
    "- Order Service hears it and cancels the order (compensating transaction).",
    "",
    "**Two flavors:**",
    "- **Choreography:** Each service listens for events and decides what to do. No coordinator. Simple but hard to trace.",
    "- **Orchestration:** A central \"saga orchestrator\" tells each service what to do and handles failures. More control but adds a single point of coordination."
  ].join("\n"),

  "Troubleshooting and Production Issues": [
    "## Troubleshooting and Production Issues",
    "",
    "### Q30: Messages are piling up in the queue and consumers can't keep up. What do you do?",
    "",
    "This is a **consumer lag** problem. Walk through this checklist:",
    "",
    "1. **Check consumer health.** Are consumers running? Are they throwing errors? Check logs first.",
    "2. **Check processing time.** Is each message taking too long? Maybe a downstream service (database, API) is slow. Fix the bottleneck.",
    "3. **Scale horizontally.** Add more consumer instances. In Kafka, you can add consumers up to the number of partitions. In RabbitMQ/SQS, just add more workers.",
    "4. **Increase prefetch/batch size.** If consumers spend more time waiting for messages than processing them, fetch more messages per round trip.",
    "5. **Check for poison messages.** One bad message retrying infinitely can block the queue. Make sure you have a DLQ and max retry limit.",
    "6. **Temporarily increase resources.** Give consumers more CPU/memory if they're resource-constrained.",
    "7. **Back-pressure the producer.** If nothing else works, slow down the producer (rate limiting) to prevent the queue from growing further while you fix the root cause.",
    "",
    "---",
    "",
    "### Q31: A consumer processed a message but the ACK was lost. What happens?",
    "",
    "The broker thinks the message was never processed (because it never received the ACK). It will **redeliver** the message to the same or a different consumer.",
    "",
    "**Result:** The message is processed twice.",
    "",
    "**How to prevent problems from this:**",
    "- Make your consumer **idempotent** (processing the same message twice has the same effect as processing it once).",
    "- Use a deduplication store: before processing, check if you've already handled this message ID.",
    "",
    "This is a fundamental reality of distributed systems: you can't have both \"never lose a message\" and \"never process a duplicate\" without significant coordination (idempotent consumers, transactional exactly-once, etc.).",
    "",
    "---",
    "",
    "### Q32: How do you monitor a message queue in production?",
    "",
    "**Key metrics to watch:**",
    "",
    "| Metric | What it tells you | Alert when |",
    "|---|---|---|",
    "| **Queue depth / consumer lag** | How many messages are waiting | Growing steadily (consumers can't keep up) |",
    "| **Processing rate** | Messages consumed per second | Drops significantly |",
    "| **Error rate** | Messages that fail processing | Spikes |",
    "| **DLQ size** | Messages that permanently failed | Any messages appear |",
    "| **Consumer count** | Number of active consumers | Drops below expected |",
    "| **Message age** | How long the oldest message has been waiting | Exceeds your SLA |",
    "| **Broker disk / memory** | Resource usage of the broker | Approaching limits |",
    "",
    "**Tools:**",
    "- RabbitMQ: Built-in Management UI (port 15672), Prometheus exporter.",
    "- Kafka: Burrow (consumer lag), Cruise Control (cluster balancing), Prometheus + Grafana.",
    "- SQS: CloudWatch metrics (ApproximateNumberOfMessagesVisible, ApproximateAgeOfOldestMessage).",
    "",
    "---",
    "",
    "### Q33: Your Kafka consumer group is constantly rebalancing. What's wrong?",
    "",
    "Rebalancing happens when consumers join or leave a group. If it's happening constantly, consumers are likely **timing out** and being kicked from the group, then rejoining, causing another rebalance.",
    "",
    "**Common causes:**",
    "1. **Processing takes too long.** The consumer doesn't call `poll()` within `max.poll.interval.ms` (default 5 minutes). Kafka thinks it's dead and triggers rebalance. Fix: increase the interval or reduce `max.poll.records` so each poll cycle processes fewer messages.",
    "2. **GC pauses.** Long Java garbage collection pauses can cause the consumer to miss heartbeats (`session.timeout.ms`, default 45s). Fix: tune JVM GC settings.",
    "3. **Consumer crashes and restarts in a loop.** A bug causes the consumer to crash, restart, rejoin, crash again. Fix: check logs for the underlying error.",
    "4. **Network issues.** Unstable network between consumers and brokers causes heartbeat failures. Fix: check network health.",
    "",
    "**Impact of constant rebalancing:** During rebalance, all consumers in the group **stop processing**. If rebalancing takes 30 seconds and happens every minute, your consumers are idle half the time.",
    "",
    "---",
    "",
    "### Q34: How do you guarantee message ordering?",
    "",
    "**The hard truth:** Guaranteeing global order across all messages is expensive and limits throughput. Most systems guarantee order within a subset.",
    "",
    "**By queue type:**",
    "- **RabbitMQ:** Messages in a single queue are FIFO. But if you have multiple consumers on one queue, messages may be processed out of order (consumer A takes message 1, consumer B takes message 2, B finishes first). Solution: use a single consumer per queue, or design for out-of-order processing.",
    "- **Kafka:** Messages within a single partition are strictly ordered. Use a partition key (e.g., customer ID) to ensure all events for the same entity go to the same partition.",
    "- **SQS Standard:** No ordering guarantee at all. Use SQS FIFO with message group IDs for ordering.",
    "- **SQS FIFO:** Strict ordering within a message group. Different groups are processed independently.",
    "",
    "**Interview tip:** Don't say \"Kafka guarantees ordering.\" Say \"Kafka guarantees ordering within a partition.\" This shows you understand the nuance.",
    "",
    "---",
    "",
    "### Q35: How do you handle schema evolution in message queues?",
    "",
    "**The problem:** The producer changes the message format (adds a new field, removes an old one, changes a type). Consumers that expect the old format break.",
    "",
    "**Solutions:**",
    "1. **Schema registry (Kafka).** Use a schema registry (Confluent Schema Registry) that stores Avro/Protobuf/JSON schemas. Producers and consumers validate against the registry. The registry enforces compatibility rules (backward, forward, full).",
    "2. **Backward compatibility.** New fields must have default values. Old consumers ignore unknown fields. Old messages can be read by new consumers (missing fields use defaults).",
    "3. **Versioning.** Include a `version` field in the message. Consumers check the version and handle different formats.",
    "4. **Contract testing.** Test that producer output matches consumer expectations before deployment.",
    "",
    "**Best practice:** Use Avro or Protobuf (not raw JSON) with a schema registry. They enforce structure and handle evolution gracefully."
  ].join("\n")
};

/**
 * Spring Framework — Questions & Answers
 *
 * Keyed by the exact section heading from Spring_Interview_Questions_Study_Guide.md.
 */
var SPRING_QUESTION_SECTIONS = {
  "Spring Core & IoC": [
    "## Spring Core & IoC",
    "",
    "### 1. What is Spring Framework?",
    "",
    "Spring is a lightweight, open-source Java application framework created by **Rod Johnson in 2003** as an alternative to the heavyweight J2EE model. It provides comprehensive infrastructure support for developing enterprise Java applications through dependency injection, aspect-oriented programming, and a modular architecture. The framework enables developers to build loosely coupled, testable, and maintainable applications while reducing boilerplate code.",
    "",
    "### 2. What are the benefits of using Spring?",
    "",
    "- **Lightweight container** — minimal overhead compared to traditional Java EE servers",
    "- **Loose coupling** through Dependency Injection, making components independently testable",
    "- **Modular design** — use only the modules you need (Core, MVC, Data, Security, etc.)",
    "- **AOP support** for separating cross-cutting concerns (logging, transactions, security)",
    "- **Declarative transaction management** without requiring a full application server",
    "- **Built-in integration** with JDBC, JPA/Hibernate, JMS, REST, and other technologies",
    "- **Extensive testing support** with `@SpringBootTest`, mock objects, and `TestContext` framework",
    "- **Large ecosystem** and active community with well-maintained documentation",
    "",
    "### 3. What are the different modules in Spring?",
    "",
    "| Module Group | Key Modules | Purpose |",
    "|---|---|---|",
    "| **Core Container** | Core, Beans, Context, SpEL | IoC container, DI, application context, expression language |",
    "| **Data Access** | JDBC, ORM, Transaction, JMS | Database access, ORM integration, declarative transactions |",
    "| **Web** | Web MVC, WebFlux, WebSocket | Servlet-based and reactive web frameworks |",
    "| **AOP & Instrumentation** | AOP, Aspects, Instrumentation | Cross-cutting concerns, AspectJ integration |",
    "| **Security** | Spring Security | Authentication, authorization, CSRF protection |",
    "| **Test** | Spring Test | Unit and integration testing support |",
    "",
    "The **Core Container** is the foundation — it provides the IoC container (`BeanFactory`, `ApplicationContext`) that all other modules build upon.",
    "",
    "### 4. What is Dependency Injection?",
    "",
    "Dependency Injection (DI) is a design pattern where an object's dependencies are provided by an external entity (the container) rather than created by the object itself. Instead of a class calling `new` on its dependencies, the Spring container injects them at runtime.",
    "",
    "```java",
    "// Without DI — tight coupling",
    "public class OrderService {",
    "    private PaymentService paymentService = new PaymentService();",
    "}",
    "",
    "// With DI — loose coupling",
    "public class OrderService {",
    "    private final PaymentService paymentService;",
    "",
    "    public OrderService(PaymentService paymentService) {",
    "        this.paymentService = paymentService;",
    "    }",
    "}",
    "```",
    "",
    "DI promotes loose coupling, easier testing (swap real implementations with mocks), and better separation of concerns.",
    "",
    "### 5. What is Inversion of Control?",
    "",
    "Inversion of Control (IoC) is a broader principle where the control of object creation and lifecycle is transferred from the application code to a framework or container. DI is the most common implementation of IoC. In traditional programming, your code calls library functions; with IoC, the framework calls your code (the \"Hollywood Principle\": don't call us, we'll call you).",
    "",
    "In Spring, the **IoC container** (`ApplicationContext`) manages bean creation, wiring, configuration, and destruction — the developer defines *what* the dependencies are, and the container handles *how* and *when* they are provided.",
    "",
    "### 6. What are the types of Dependency Injection?",
    "",
    "**Constructor Injection** (recommended) — dependencies provided via constructor arguments. Guarantees immutability and that the bean is fully initialized.",
    "",
    "```java",
    "@Service",
    "public class OrderService {",
    "    private final PaymentService paymentService;",
    "",
    "    public OrderService(PaymentService paymentService) {",
    "        this.paymentService = paymentService;",
    "    }",
    "}",
    "```",
    "",
    "**Setter Injection** — dependencies provided via setter methods. Suitable for optional dependencies.",
    "",
    "```java",
    "@Service",
    "public class OrderService {",
    "    private PaymentService paymentService;",
    "",
    "    @Autowired",
    "    public void setPaymentService(PaymentService paymentService) {",
    "        this.paymentService = paymentService;",
    "    }",
    "}",
    "```",
    "",
    "**Field Injection** — dependencies injected directly into fields. Convenient but discouraged in production because it hides dependencies, makes testing harder, and prevents immutability.",
    "",
    "```java",
    "@Service",
    "public class OrderService {",
    "    @Autowired",
    "    private PaymentService paymentService; // avoid in production code",
    "}",
    "```",
    "",
    "### 7. What is a Spring Bean?",
    "",
    "A Spring Bean is any object that is instantiated, configured, and managed by the Spring IoC container. Beans are defined through annotations (`@Component`, `@Service`, `@Bean`, etc.) or XML configuration. The container is responsible for their full lifecycle — from creation to destruction. Any POJO can become a Spring Bean when registered with the container.",
    "",
    "### 8. What are the different scopes of Spring Bean?",
    "",
    "| Scope | Description | When Destroyed |",
    "|---|---|---|",
    "| **singleton** | One instance per Spring container (default) | Container shutdown |",
    "| **prototype** | New instance every time the bean is requested | Not managed by container |",
    "| **request** | One instance per HTTP request (web only) | Request completes |",
    "| **session** | One instance per HTTP session (web only) | Session expires |",
    "| **application** | One instance per `ServletContext` (web only) | Application shutdown |",
    "| **websocket** | One instance per WebSocket session (web only) | WebSocket closes |",
    "",
    "### 9. What is the default scope of a Spring Bean?",
    "",
    "The default scope is **singleton**. The Spring container creates exactly one instance of the bean and caches it — every subsequent injection or lookup returns the same object. This is different from the GoF Singleton pattern because singleton scope is per-container, not per-JVM.",
    "",
    "### 10. What is the Bean lifecycle in Spring?",
    "",
    "1. **Instantiation** — container creates the bean instance",
    "2. **Property Population** — dependencies are injected",
    "3. **`BeanNameAware.setBeanName()`** — bean receives its name",
    "4. **`BeanFactoryAware.setBeanFactory()`** — bean receives the factory reference",
    "5. **`ApplicationContextAware.setApplicationContext()`** — bean receives the context",
    "6. **`BeanPostProcessor.postProcessBeforeInitialization()`** — pre-init processing",
    "7. **`@PostConstruct`** — annotated method called",
    "8. **`InitializingBean.afterPropertiesSet()`** — callback interface",
    "9. **Custom `init-method`** — XML-configured initialization method",
    "10. **`BeanPostProcessor.postProcessAfterInitialization()`** — post-init processing",
    "11. **Bean is ready for use**",
    "12. **`@PreDestroy`** — annotated method called (on shutdown)",
    "13. **`DisposableBean.destroy()`** — callback interface",
    "14. **Custom `destroy-method`** — XML-configured destruction method",
    "",
    "### 11. What is the difference between BeanFactory and ApplicationContext?",
    "",
    "| Feature | BeanFactory | ApplicationContext |",
    "|---|---|---|",
    "| **Bean Loading** | Lazy (on first request) | Eager (at startup) |",
    "| **Event Publishing** | No | Yes (`ApplicationEvent`) |",
    "| **Internationalization** | No | Yes (`MessageSource`) |",
    "| **AOP Integration** | Manual | Automatic |",
    "| **`BeanPostProcessor` registration** | Manual | Automatic |",
    "| **Annotation Support** | Limited | Full |",
    "| **Use Case** | Resource-constrained environments | Enterprise applications |",
    "",
    "`ApplicationContext` extends `BeanFactory` and is the preferred choice for virtually all applications.",
    "",
    "### 12. What is Autowiring in Spring?",
    "",
    "Autowiring allows the Spring container to automatically resolve and inject bean dependencies without explicit configuration. Instead of manually wiring every dependency via XML or `@Bean` methods, the container infers which beans to inject based on type, name, or constructor signatures.",
    "",
    "### 13. What are the different modes of Autowiring?",
    "",
    "| Mode | Description |",
    "|---|---|",
    "| **no** | Default in XML. No autowiring; dependencies must be explicitly configured. |",
    "| **byName** | Matches bean property name to a bean id/name in the container. |",
    "| **byType** | Matches by the type of the property. Throws exception if multiple candidates exist. |",
    "| **constructor** | Similar to byType but applies to constructor arguments. |",
    "| **autodetect** | (Deprecated in Spring 3+) Tried constructor first, then byType. |",
    "",
    "In annotation-based configuration, `@Autowired` uses **byType** by default, falling back to **byName** when combined with `@Qualifier`.",
    "",
    "### 14. What is the use of @Autowired annotation?",
    "",
    "`@Autowired` tells the Spring container to automatically inject a matching bean dependency. It can be placed on constructors, setters, or fields. By default it resolves **by type**; if multiple beans of the same type exist, use `@Qualifier` to disambiguate. Set `required = false` for optional dependencies. Since Spring 4.3, `@Autowired` is optional on a class with a single constructor.",
    "",
    "### 15. What is the difference between constructor injection and setter injection?",
    "",
    "| Aspect | Constructor Injection | Setter Injection |",
    "|---|---|---|",
    "| **Immutability** | Fields can be `final` | Fields must be mutable |",
    "| **Required Dependencies** | Enforced at compile time | Can be `null` at runtime |",
    "| **Circular Dependencies** | Detected at startup (fails fast) | Resolved via proxy/lazy |",
    "| **Readability** | All deps visible in constructor | Scattered across setters |",
    "| **Testing** | Easy — pass mocks via constructor | Requires calling setters |",
    "| **Use Case** | Mandatory dependencies | Optional/changeable dependencies |",
    "",
    "**Best practice**: Use constructor injection for mandatory dependencies and setter injection only for truly optional ones.",
    "",
    "### 16. How can you inject a Java Collection in Spring?",
    "",
    "Spring supports injecting `List`, `Set`, `Map`, and `Properties` collections. Using `@Autowired`, Spring injects all beans of a matching type:",
    "",
    "```java",
    "@Service",
    "public class NotificationService {",
    "    private final List<NotificationChannel> channels;",
    "",
    "    @Autowired",
    "    public NotificationService(List<NotificationChannel> channels) {",
    "        this.channels = channels; // all NotificationChannel beans",
    "    }",
    "}",
    "```",
    "",
    "Collections can also be injected via XML `<list>`, `<set>`, `<map>`, `<props>` elements, or from property files using `@Value` with SpEL.",
    "",
    "### 17. What is the role of @Component, @Repository, @Service, and @Controller annotations?",
    "",
    "All four are **stereotype annotations** detected via component scanning. They share the same DI behavior but carry semantic meaning:",
    "",
    "| Annotation | Layer | Extra Behavior |",
    "|---|---|---|",
    "| `@Component` | Generic | Base annotation — no extra behavior |",
    "| `@Repository` | Data Access | **Automatic exception translation** (JDBC/JPA exceptions → Spring's `DataAccessException`) |",
    "| `@Service` | Business Logic | No extra behavior; marks service-layer intent |",
    "| `@Controller` | Web / Presentation | Marks as MVC controller; enables `@RequestMapping` handling |",
    "",
    "### 18. What is the difference between @Component and @Bean?",
    "",
    "| Aspect | @Component | @Bean |",
    "|---|---|---|",
    "| **Target** | Class-level | Method-level (inside `@Configuration`) |",
    "| **Detection** | Auto-detected via component scanning | Explicitly declared |",
    "| **Use Case** | Your own classes | Third-party library classes you can't annotate |",
    "| **Customization** | Limited | Full programmatic control over instantiation |",
    "",
    "Use `@Component` for your own classes and `@Bean` for third-party classes (e.g., `RestTemplate`, `ObjectMapper`).",
    "",
    "### 19. How do you define a Bean in Spring using XML?",
    "",
    "```xml",
    "<beans xmlns=\"http://www.springframework.org/schema/beans\" ...>",
    "    <bean id=\"paymentService\" class=\"com.example.PaymentService\"/>",
    "",
    "    <bean id=\"orderService\" class=\"com.example.OrderService\">",
    "        <constructor-arg ref=\"paymentService\"/>",
    "        <property name=\"maxRetries\" value=\"3\"/>",
    "    </bean>",
    "</beans>",
    "```",
    "",
    "XML configuration is largely replaced by Java-based config in modern Spring but is still used in legacy projects.",
    "",
    "### 20. How do you define a Bean using Java-based configuration?",
    "",
    "```java",
    "@Configuration",
    "public class AppConfig {",
    "",
    "    @Bean",
    "    public PaymentService paymentService() {",
    "        return new PaymentService();",
    "    }",
    "",
    "    @Bean",
    "    public OrderService orderService() {",
    "        return new OrderService(paymentService());",
    "    }",
    "}",
    "```",
    "",
    "This is the preferred modern approach — type-safe, refactor-friendly, and supports IDE navigation.",
    "",
    "### 21. What is the use of @Configuration and @Bean annotations?",
    "",
    "`@Configuration` marks a class as a source of bean definitions. Spring creates a **CGLIB proxy** of the class so that calls to `@Bean` methods return the existing singleton instance instead of creating a new object. This guarantees singleton semantics for inter-bean references.",
    "",
    "`@Bean` marks a method whose return value is registered as a bean. Without `@Configuration` (using `@Component` + `@Bean` — \"lite mode\"), inter-bean method calls create new instances each time because no CGLIB proxy is applied.",
    "",
    "### 22. What is the use of @Qualifier annotation?",
    "",
    "`@Qualifier` resolves ambiguity when multiple beans of the same type exist by specifying which bean to inject by name:",
    "",
    "```java",
    "@Service",
    "public class AlertService {",
    "    @Autowired",
    "    public AlertService(@Qualifier(\"smsSender\") NotificationSender sender) {",
    "        this.sender = sender;",
    "    }",
    "}",
    "```",
    "",
    "Without `@Qualifier`, Spring throws `NoUniqueBeanDefinitionException`.",
    "",
    "### 23. What is the use of @Primary annotation?",
    "",
    "`@Primary` designates a bean as the **default candidate** when multiple beans of the same type exist and no `@Qualifier` is specified. `@Qualifier` takes precedence over `@Primary` when both are present.",
    "",
    "### 24. What is the difference between @PostConstruct and @PreDestroy?",
    "",
    "- **`@PostConstruct`** — called **after** dependency injection is complete. Used for initialization logic that depends on injected fields.",
    "- **`@PreDestroy`** — called **before** the container destroys the bean. Used for cleanup (closing connections, releasing resources).",
    "",
    "Both are JSR-250 annotations. `@PreDestroy` does **not** apply to prototype-scoped beans.",
    "",
    "### 25. What is the use of init-method and destroy-method in XML configuration?",
    "",
    "They serve the same purpose as `@PostConstruct`/`@PreDestroy` but via XML — useful when you cannot modify source code. **Execution order**: `@PostConstruct` → `InitializingBean.afterPropertiesSet()` → `init-method`.",
    "",
    "### 26. What is the use of FactoryBean in Spring?",
    "",
    "`FactoryBean<T>` customizes complex bean creation logic. The container calls `getObject()` to produce the actual bean. Use `context.getBean(\"name\")` for the produced object and `context.getBean(\"&name\")` for the factory itself. Spring uses this internally for `ProxyFactoryBean`, `LocalSessionFactoryBean`, etc.",
    "",
    "### 27. What is the use of ApplicationContextAware interface?",
    "",
    "It provides a bean with a reference to the `ApplicationContext` for programmatic bean lookup, resource access, or event publishing. **Prefer `@Autowired` injection** in most cases — use `ApplicationContextAware` only for dynamic/runtime bean resolution.",
    "",
    "### 28. What is the use of BeanPostProcessor interface?",
    "",
    "`BeanPostProcessor` modifies or wraps bean **instances** during initialization. It intercepts **every bean** in the container with `postProcessBeforeInitialization()` and `postProcessAfterInitialization()`. Spring uses it internally for `@Autowired` processing, `@PostConstruct` handling, and AOP proxy creation.",
    "",
    "### 29. What is the use of BeanFactoryPostProcessor interface?",
    "",
    "`BeanFactoryPostProcessor` modifies bean **definitions** (metadata) **before** any beans are instantiated. It can alter properties, scope, or lazy-init settings. Spring's `PropertySourcesPlaceholderConfigurer` (resolving `${...}` placeholders) is a well-known example.",
    "",
    "### 30. What is the difference between ApplicationContext and WebApplicationContext?",
    "",
    "| Aspect | ApplicationContext | WebApplicationContext |",
    "|---|---|---|",
    "| **Scope** | General-purpose | Web applications only |",
    "| **Servlet Access** | No `ServletContext` | Has `getServletContext()` |",
    "| **Bean Scopes** | `singleton`, `prototype` | Adds `request`, `session`, `application`, `websocket` |",
    "| **Hierarchy** | Root context | Child of root (inherits beans) |",
    "",
    "In a typical Spring web app, the root `ApplicationContext` holds services/repositories and the child `WebApplicationContext` (created by `DispatcherServlet`) holds controllers/view resolvers."
  ].join("\n"),

  "Spring Boot": [
    "## Spring Boot",
    "",
    "### 1. What is Spring Boot?",
    "",
    "Spring Boot is an opinionated framework built on top of the Spring Framework that simplifies the creation of production-ready applications. It follows **convention over configuration**, providing auto-configuration, embedded servers (Tomcat/Jetty/Undertow), and starter dependencies so developers can get started with minimal setup.",
    "",
    "### 2. What are the advantages of using Spring Boot?",
    "",
    "- **Auto-configuration** — automatically configures beans based on classpath dependencies",
    "- **Embedded servers** — no need to deploy WAR files",
    "- **Starter dependencies** — curated dependency sets with compatible versions",
    "- **Production-ready features** — health checks, metrics via Actuator",
    "- **No XML required** — annotation-driven configuration",
    "- **Rapid development** — Spring Initializr generates runnable projects in seconds",
    "- **Opinionated defaults** that can be overridden when needed",
    "",
    "### 3. What are the key features of Spring Boot?",
    "",
    "Auto-configuration, starter POMs, embedded server, Spring Boot Actuator, Spring Boot CLI, externalized configuration (`application.properties`/`application.yml`/env vars), Spring Boot DevTools (auto-restart, live reload).",
    "",
    "### 4. What is the difference between Spring and Spring Boot?",
    "",
    "| Aspect | Spring Framework | Spring Boot |",
    "|---|---|---|",
    "| Configuration | Requires explicit config | Auto-configuration with defaults |",
    "| Server | External server (WAR deploy) | Embedded server (executable JAR) |",
    "| Dependencies | Manual version management | Starter dependencies |",
    "| Boilerplate | Significant | Minimal |",
    "| Production features | Manual setup | Actuator built-in |",
    "",
    "Spring Boot is a layer on top that removes the ceremony of configuring Spring.",
    "",
    "### 5. What is Spring Boot Starter?",
    "",
    "A Starter is a dependency descriptor that bundles related libraries needed for a capability. Add one starter and get all transitive dependencies with compatible versions: `spring-boot-starter-web` (Tomcat + Spring MVC + Jackson), `spring-boot-starter-data-jpa` (Hibernate + HikariCP), `spring-boot-starter-test` (JUnit 5 + Mockito + AssertJ).",
    "",
    "### 6. What is Spring Boot Auto Configuration?",
    "",
    "Auto-configuration creates and configures beans based on classpath JARs and defined properties. It uses `@Conditional` annotations (`@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`). **Your beans always win** — if you define a bean of the same type, auto-configuration backs off. Debug with `--debug` flag or `/actuator/conditions`.",
    "",
    "### 7. What is Spring Boot CLI?",
    "",
    "A command-line tool for quickly prototyping Spring apps using Groovy scripts. Run `spring run hello.groovy` — dependencies are resolved automatically. Useful for prototyping, not for production.",
    "",
    "### 8. What is Spring Initializer?",
    "",
    "Spring Initializr ([start.spring.io](https://start.spring.io)) is a web-based project generator. Select build tool, language, Spring Boot version, and starters — it generates a downloadable ZIP with project structure and a ready-to-run main class. Also available via IDE and CLI.",
    "",
    "### 9. What is Spring Boot Actuator?",
    "",
    "Actuator provides production-ready endpoints for monitoring: `/actuator/health` (application health), `/actuator/metrics` (JVM and custom metrics), `/actuator/info` (application info), `/actuator/env` (environment properties). Supports custom health indicators and integrates with Prometheus/Grafana.",
    "",
    "### 10. What is the use of @SpringBootApplication annotation?",
    "",
    "It combines three annotations: `@Configuration` (bean definition source), `@EnableAutoConfiguration` (enables auto-config), and `@ComponentScan` (scans current package and sub-packages). Place it on your main class in the root package.",
    "",
    "### 11. What is the use of @EnableAutoConfiguration annotation?",
    "",
    "Tells Spring Boot to auto-configure beans based on classpath dependencies. Already included in `@SpringBootApplication`. Exclude specific auto-configs with `exclude = {DataSourceAutoConfiguration.class}`.",
    "",
    "### 12. What is the use of @ComponentScan annotation?",
    "",
    "Tells Spring which packages to scan for `@Component`, `@Service`, `@Repository`, `@Controller` classes. Included in `@SpringBootApplication`, which scans the annotated class's package and sub-packages by default.",
    "",
    "### 13. What is the use of @Configuration annotation in Spring Boot?",
    "",
    "Marks a class as a source of `@Bean` definitions. Spring creates a CGLIB proxy to ensure singleton semantics on inter-bean method calls. Use it for custom beans that auto-configuration doesn't cover (e.g., `RestTemplate`, `ObjectMapper`).",
    "",
    "### 14. How do you create a Spring Boot application?",
    "",
    "Use Spring Initializr ([start.spring.io](https://start.spring.io)), IDE integration, Spring Boot CLI (`spring init`), or manually create a Gradle/Maven project with `spring-boot-starter-parent` and a `@SpringBootApplication` main class.",
    "",
    "### 15. How do you run a Spring Boot application?",
    "",
    "From IDE (run `main()`), Maven (`./mvnw spring-boot:run`), Gradle (`./gradlew bootRun`), or as an executable JAR (`java -jar target/my-app.jar`).",
    "",
    "### 16. How do you change the default port in Spring Boot?",
    "",
    "`server.port=9090` in `application.properties`, `--server.port=9090` via command line, `SERVER_PORT=9090` env var, or programmatically. Set `server.port=0` for a random port.",
    "",
    "### 17. How do you disable a specific auto-configuration in Spring Boot?",
    "",
    "```java",
    "@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})",
    "```",
    "",
    "Or via properties: `spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration`",
    "",
    "### 18. What are the different ways to provide configuration to a Spring Boot application?",
    "",
    "Precedence (highest first): command-line args → Java system properties → OS env vars → profile-specific properties → `application.properties`/`application.yml` → `@PropertySource` → default properties.",
    "",
    "### 19. What is application.properties file in Spring Boot?",
    "",
    "The default configuration file at `src/main/resources`. Uses `key=value` format for server settings, database connection, JPA, logging, and custom properties. Loaded automatically at startup.",
    "",
    "### 20. What is application.yml file in Spring Boot?",
    "",
    "YAML alternative to `application.properties` with hierarchical format. Avoids prefix repetition, supports multi-profile blocks with `---` separators, and native list/map syntax. Both formats are functionally equivalent; `.properties` takes precedence when both exist.",
    "",
    "### 21. How do you profile configuration in Spring Boot?",
    "",
    "Create `application-{profile}.properties` files (e.g., `application-dev.properties`, `application-prod.properties`). Activate with `spring.profiles.active=dev` in properties, `--spring.profiles.active=prod` via command line, or `SPRING_PROFILES_ACTIVE=prod` env var. Use `@Profile(\"dev\")` to conditionally register beans.",
    "",
    "### 22. What is the use of @Value annotation in Spring Boot?",
    "",
    "Injects individual property values: `@Value(\"${app.name}\")`. Supports defaults (`@Value(\"${timeout:5000}\")`), SpEL expressions, and environment variables. Best for a small number of individual values; use `@ConfigurationProperties` for groups.",
    "",
    "### 23. What is the use of @ConfigurationProperties annotation?",
    "",
    "Binds a group of related properties to a type-safe POJO with `@ConfigurationProperties(prefix = \"app.mail\")`. Supports `@Validated` with Jakarta annotations for startup validation, IDE autocomplete, and nested objects. Preferred over `@Value` for structured configuration.",
    "",
    "### 24. How do you connect Spring Boot to a database?",
    "",
    "Add `spring-boot-starter-data-jpa` + JDBC driver dependency, then configure `spring.datasource.url`, `username`, `password` in `application.properties`. Spring Boot auto-configures `DataSource` (HikariCP), `EntityManagerFactory`, and `TransactionManager`.",
    "",
    "### 25. What is Spring Data JPA in Spring Boot?",
    "",
    "Generates repository implementations from interface definitions. Define method signatures like `findByEmail(String email)` and Spring writes the SQL. Supports derived queries, `@Query` for custom JPQL/native SQL, and pagination/sorting out of the box.",
    "",
    "### 26. What is the use of @Entity annotation?",
    "",
    "Maps a Java class to a database table. Each instance = a row, each field = a column. Requires `@Id` for primary key and a no-arg constructor.",
    "",
    "### 27. What is the use of @Repository annotation?",
    "",
    "Specialization of `@Component` for data access. Enables exception translation (vendor-specific exceptions → `DataAccessException`). When using Spring Data JPA interfaces, `@Repository` is implicit.",
    "",
    "### 28. What is the use of CrudRepository and JpaRepository?",
    "",
    "`CrudRepository` provides basic CRUD operations. `JpaRepository` extends it with pagination, sorting, batch operations, flush, and query-by-example. **Use `JpaRepository` by default.**",
    "",
    "| Feature | CrudRepository | JpaRepository |",
    "|---|---|---|",
    "| Basic CRUD | ✅ | ✅ |",
    "| Pagination/Sorting | ❌ | ✅ |",
    "| Batch operations | ❌ | ✅ |",
    "| Return types | `Iterable` | `List` |",
    "",
    "### 29. How do you handle exceptions in Spring Boot?",
    "",
    "Use `@RestControllerAdvice` with `@ExceptionHandler` methods for global exception handling. Define custom exception classes with `@ResponseStatus`. Catch `MethodArgumentNotValidException` for validation errors. Return structured error response objects.",
    "",
    "### 30. What is the use of @ControllerAdvice annotation?",
    "",
    "Declares a global handler for all controllers. Centralizes exception handling, model attributes, and data binding. `@RestControllerAdvice` combines it with `@ResponseBody` for REST APIs. Can be scoped to specific packages with `basePackages`.",
    "",
    "### 31. What is the use of @RestController annotation?",
    "",
    "`@RestController` = `@Controller` + `@ResponseBody`. Every method returns data (serialized to JSON) instead of a view name. Use for REST APIs; use `@Controller` for server-rendered HTML pages.",
    "",
    "### 32. What is the use of @RequestMapping annotation?",
    "",
    "Maps HTTP requests to handler methods by URL, HTTP method, content type, and headers. Applied at class level (base path) and method level (specific path). In practice, shortcut annotations (`@GetMapping`, etc.) are preferred.",
    "",
    "### 33. What is the use of @GetMapping, @PostMapping, @PutMapping, @DeleteMapping?",
    "",
    "Shortcut annotations for `@RequestMapping` restricted to a specific HTTP method. `@GetMapping(\"/path\")` equals `@RequestMapping(value=\"/path\", method=GET)`. Improves readability.",
    "",
    "### 34. What is the use of @PathVariable and @RequestParam?",
    "",
    "`@PathVariable` extracts from URL path (`/users/{id}`). `@RequestParam` extracts from query string (`?key=val`). `@RequestParam` supports `required` and `defaultValue`. Convention: path variables identify resources, query params filter/modify.",
    "",
    "### 35. What is the use of @ResponseBody annotation?",
    "",
    "Serializes the return value directly to the HTTP response body (JSON/XML) via `HttpMessageConverter`, bypassing view resolution. Implicit in `@RestController`.",
    "",
    "### 36. What is the use of @RequestBody annotation?",
    "",
    "Deserializes the HTTP request body (JSON) into a Java object. Used with POST/PUT/PATCH. Requires `Content-Type: application/json`. Combine with `@Valid` for validation.",
    "",
    "### 37. What is the use of @Valid annotation?",
    "",
    "Triggers Jakarta Bean Validation on the parameter. Constraints like `@NotBlank`, `@Email`, `@Min` on DTO fields are checked before the method executes. Failures throw `MethodArgumentNotValidException` (400).",
    "",
    "### 38. How do you implement logging in Spring Boot?",
    "",
    "Spring Boot uses SLF4J + Logback by default. Use `LoggerFactory.getLogger(MyClass.class)` or Lombok's `@Slf4j`. Configure levels in `application.properties`: `logging.level.com.example=DEBUG`. Use `{}` placeholders for efficient message formatting.",
    "",
    "### 39. What is the default logging framework in Spring Boot?",
    "",
    "**SLF4J** as the API facade with **Logback** as the implementation, included transitively via every starter. Spring Boot bridges other logging frameworks (JUL, Log4j, Commons Logging) to SLF4J. Switch to Log4j2 by excluding `spring-boot-starter-logging` and adding `spring-boot-starter-log4j2`.",
    "",
    "### 40. How do you monitor a Spring Boot application?",
    "",
    "Spring Boot Actuator exposes `/actuator/health`, `/actuator/metrics`, `/actuator/prometheus`. Integrate with Micrometer for Prometheus/Grafana, Spring Boot Admin for web UI, or Micrometer Tracing for distributed tracing with Zipkin/Jaeger.",
    "",
    "### 41. What is the use of Spring Boot DevTools?",
    "",
    "Provides **automatic restart** (uses two classloaders — only your code is reloaded), **LiveReload** (auto-refreshes browser), and **development-friendly defaults** (disables template caching). Automatically disabled in production builds.",
    "",
    "### 42. How do you secure a Spring Boot application?",
    "",
    "Add `spring-boot-starter-security` — all endpoints are secured with HTTP Basic by default. Customize with a `SecurityFilterChain` bean: `.requestMatchers(\"/public/**\").permitAll()`, `.requestMatchers(\"/admin/**\").hasRole(\"ADMIN\")`, `.sessionManagement(STATELESS)` for JWT.",
    "",
    "### 43. What is Spring Security in Spring Boot?",
    "",
    "Framework providing authentication, authorization, and protection against common attacks. Auto-configures all endpoints as secured, default user \"user\" with generated password, CSRF protection, and security headers. Customize with `SecurityFilterChain`, `UserDetailsService`, and `PasswordEncoder` beans.",
    "",
    "### 44. How do you test a Spring Boot application?",
    "",
    "`spring-boot-starter-test` includes JUnit 5, Mockito, AssertJ, Spring Test, MockMvc. Use **`@SpringBootTest`** for full integration tests, **`@WebMvcTest`** for controller-only tests with MockMvc, **`@DataJpaTest`** for repository tests with embedded DB, and **`@MockBean`** to replace beans with mocks.",
    "",
    "### 45. What is the use of @SpringBootTest annotation?",
    "",
    "Creates the **full application context** for integration testing. Options: `MOCK` (mock servlet, use MockMvc), `RANDOM_PORT` (real server, use TestRestTemplate), `DEFINED_PORT`, `NONE`. Use sparingly — prefer slice annotations for faster, focused tests."
  ].join("\n"),

  "Spring AOP": [
    "## Spring AOP",
    "",
    "### 1. What is AOP in Spring?",
    "",
    "**Aspect-Oriented Programming** separates cross-cutting concerns from business logic. Instead of scattering logging, security, or transaction code across every service method, you define that behavior once in an **aspect** and apply it declaratively via pointcut expressions. Spring AOP uses runtime proxies to intercept method calls.",
    "",
    "```java",
    "@Aspect",
    "@Component",
    "public class LoggingAspect {",
    "    @Before(\"execution(* com.example.service.*.*(..))\")",
    "    public void logBefore(JoinPoint jp) {",
    "        System.out.println(\"Calling: \" + jp.getSignature());",
    "    }",
    "}",
    "```",
    "",
    "### 2. What are cross-cutting concerns?",
    "",
    "Behaviors that span multiple layers and cannot be cleanly modularized with standard OOP: **logging**, **security**, **transaction management**, **caching**, **exception handling**, **performance monitoring**. Without AOP, these concerns lead to duplicated, tangled code.",
    "",
    "### 3. What are the different types of advice in Spring AOP?",
    "",
    "| Advice | Annotation | When It Runs |",
    "|---|---|---|",
    "| **Before** | `@Before` | Before the target method |",
    "| **After Returning** | `@AfterReturning` | After successful return |",
    "| **After Throwing** | `@AfterThrowing` | After an exception |",
    "| **After (Finally)** | `@After` | After method completes (success or failure) |",
    "| **Around** | `@Around` | Wraps the method; controls execution |",
    "",
    "### 4. What is the difference between @Before and @After annotations?",
    "",
    "`@Before` runs **before** the target method — used for validation, security checks, logging entry. `@After` runs **after** the method regardless of outcome (like a `finally` block) — used for cleanup and logging exit. `@After` cannot access the return value (use `@AfterReturning` for that).",
    "",
    "### 5. What is the use of @Around advice?",
    "",
    "`@Around` is the most powerful advice. It wraps the entire method using `ProceedingJoinPoint`, giving control to: execute code before/after, skip execution, retry on failure, modify arguments, modify the return value, and measure execution time.",
    "",
    "```java",
    "@Around(\"execution(* com.example.service.*.*(..))\")",
    "public Object measure(ProceedingJoinPoint pjp) throws Throwable {",
    "    long start = System.currentTimeMillis();",
    "    Object result = pjp.proceed();",
    "    System.out.println(pjp.getSignature() + \" took \" + (System.currentTimeMillis() - start) + \"ms\");",
    "    return result;",
    "}",
    "```",
    "",
    "### 6. What is a JoinPoint in Spring AOP?",
    "",
    "A JoinPoint represents a point during execution where an aspect can be applied — always **method executions** in Spring AOP. The `JoinPoint` interface provides metadata: `getSignature()` (method name), `getArgs()` (arguments), `getTarget()` (target object). `ProceedingJoinPoint` extends it with `proceed()` for `@Around` only.",
    "",
    "### 7. What is a Pointcut in Spring AOP?",
    "",
    "A Pointcut is an expression that selects which joinpoints an advice applies to. Common designators: `execution()` (method pattern), `within()` (class/package), `@annotation()` (annotated methods), `bean()` (by bean name). Combine with `&&`, `||`, `!`.",
    "",
    "```java",
    "@Pointcut(\"execution(* com.example.service.*.*(..))\")",
    "public void serviceMethods() {}",
    "",
    "@Before(\"serviceMethods() && !execution(* get*(..))\")",
    "public void logNonGetters(JoinPoint jp) { ... }",
    "```",
    "",
    "### 8. What is an Aspect in Spring AOP?",
    "",
    "An Aspect is a class that encapsulates a cross-cutting concern by combining **pointcuts** (where) with **advice** (what). Annotated with `@Aspect` + `@Component`. Each concern gets its own aspect: `LoggingAspect`, `SecurityAspect`, `CachingAspect`.",
    "",
    "### 9. What is the use of @Aspect annotation?",
    "",
    "Marks a class as an AOP aspect so Spring recognizes its advice and pointcut definitions. Requires `@Component` for bean registration and `@EnableAspectJAutoProxy` on a config class (auto-enabled in Spring Boot with `spring-boot-starter-aop`).",
    "",
    "### 10. What is the difference between Spring AOP and AspectJ?",
    "",
    "| Feature | Spring AOP | AspectJ |",
    "|---|---|---|",
    "| **Weaving** | Runtime (proxy-based) | Compile/load-time (bytecode) |",
    "| **Joinpoints** | Method execution only | Methods, constructors, fields |",
    "| **Self-invocation** | Not intercepted | Intercepted |",
    "| **Final classes** | Cannot proxy (CGLIB) | Can weave |",
    "| **Performance** | Slight proxy overhead | Zero runtime overhead |",
    "| **Complexity** | Simple, no tooling | Requires ajc compiler |",
    "",
    "Spring AOP covers 90%+ of use cases. Use AspectJ only for non-public methods, self-invocations, or non-Spring objects.",
    "",
    "### 11. What is weaving in AOP?",
    "",
    "Weaving is the process of linking aspects with target objects. Spring AOP uses **runtime weaving** — when a bean is created, Spring wraps it in a proxy that intercepts method calls and invokes advice. Other types: **compile-time** (AspectJ compiler) and **load-time** (Java agent).",
    "",
    "### 12. What is proxy in Spring AOP?",
    "",
    "A proxy wraps the target bean to intercept method calls. **JDK Dynamic Proxy**: used when the bean implements an interface. **CGLIB Proxy**: creates a subclass (Spring Boot default). Cannot proxy `final` classes/methods. External calls go through the proxy; internal (`this.method()`) calls bypass it.",
    "",
    "### 13. What are the limitations of Spring AOP?",
    "",
    "1. **Method execution only** — no field/constructor interception",
    "2. **Public methods only** (effectively)",
    "3. **Self-invocation not intercepted** — `this.method()` bypasses proxy",
    "4. **Spring beans only** — can't advise non-managed objects",
    "5. **No final classes/methods** with CGLIB",
    "6. **Slight runtime overhead** per proxy call",
    "7. **Limited pointcut expressiveness** compared to full AspectJ"
  ].join("\n"),

  "Spring JDBC": [
    "## Spring JDBC",
    "",
    "### 1. What is Spring JDBC?",
    "",
    "Spring JDBC simplifies database access by eliminating JDBC boilerplate — connection management, statement creation, result set iteration, exception handling, and resource cleanup. The `spring-jdbc` module provides `JdbcTemplate`, `NamedParameterJdbcTemplate`, `SimpleJdbcInsert`/`SimpleJdbcCall`, and automatic exception translation.",
    "",
    "### 2. What is JdbcTemplate in Spring?",
    "",
    "The core class in Spring JDBC. **Thread-safe**, instantiated once and shared. Handles connection acquisition, statement creation, execution, result set processing, exception translation, and resource cleanup.",
    "",
    "```java",
    "@Repository",
    "public class UserRepository {",
    "    private final JdbcTemplate jdbc;",
    "",
    "    public UserRepository(JdbcTemplate jdbc) { this.jdbc = jdbc; }",
    "",
    "    public User findById(Long id) {",
    "        return jdbc.queryForObject(\"SELECT * FROM users WHERE id = ?\",",
    "            (rs, rowNum) -> new User(rs.getLong(\"id\"), rs.getString(\"name\")), id);",
    "    }",
    "",
    "    public int save(User u) {",
    "        return jdbc.update(\"INSERT INTO users (name, email) VALUES (?, ?)\", u.getName(), u.getEmail());",
    "    }",
    "}",
    "```",
    "",
    "### 3. What are the advantages of using JdbcTemplate?",
    "",
    "- Automatic connection, statement, and resource management",
    "- Automatic exception translation (`SQLException` → `DataAccessException`)",
    "- Thread-safe — one instance for the whole application",
    "- Integrates with Spring's `@Transactional`",
    "- Reduces code by 60-80% compared to raw JDBC",
    "- Parameter binding prevents SQL injection",
    "",
    "### 4. What is the difference between JdbcTemplate and NamedParameterJdbcTemplate?",
    "",
    "`JdbcTemplate` uses positional `?` parameters. `NamedParameterJdbcTemplate` uses `:name` parameters — more readable with many params and supports `IN` clauses with `List` values directly.",
    "",
    "```java",
    "// JdbcTemplate: \"WHERE dept = ? AND salary > ?\"",
    "// NamedParameterJdbcTemplate: \"WHERE dept = :dept AND salary > :minSalary\"",
    "// IN clause: \"WHERE id IN (:ids)\" with .addValue(\"ids\", List.of(1, 2, 3))",
    "```",
    "",
    "### 5. What is RowMapper in Spring JDBC?",
    "",
    "Maps **one row** of a `ResultSet` to **one object**. Called once per row. Define as a class, lambda, or use `BeanPropertyRowMapper` for automatic column-to-property matching (supports `snake_case` → `camelCase`).",
    "",
    "```java",
    "List<User> users = jdbc.query(\"SELECT * FROM users\",",
    "    (rs, rowNum) -> new User(rs.getLong(\"id\"), rs.getString(\"name\")));",
    "```",
    "",
    "### 6. What is ResultSetExtractor in Spring JDBC?",
    "",
    "Processes the **entire ResultSet** in one call — you control the `while(rs.next())` loop. Ideal for complex mappings like one-to-many relationships where `RowMapper` would produce duplicates.",
    "",
    "| | RowMapper | ResultSetExtractor |",
    "|---|---|---|",
    "| Called | Once per row | Once for entire ResultSet |",
    "| Best for | Flat list of objects | Aggregated/grouped results, one-to-many |",
    "",
    "### 7. How do you handle exceptions in Spring JDBC?",
    "",
    "Spring automatically translates `SQLException` into a rich hierarchy of unchecked `DataAccessException` subtypes: `DuplicateKeyException`, `DataIntegrityViolationException`, `EmptyResultDataAccessException`, `BadSqlGrammarException`, etc. This makes exception handling vendor-agnostic.",
    "",
    "### 8. How do you perform batch updates in Spring JDBC?",
    "",
    "`batchUpdate()` sends multiple statements in one round-trip. Use `BatchPreparedStatementSetter` with `JdbcTemplate` or `SqlParameterSourceUtils.createBatch()` with `NamedParameterJdbcTemplate`. For very large batches, process in chunks.",
    "",
    "### 9. How do you connect to a database using Spring JDBC?",
    "",
    "In **Spring Boot**: configure `spring.datasource.url`, `username`, `password` in `application.properties` — Spring auto-configures `DataSource` and `JdbcTemplate`. In **plain Spring**: define `DataSource` and `JdbcTemplate` beans manually via `@Configuration`.",
    "",
    "### 10. What is the use of DataSource in Spring JDBC?",
    "",
    "`DataSource` (`javax.sql.DataSource`) is a factory for database connections. It enables connection pooling (critical for performance), configuration externalization, transaction integration, and testability. **HikariCP** is the Spring Boot default pool. Spring's `@Transactional` acquires/releases connections from the pool automatically."
  ].join("\n"),

  "Spring Hibernate": [
    "## Spring Hibernate",
    "",
    "### 1. What is Hibernate?",
    "",
    "Hibernate is an open-source **ORM (Object-Relational Mapping)** framework that maps Java objects to database tables. It implements the JPA specification and handles all low-level JDBC interactions — you work with objects and Hibernate generates SQL.",
    "",
    "### 2. What are the advantages of using Hibernate?",
    "",
    "Eliminates SQL boilerplate, provides database portability via dialects, automatic schema generation (`hbm2ddl.auto`), L1/L2 caching, lazy loading, HQL/JPQL for database-independent queries, dirty checking (auto-detects changed entities), and seamless integration with Spring's `@Transactional`.",
    "",
    "### 3. What is ORM in Hibernate?",
    "",
    "ORM maps Java concepts to relational concepts: Class → Table, Object → Row, Field → Column, Relationship (`@OneToMany`) → Foreign Key. Hibernate reads annotations to define these correspondences and generates SQL automatically.",
    "",
    "### 4. What is the difference between Hibernate and JDBC?",
    "",
    "| Aspect | JDBC | Hibernate |",
    "|---|---|---|",
    "| SQL | Write manually | Generated automatically |",
    "| Result mapping | Manual `ResultSet` → Object | Automatic via annotations |",
    "| Caching | None | L1 + L2 |",
    "| Lazy loading | Not supported | Built-in |",
    "| Schema generation | Manual DDL | Auto via `hbm2ddl.auto` |",
    "",
    "### 5. What is HibernateTemplate in Spring?",
    "",
    "A **legacy** helper class (pre-Spring 3.x) that simplified Session management. **Modern code should use `SessionFactory.getCurrentSession()` directly** within `@Transactional` methods.",
    "",
    "### 6. How do you integrate Hibernate with Spring?",
    "",
    "Configure three beans: `DataSource`, `LocalSessionFactoryBean` (with `packagesToScan` and Hibernate properties), and `HibernateTransactionManager`. Enable `@EnableTransactionManagement`. With Spring Boot + JPA, this is auto-configured.",
    "",
    "### 7. What is the use of SessionFactory in Hibernate?",
    "",
    "`SessionFactory` is a **heavyweight, thread-safe, one-per-app** factory holding compiled mappings and the L2 cache. `Session` is a **lightweight, one-per-transaction** unit of work containing the L1 cache. Use `sessionFactory.getCurrentSession()` within `@Transactional` methods.",
    "",
    "### 8. What is the use of @Entity annotation in Hibernate?",
    "",
    "Marks a class as a JPA entity mapped to a database table. Requires a no-arg constructor and an `@Id` field.",
    "",
    "### 9. What is the use of @Table annotation in Hibernate?",
    "",
    "Customizes the table mapping: `@Table(name = \"customers\", schema = \"sales\", uniqueConstraints = ...)`. Without it, the table name defaults to the class name.",
    "",
    "### 10. What is the use of @Id annotation in Hibernate?",
    "",
    "Marks the primary key field. Every entity must have exactly one `@Id` (or `@EmbeddedId`/`@IdClass` for composite keys).",
    "",
    "### 11. What is the use of @GeneratedValue annotation?",
    "",
    "Auto-generates primary key values. Strategies: **IDENTITY** (auto-increment, MySQL/PostgreSQL), **SEQUENCE** (database sequence, preferred for high-throughput), **TABLE** (portable but slow), **AUTO** (provider chooses).",
    "",
    "### 12. What is the difference between get() and load() methods in Hibernate?",
    "",
    "| | `get()` | `load()` |",
    "|---|---|---|",
    "| Return | Actual object or `null` | Proxy (never null) |",
    "| DB hit | Immediate | Deferred until property access |",
    "| Not found | Returns `null` | Throws `ObjectNotFoundException` on access |",
    "",
    "`load()` is useful for setting foreign key references without loading the full entity.",
    "",
    "### 13. What is the difference between save(), persist(), and saveOrUpdate() methods?",
    "",
    "| Method | Return | Behavior | Standard |",
    "|---|---|---|---|",
    "| `save()` | Generated ID | Always inserts | Hibernate-specific |",
    "| `persist()` | `void` | Inserts within transaction | JPA standard |",
    "| `saveOrUpdate()` | `void` | Inserts or updates based on ID | Hibernate-specific |",
    "",
    "**Best practice**: Use `persist()` for new entities, `merge()` for detached entities.",
    "",
    "### 14. What is lazy loading in Hibernate?",
    "",
    "Defers fetching of associated entities until accessed. Default for collections (`@OneToMany`, `@ManyToMany`): `LAZY`. Default for single associations (`@ManyToOne`, `@OneToOne`): `EAGER`.",
    "",
    "**N+1 problem**: Loading N parents triggers N additional queries for children. Fix with `JOIN FETCH` in JPQL, `@EntityGraph`, or `@BatchSize`.",
    "",
    "**`LazyInitializationException`**: Occurs when accessing a lazy association after the Session closes. Ensure access within `@Transactional` boundaries.",
    "",
    "### 15. What is the use of Hibernate Validator?",
    "",
    "Reference implementation of Jakarta Bean Validation. Provides constraint annotations (`@NotNull`, `@NotBlank`, `@Email`, `@Size`, `@Min`, `@Pattern`, `@Past`, `@Future`) for declarative validation. In Spring MVC, triggers automatically with `@Valid` on controller parameters."
  ].join("\n"),

  "Spring MVC": [
    "## Spring MVC",
    "",
    "### 1. What is Spring MVC?",
    "",
    "A web framework built on the Servlet API implementing the **Model-View-Controller** pattern. It provides clean separation between presentation, business logic, and data, using `DispatcherServlet` as a front controller to handle routing, parameter binding, validation, and view resolution.",
    "",
    "### 2. What are the components of Spring MVC architecture?",
    "",
    "1. **DispatcherServlet** — front controller, dispatches requests",
    "2. **HandlerMapping** — maps URLs to controller methods",
    "3. **Controller** — processes request, returns model + view name",
    "4. **ViewResolver** — resolves view name to actual view",
    "5. **View** — renders the response (HTML, JSON)",
    "",
    "Flow: `Client → DispatcherServlet → HandlerMapping → Controller → ViewResolver → View → Response`",
    "",
    "### 3. What is DispatcherServlet in Spring MVC?",
    "",
    "The **front controller** — single entry point for all HTTP requests. Coordinates handler lookup, execution, view resolution, and response rendering. Auto-configured in Spring Boot, mapped to `/` by default.",
    "",
    "### 4. What is the role of HandlerMapping in Spring MVC?",
    "",
    "Determines which controller method handles a request URL. `RequestMappingHandlerMapping` (default) maps based on `@RequestMapping` annotations. Spring builds a routing table at startup.",
    "",
    "### 5. What is the role of Controller in Spring MVC?",
    "",
    "Processes requests, invokes business logic, populates the model, and returns either a view name (server-rendered pages) or response body (REST APIs).",
    "",
    "### 6. What is the role of ViewResolver in Spring MVC?",
    "",
    "Maps logical view names (e.g., `\"home\"`) to actual view files (e.g., `/WEB-INF/views/home.jsp`). Common implementations: `InternalResourceViewResolver` (JSP), `ThymeleafViewResolver`.",
    "",
    "### 7. What is the role of View in Spring MVC?",
    "",
    "Renders model data into the response. Different implementations: `JstlView` (JSP), `ThymeleafView` (Thymeleaf), `MappingJackson2JsonView` (JSON).",
    "",
    "### 8. What is the use of @Controller annotation?",
    "",
    "Specialization of `@Component` for Spring MVC controllers. Handler methods return **view names** resolved by `ViewResolver`.",
    "",
    "### 9. What is the use of @RestController annotation?",
    "",
    "`@Controller` + `@ResponseBody`. Every method returns data serialized to JSON, not view names. Used for REST APIs.",
    "",
    "### 10. What is the use of @RequestMapping annotation?",
    "",
    "Maps HTTP requests to handlers by URL, HTTP method, headers, and content type. Applied at class level (base path) and method level.",
    "",
    "### 11. What is the difference between @RequestMapping and @GetMapping?",
    "",
    "`@GetMapping` is a shortcut for `@RequestMapping(method = RequestMethod.GET)`. Same for `@PostMapping`, `@PutMapping`, `@DeleteMapping`, `@PatchMapping`. The shortcuts are preferred for readability.",
    "",
    "### 12. What is the use of @ModelAttribute annotation?",
    "",
    "**On a parameter**: binds request parameters (form fields) to an object. **On a method**: adds attributes to the Model for all handler methods in the controller (e.g., dropdown values).",
    "",
    "### 13. What is the use of @RequestParam annotation?",
    "",
    "Extracts query string parameters: `@RequestParam String name` from `?name=John`. Supports `required`, `defaultValue`.",
    "",
    "### 14. What is the use of @PathVariable annotation?",
    "",
    "Extracts values from URL path segments: `@PathVariable Long id` from `/users/{id}`. Values are mandatory since they're part of the URI structure.",
    "",
    "### 15. What is the use of @ResponseBody annotation?",
    "",
    "Serializes the return value to the HTTP response body (JSON/XML), bypassing view resolution. Implicit in `@RestController`.",
    "",
    "### 16. What is the use of @RequestBody annotation?",
    "",
    "Deserializes the HTTP request body (JSON) into a Java object. Used with POST/PUT. Combine with `@Valid` for validation.",
    "",
    "### 17. What is the use of @Valid annotation in Spring MVC?",
    "",
    "Triggers Bean Validation on the annotated parameter. Errors are captured in `BindingResult`. For REST APIs, omitting `BindingResult` causes `MethodArgumentNotValidException`.",
    "",
    "### 18. How do you handle form submission in Spring MVC?",
    "",
    "GET handler displays the form with an empty model attribute. POST handler processes with `@Valid @ModelAttribute`. If `BindingResult` has errors, return the form view; otherwise, redirect after save (PRG pattern).",
    "",
    "### 19. How do you perform validation in Spring MVC?",
    "",
    "Three approaches: (1) Bean Validation annotations + `@Valid`, (2) Spring's `Validator` interface for cross-field logic, (3) Custom constraint annotations. All work with `BindingResult`.",
    "",
    "### 20. What is BindingResult in Spring MVC?",
    "",
    "Holds validation and data binding results — field errors, global errors, rejected values. **Must immediately follow the `@Valid`/`@ModelAttribute` parameter** in the method signature. If omitted, Spring throws an exception.",
    "",
    "### 21. How do you create custom validators in Spring MVC?",
    "",
    "**Approach 1**: Implement `Validator` interface with `supports()` and `validate()` for cross-field validation (password match, date ranges, uniqueness checks).",
    "",
    "**Approach 2**: Create a custom constraint annotation with `@Constraint(validatedBy = ...)` backed by a `ConstraintValidator` — reusable across entities.",
    "",
    "### 22. What is the use of Interceptors in Spring MVC?",
    "",
    "Execute logic **before**, **after**, and **upon completion** of controller execution. Used for logging, authentication, locale resolution, performance monitoring. Register via `WebMvcConfigurer.addInterceptors()`.",
    "",
    "### 23. What is HandlerInterceptor in Spring MVC?",
    "",
    "Interface with three methods: `preHandle()` (before controller, return `false` to abort), `postHandle()` (after controller, before view rendering), `afterCompletion()` (after everything, for cleanup/timing).",
    "",
    "### 24. What is the difference between filters and interceptors?",
    "",
    "| Aspect | Filter | HandlerInterceptor |",
    "|---|---|---|",
    "| Spec | Servlet API | Spring MVC |",
    "| Scope | All requests (including static) | Only Spring-dispatched requests |",
    "| Access to | `ServletRequest/Response` only | Handler method, `ModelAndView` |",
    "| Execution | Before DispatcherServlet | After DispatcherServlet, before Controller |",
    "",
    "### 25. How do you configure view resolvers in Spring MVC?",
    "",
    "Define `ViewResolver` beans in `@Configuration` class. Chain multiple resolvers with `setOrder()` (lower = higher priority). Common: `InternalResourceViewResolver` (JSP), `ThymeleafViewResolver`.",
    "",
    "### 26. What is InternalResourceViewResolver?",
    "",
    "Resolves view names to JSP by prepending a **prefix** and appending a **suffix**: `\"home\"` → `/WEB-INF/views/home.jsp`. Placing JSPs under `/WEB-INF/` prevents direct browser access.",
    "",
    "### 27. How do you handle exceptions in Spring MVC?",
    "",
    "Three mechanisms: (1) `@ExceptionHandler` in a controller (local), (2) `@ControllerAdvice` + `@ExceptionHandler` (global), (3) `HandlerExceptionResolver` (low-level). Use `@RestControllerAdvice` for REST APIs.",
    "",
    "### 28. What is @ExceptionHandler annotation?",
    "",
    "Marks a method that handles specific exception types. Can return a view name, `ResponseEntity`, or response body. In a controller, handles that controller's exceptions only; in `@ControllerAdvice`, handles all.",
    "",
    "### 29. What is @ControllerAdvice annotation?",
    "",
    "Declares global exception handlers, model attributes, and data binding initialization for all controllers. `@RestControllerAdvice` = `@ControllerAdvice` + `@ResponseBody`.",
    "",
    "### 30. What is the difference between @Controller and @RestController?",
    "",
    "| Aspect | `@Controller` | `@RestController` |",
    "|---|---|---|",
    "| Returns | View name (HTML) | Response body (JSON) |",
    "| `@ResponseBody` | Per method | Implicit |",
    "| Use case | Server-rendered pages | REST APIs |",
    "",
    "### 31. How do you manage session attributes in Spring MVC?",
    "",
    "Use `HttpSession` directly or `@SessionAttributes` (preferred for multi-step forms). `@SessionAttributes(\"order\")` keeps the attribute in the session across requests. Call `SessionStatus.setComplete()` to clean up.",
    "",
    "### 32. What is the use of @SessionAttributes annotation?",
    "",
    "Declares which model attributes persist in the HTTP session between requests. Essential for multi-step workflows (wizards). Only applies to the controller it's declared on.",
    "",
    "### 33. What is the use of @RequestScope and @SessionScope annotations?",
    "",
    "`@RequestScope` — new bean per HTTP request. `@SessionScope` — new bean per HTTP session. When injected into singletons, Spring uses scoped proxies to ensure the correct scoped instance is used.",
    "",
    "### 34. How do you internationalize a Spring MVC application?",
    "",
    "Three components: **MessageSource** (loads `messages_XX.properties`), **LocaleResolver** (determines locale), **LocaleChangeInterceptor** (switches locale via `?lang=fr`). In views, use `<spring:message code=\"key\"/>` or Thymeleaf's `#{key}`.",
    "",
    "### 35. What is LocaleResolver in Spring MVC?",
    "",
    "Determines the user's locale. Implementations: `SessionLocaleResolver` (most common), `CookieLocaleResolver` (persists across sessions), `AcceptHeaderLocaleResolver` (browser setting, read-only), `FixedLocaleResolver`.",
    "",
    "### 36. What is LocaleChangeInterceptor in Spring MVC?",
    "",
    "Intercepts requests and switches locale based on a request parameter (e.g., `?lang=fr`). Updates the configured `LocaleResolver`. Register via `WebMvcConfigurer.addInterceptors()`."
  ].join("\n")
};

var DESIGN_PATTERN_QUESTION_SECTIONS = {
  "Creational": "# Top 50 Interview Questions on Creational Design Patterns\n\n## Overview / Index\n\n- [Singleton Pattern](#singleton-pattern) (Q1–Q10)\n- [Factory Method Pattern](#factory-method-pattern) (Q11–Q20)\n- [Abstract Factory Pattern](#abstract-factory-pattern) (Q21–Q30)\n- [Builder Pattern](#builder-pattern) (Q31–Q40)\n- [Prototype Pattern](#prototype-pattern) (Q41–Q50)\n\n## Singleton Pattern\n\n1. **What is the Singleton pattern and what problem does it solve?**\n   - Ensures a class has only one instance\n   - Provides a global access point to that instance\n   - Useful when exactly one shared object is needed (e.g., logger, config, connection pool)\n   - Controls instance creation (prevents accidental multiple instances)\n   - Reasoning / scenario (detailed):\n     - Scenario: you want exactly one `Logger` used across the app so logs have consistent formatting/level and share the same output destination.\n     - If every class does `new Logger()`, you can end up with multiple loggers writing to different files/levels.\n     - Singleton centralizes creation (`getInstance()`) and enforces “one shared object” semantics.\n\n2. **What are common Singleton implementation approaches?**\n   - Eager initialization (instance created at class load)\n   - Lazy initialization (create on first use)\n   - Synchronized accessor (simple thread safety)\n   - Double-checked locking (reduce synchronization overhead)\n   - Static inner helper (Bill Pugh approach)\n   - Reasoning / scenario (detailed):\n     - Eager is simplest but may create the instance even if never used.\n     - Lazy saves resources but introduces concurrency concerns (two threads can create two instances unless guarded).\n     - Bill Pugh (static inner class) is popular in Java because it is lazy and thread-safe due to class loading semantics.\n\n3. **Why is thread safety a concern for lazy Singletons?**\n   - Two threads can see `instance == null` and both create an instance\n   - Race conditions can break “only one instance”\n   - Needs synchronization or safe publication\n   - Double-checked locking requires `volatile` in Java\n   - Reasoning / scenario (detailed):\n     - Scenario: a web server receives two requests at startup; both call `DbConnectionPool.getInstance()` simultaneously.\n     - Without synchronization, both threads can create separate pools, defeating pooling and causing resource spikes.\n     - Thread-safe patterns ensure one instance is created and safely visible to all threads.\n\n4. **What is double-checked locking and why does it use `volatile`?**\n   - Checks `instance` outside synchronized block, then checks again inside\n   - Reduces synchronization cost after instance is initialized\n   - In Java, `volatile` prevents instruction reordering and ensures visibility\n   - Correct only when implemented carefully\n   - Reasoning / scenario (detailed):\n     - Without `volatile`, one thread might observe a partially constructed object due to reordering (reference published before constructor finishes).\n     - `volatile` ensures that once `instance` is assigned, other threads see a fully initialized object.\n\n5. **What are the drawbacks of Singleton?**\n   - Acts like global state (can hide dependencies)\n   - Harder to test (difficult to substitute/mocking)\n   - Can introduce tight coupling across modules\n   - Can become a bottleneck under contention (if synchronized poorly)\n   - Reasoning / scenario (detailed):\n     - Scenario: a `ConfigManager` singleton is read everywhere.\n     - Later, tests need to run with different configs in parallel, but global singleton state leaks between tests.\n     - Code that silently reads a singleton is harder to reason about than code that receives dependencies explicitly.\n\n6. **How can Singleton complicate unit testing, and how do teams mitigate it?**\n   - Hidden dependency makes tests brittle\n   - Global instance persists between tests\n   - Hard to inject fake implementations\n   - Mitigations: dependency injection, wrapper interfaces, controlled reset hooks (with caution)\n   - Reasoning / scenario (detailed):\n     - Scenario: `PaymentService` calls `Logger.getInstance()` internally; tests can’t easily verify behavior without touching global logger.\n     - Better approach: depend on an interface (`Log`) and inject a real logger in production, fake logger in tests.\n     - If you keep Singleton, provide controlled seams (e.g., allow overriding instance in test-only code) carefully to avoid production misuse.\n\n7. **How is Singleton different from a static utility class?**\n   - Singleton is an instance with identity and lifecycle; can implement interfaces\n   - Static class has no instance; harder to mock/replace\n   - Singleton can hold state and be swapped via DI (in some designs)\n   - Static is simplest for pure functions\n   - Reasoning / scenario (detailed):\n     - Scenario: you want different logging strategies (console vs file) depending on environment.\n     - Singleton can be an interface implementation chosen at startup; static methods lock you into one implementation and encourage global coupling.\n\n8. **When should you avoid Singleton?**\n   - When it becomes hidden global state across the codebase\n   - When you need multiple instances in some contexts (multi-tenant, sharding, tests)\n   - When lifecycle management matters (start/stop/reload)\n   - When DI provides a cleaner “single instance per scope”\n   - Reasoning / scenario (detailed):\n     - Scenario: multi-tenant app needs one `DbConnectionPool` per tenant.\n     - A global singleton cannot represent “one per tenant” cleanly.\n     - Prefer scoped lifetimes managed by a container rather than a global singleton.\n\n9. **How do you protect a Singleton from reflection/serialization attacks (Java)?**\n   - Use `enum` Singleton (most robust)\n   - In serialization, implement `readResolve()` to preserve single instance\n   - Guard constructor (throw if instance already exists)\n   - Restrict reflective access where possible\n   - Reasoning / scenario (detailed):\n     - Reflection can call private constructors; serialization can create a new object on deserialization.\n     - `enum` Singletons are protected by JVM guarantees.\n     - `readResolve()` ensures deserialization returns the existing instance rather than a new one.\n\n10. **Give real-world examples of Singleton usage.**\n    - Logger\n    - Configuration manager (read-mostly)\n    - Connection pool (shared resource manager)\n    - Cache manager (with careful eviction policies)\n    - Reasoning / scenario (detailed):\n      - These are examples where “one shared instance” is often desired to coordinate resources and provide a consistent view.\n      - Many modern systems model these as DI-managed singletons rather than manual `getInstance()` calls.\n\n## Factory Method Pattern\n\n11. **What is the Factory Method pattern and what problem does it solve?**\n    - Defines an interface for creating an object, but lets subclasses decide which class to instantiate\n    - Moves object creation logic out of client code\n    - Helps when the exact type isn’t known until runtime\n    - Promotes Open/Closed by adding new products via new subclasses\n    - Reasoning / scenario (detailed):\n      - Scenario (from your README): `DocumentCreator.openDocument()` defines a workflow, but the concrete document type (PDF vs Word) varies.\n      - Base class calls `createDocument()` (factory method). Subclasses override it to return `PDFDocument` or `WordDocument`.\n      - Client code depends on `DocumentCreator` and doesn’t need `new PDFDocument()` scattered everywhere.\n\n12. **What are the key participants in Factory Method?**\n    - Product interface (e.g., `Document`)\n    - Concrete products (e.g., `PDFDocument`, `WordDocument`)\n    - Creator base class (declares factory method)\n    - Concrete creators (override factory method)\n    - Reasoning / scenario (detailed):\n      - Creator provides a stable API/workflow; products vary.\n      - Concrete creator chooses which product class to instantiate, keeping the decision localized.\n\n13. **How does Factory Method differ from Simple Factory?**\n    - Simple factory: one class with a `create(type)` switch/if\n    - Factory Method: creation decision is in subclasses via overriding\n    - Factory Method uses inheritance; Simple factory uses conditionals\n    - Factory Method scales better with OCP in many OO designs\n    - Reasoning / scenario (detailed):\n      - A simple factory often grows a big `switch (type)` that must be edited for each new product.\n      - Factory Method adds a new subclass to introduce a new product, avoiding edits to the existing creator.\n\n14. **How does Factory Method support the Open/Closed Principle?**\n    - Open for extension: new product types via new concrete creator\n    - Closed for modification: creator workflow stays unchanged\n    - Clients depend on creator/product abstractions\n    - Avoids changing client code when new products appear\n    - Reasoning / scenario (detailed):\n      - Scenario: add `ExcelDocument` later.\n      - You implement `ExcelCreator extends DocumentCreator` overriding `createDocument()`.\n      - No change is needed in existing creators’ workflow logic (`openDocument()`).\n\n15. **When should you use Factory Method?**\n    - When you don’t know exact product type in advance\n    - When creation varies by subclass/environment\n    - When you want to reuse a creation-aware workflow\n    - When you want to isolate `new` calls from clients\n    - Reasoning / scenario (detailed):\n      - Scenario: different deployments choose different storage: local filesystem vs S3.\n      - A base service defines behavior, while subclasses decide which concrete client to create.\n\n16. **What are disadvantages of Factory Method?**\n    - More classes (creator subclasses) for each product variation\n    - Can be overkill for small systems\n    - Harder to see all product types in one place (scattered subclasses)\n    - Inheritance coupling between creators\n    - Reasoning / scenario (detailed):\n      - If you only have 2 product types and it will never grow, a small conditional may be simpler.\n      - As it grows, Factory Method pays off by localizing changes and preventing the “giant switch” problem.\n\n17. **How do you parameterize Factory Method selection at runtime?**\n    - Choose a creator based on configuration/environment\n    - Use a creator registry/map (key → creator)\n    - Use DI container to pick implementation\n    - Combine with Abstract Factory for families\n    - Reasoning / scenario (detailed):\n      - You usually select the concrete creator once at startup (based on config) and reuse it.\n      - The runtime flexibility often comes from choosing which creator object to use, not from switching inside the factory method itself.\n\n18. **How does Factory Method relate to Template Method?**\n    - Factory Method is often a “hook” in a template workflow\n    - Template Method defines algorithm skeleton; factory method creates needed products\n    - Both rely on inheritance and overriding\n    - Reasoning / scenario (detailed):\n      - Scenario: `openDocument()` is a template method (fixed steps), `createDocument()` is the factory method step.\n      - This keeps the overall workflow stable while letting object creation vary.\n\n19. **Can Factory Method be combined with caching/pooling?**\n    - Yes, creator can reuse instances instead of always creating new ones\n    - Useful for expensive-to-create products\n    - Must consider lifecycle and thread safety\n    - Reasoning / scenario (detailed):\n      - Scenario: creating a parser is expensive; creator may return a cached parser instance.\n      - At that point, your “factory” acts more like a provider/pool, so you must document ownership and concurrency rules.\n\n20. **Give real-world examples of Factory Method usage.**\n    - Java `Calendar.getInstance()` / `NumberFormat.getInstance()` style APIs (factory methods)\n    - Frameworks that choose platform-specific implementations\n    - Document creators (PDF/Word) as in your README\n    - Reasoning / scenario (detailed):\n      - These APIs hide concrete classes behind stable methods, allowing the library/framework to decide which implementation to return.\n\n## Abstract Factory Pattern\n\n21. **What is the Abstract Factory pattern and what problem does it solve?**\n    - Provides an interface for creating families of related/dependent objects\n    - Ensures created objects are compatible with each other\n    - Lets you switch entire product families without changing client code\n    - Useful for platform/theme-specific UI components\n    - Reasoning / scenario (detailed):\n      - Scenario (from your README): Windows vs Mac UI families (`Button`, `TextField`, `Checkbox`).\n      - If you mix `WindowsButton` with `MacCheckbox`, UI looks inconsistent or breaks assumptions.\n      - Abstract Factory gives one factory per family (`WindowsUIFactory`, `MacUIFactory`) so clients always get a consistent set.\n\n22. **What are the participants in Abstract Factory?**\n    - Abstract factory interface (declares creation methods)\n    - Concrete factories (one per family)\n    - Abstract products (interfaces)\n    - Concrete products (per family)\n    - Client (depends on abstractions only)\n    - Reasoning / scenario (detailed):\n      - Client code calls `factory.createButton()` and doesn’t care whether it’s Windows or Mac.\n      - Concrete factories encode the compatibility rule: they return matching product variants.\n\n23. **How is Abstract Factory different from Factory Method?**\n    - Factory Method creates one product type via inheritance\n    - Abstract Factory creates a family of related products via composition\n    - Factory Method: “which concrete product?”; Abstract Factory: “which product family?”\n    - Abstract Factory often uses multiple factory methods\n    - Reasoning / scenario (detailed):\n      - If you only need to create `Document` (one product type), Factory Method fits.\n      - If you must create `Button + TextField + Checkbox` in matching styles, Abstract Factory fits.\n\n24. **How does Abstract Factory support the Open/Closed Principle?**\n    - Add a new family by adding a new concrete factory\n    - Existing client code stays unchanged (depends on interfaces)\n    - Existing families stay unchanged\n    - Reasoning / scenario (detailed):\n      - Add `LinuxUIFactory` later; client still calls the same factory interface.\n      - Only wiring/configuration changes to pick the new factory.\n\n25. **What is the main disadvantage of Abstract Factory?**\n    - Adding a new product type is hard (requires updating factory interface and all factories)\n    - Can lead to many classes (families × product types)\n    - More upfront design than simple `new`\n    - Reasoning / scenario (detailed):\n      - If you add a new product type `Slider`, you must:\n        - add `createSlider()` to the abstract factory,\n        - implement it in every concrete factory (`WindowsUIFactory`, `MacUIFactory`, ...).\n      - This is the main trade-off: easy to add families, harder to add new product types.\n\n26. **When should you use Abstract Factory?**\n    - When you need to ensure products work together as a set\n    - When your system must support multiple “themes/platforms”\n    - When swapping families should not affect client code\n    - When you want to isolate platform-specific class names\n    - Reasoning / scenario (detailed):\n      - UI toolkit example is the classic: choose `WindowsUIFactory` vs `MacUIFactory` once, then build UI consistently.\n      - Another scenario: database family (connections, commands, transactions) for different DB vendors.\n\n27. **How do you select the concrete factory at runtime?**\n    - Based on configuration (OS, theme, environment)\n    - Based on dependency injection wiring\n    - Factory-of-factories (registry) mapping key → factory\n    - Reasoning / scenario (detailed):\n      - You typically decide the family once at startup (e.g., detect OS) and keep one factory instance.\n      - This avoids mixing families accidentally.\n\n28. **How does Abstract Factory relate to Dependency Injection (DI)?**\n    - DI can provide the chosen concrete factory\n    - Client depends on the abstract factory interface\n    - DI manages lifecycle/scope (singleton per app, per request, etc.)\n    - Reasoning / scenario (detailed):\n      - In Spring, you can bind `UIFactory` to `WindowsUIFactory` in one environment and `MacUIFactory` in another.\n      - Client code stays identical.\n\n29. **Can Abstract Factory be implemented using Factory Methods?**\n    - Yes, each creation method in the abstract factory is effectively a factory method\n    - Concrete factories override/implement those methods\n    - Reasoning / scenario (detailed):\n      - `createButton()`, `createTextField()`, etc. are factory methods.\n      - Abstract Factory is essentially “multiple coordinated factory methods grouped as one family provider.”\n\n30. **Give real-world examples of Abstract Factory.**\n    - Cross-platform UI component libraries\n    - Database drivers creating vendor-specific objects\n    - Cloud SDKs producing environment-specific clients (mock vs real)\n    - Reasoning / scenario (detailed):\n      - Anywhere you must create a compatible set of objects (not just one object) and swap that set as a unit.\n\n## Builder Pattern\n\n31. **What is the Builder pattern and what problem does it solve?**\n    - Separates construction of a complex object from its representation\n    - Useful when an object has many optional parameters\n    - Avoids telescoping constructors (many overloaded constructors)\n    - Produces immutable objects more easily\n    - Reasoning / scenario (detailed):\n      - Scenario: creating a `House` or `HttpRequest` with many optional fields.\n      - Without Builder, you get long parameter constructors or many overloads.\n      - Builder lets you set only what you need with readable calls, then `build()` once.\n\n32. **What are the participants in the Builder pattern?**\n    - Product (the complex object)\n    - Builder (interface/abstract builder)\n    - ConcreteBuilder (builds and assembles parts)\n    - Director (optional: orchestrates building steps)\n    - Reasoning / scenario (detailed):\n      - Director is useful when you have standard “recipes” (e.g., standard vs luxury) that reuse the same building steps.\n      - Many modern uses omit Director and just use a fluent builder API.\n\n33. **Builder vs Factory: when do you choose Builder?**\n    - Builder is best when there are many optional parts/steps\n    - Factory is best when creation is simple but the concrete type varies\n    - Builder focuses on step-by-step assembly\n    - Reasoning / scenario (detailed):\n      - If you need `new Car(engine, wheels, color, gps, ...)` with many combinations, Builder improves readability.\n      - If you need one of a few variants (`PDFDocument` vs `WordDocument`), Factory Method may be simpler.\n\n34. **How does Builder improve readability and correctness?**\n    - Named methods replace positional arguments\n    - Allows validation in `build()`\n    - Enables defaults for omitted fields\n    - Can enforce required fields\n    - Reasoning / scenario (detailed):\n      - Scenario: `HttpRequestBuilder.method(\"POST\").url(\"/pay\").header(\"Auth\", token).build()`\n      - This is clearer than a long constructor call where you might swap parameter order.\n      - `build()` can validate invariants (e.g., url must not be null).\n\n35. **What is the telescoping constructor problem?**\n    - Many constructor overloads to cover optional parameters\n    - Difficult to maintain and error-prone\n    - Call sites become confusing\n    - Builder is a common fix\n    - Reasoning / scenario (detailed):\n      - Scenario: `Pizza(size, cheese, olives, mushrooms, extraSauce, ...)`\n      - You either create many constructors or pass lots of `null/false` placeholders.\n      - Builder makes optional configuration explicit and avoids combinatorial constructor explosion.\n\n36. **How do you enforce immutability with Builder?**\n    - Builder is mutable during construction\n    - Product is immutable after `build()`\n    - Set fields in constructor and expose only getters\n    - Reasoning / scenario (detailed):\n      - Immutable objects are safer in concurrency and easier to reason about.\n      - Builder collects values, then constructs an immutable product once.\n\n37. **What is the difference between Builder and Prototype?**\n    - Builder constructs from scratch via steps\n    - Prototype creates by cloning an existing object\n    - Builder is good for many optional parts\n    - Prototype is good when copying is cheaper than rebuilding\n    - Reasoning / scenario (detailed):\n      - If you frequently create similar objects, Prototype can clone and tweak.\n      - If you assemble complex objects with rules/validations, Builder provides a clearer construction process.\n\n38. **What are common pitfalls of Builder?**\n    - Too much boilerplate for simple objects\n    - Defaults/validation can become inconsistent if unclear\n    - Thread-safety issues if builders are reused across threads\n    - Reasoning / scenario (detailed):\n      - Builder should typically be used per-build; reusing one builder across threads can interleave state.\n      - Keep validation and defaults documented so different call orders don’t create surprising results.\n\n39. **How do you design validation in `build()`?**\n    - Validate required fields\n    - Validate cross-field constraints\n    - Throw exceptions or return a result type\n    - Reasoning / scenario (detailed):\n      - Scenario: `HttpRequest` requires `method` and `url`; body allowed only for certain methods.\n      - `build()` is the single checkpoint to enforce invariants regardless of call order.\n\n40. **Give real-world examples of Builder usage.**\n    - StringBuilder / StringBuffer (stepwise construction)\n    - HTTP request builders in client libraries\n    - Complex configuration objects\n    - Reasoning / scenario (detailed):\n      - Builders are common wherever objects have many optional settings and you want fluent, readable creation.\n\n## Prototype Pattern\n\n41. **What is the Prototype pattern and what problem does it solve?**\n    - Creates new objects by cloning an existing instance (the prototype)\n    - Avoids expensive initialization by copying preconfigured objects\n    - Useful when object creation is costly or complex\n    - Supports dynamic object creation without knowing concrete classes\n    - Reasoning / scenario (detailed):\n      - Scenario: a game needs many similar `Enemy` objects with complex setup (textures, AI params).\n      - Building each from scratch is expensive.\n      - Create one configured prototype and clone it to get new enemies quickly.\n\n42. **What is the difference between shallow copy and deep copy?**\n    - Shallow copy duplicates the object but shares referenced sub-objects\n    - Deep copy duplicates the object and also clones referenced sub-objects\n    - Shallow is faster but can cause shared-mutable-state bugs\n    - Deep is safer but more expensive\n    - Reasoning / scenario (detailed):\n      - Scenario: `Document` has a `List<Page>`.\n      - Shallow copy shares the same pages list → editing one document edits the other.\n      - Deep copy clones the list/pages so each document can change independently.\n\n43. **How do you implement Prototype in Java?**\n    - Implement `clone()` carefully (or provide a copy constructor)\n    - Consider `Cloneable` pitfalls; many teams prefer explicit copy methods\n    - Ensure deep copy where needed\n    - Reasoning / scenario (detailed):\n      - Java’s `Cloneable` is tricky because default cloning is shallow and the API is awkward.\n      - A safer approach is an explicit `copy()` method or copy constructor that makes deep/shallow decisions explicit.\n\n44. **When is Prototype better than Builder?**\n    - When many objects are variations of a common base configuration\n    - When cloning is cheaper than step-by-step construction\n    - When you need to create objects dynamically at runtime\n    - Reasoning / scenario (detailed):\n      - Scenario: UI components with default styling.\n      - You keep a prototype “base button” and clone it for each instance, then tweak label/size.\n      - Builder is better when construction rules are complex and you need clear step ordering.\n\n45. **What are common use cases for Prototype?**\n    - Copying complex objects efficiently\n    - Avoiding repeated initialization (expensive setup)\n    - Creating objects at runtime from templates\n    - Reducing subclass explosion (configure prototypes instead of subclassing)\n    - Reasoning / scenario (detailed):\n      - Instead of creating `RedButton`, `BlueButton`, `GreenButton` subclasses, you store prototypes with different styles and clone them.\n\n46. **What are the main risks of Prototype?**\n    - Incorrect copy semantics (shared mutable state)\n    - Deep copy complexity (graphs, cycles)\n    - Cloning can bypass invariants if not designed carefully\n    - Reasoning / scenario (detailed):\n      - Scenario: object graph has cycles (A references B, B references A).\n      - Naive deep clone can infinite-loop; you may need a map of already-cloned objects.\n\n47. **How do you handle prototypes with mutable vs immutable fields?**\n    - Immutable fields can be shared safely\n    - Mutable fields usually must be deep-copied\n    - Prefer immutability to simplify cloning\n    - Reasoning / scenario (detailed):\n      - Strings and value objects can be reused safely.\n      - Lists, maps, and other mutable containers typically need copying to avoid cross-instance interference.\n\n48. **How does Prototype relate to registries (prototype manager)?**\n    - Store a set of prototypes in a registry keyed by name/type\n    - Clients request a prototype and clone it\n    - Enables runtime selection of “templates”\n    - Reasoning / scenario (detailed):\n      - Scenario: `ShapeRegistry` holds prototypes for `Circle`, `Square`, `Triangle`.\n      - Client asks `registry.create(\"circle\")` → registry clones the circle prototype.\n      - This decouples clients from concrete classes and from `new`.\n\n49. **Prototype vs Abstract Factory: how are they different?**\n    - Abstract Factory creates families through factory methods\n    - Prototype creates by copying existing instances\n    - Prototype can reduce class explosion by configuration\n    - Abstract Factory enforces family compatibility\n    - Reasoning / scenario (detailed):\n      - If you need compatibility across a family (Windows UI set), Abstract Factory is clearer.\n      - If you mostly need “templates” with variations, a Prototype registry is often simpler.\n\n50. **Give real-world examples of Prototype usage.**\n    - Copy/paste in editors (duplicate objects)\n    - Game entity templates (clone configured entities)\n    - UI component templates\n    - Reasoning / scenario (detailed):\n      - Wherever “duplicate and tweak” is a natural workflow, Prototype is a strong fit.\n\n",
  "Structural": "# Top 70 Interview Questions on Structural Design Patterns\n\n## Overview / Index\n\n- [Adapter Pattern](#adapter-pattern) (Q1–Q10)\n- [Bridge Pattern](#bridge-pattern) (Q11–Q20)\n- [Composite Pattern](#composite-pattern) (Q21–Q30)\n- [Decorator Pattern](#decorator-pattern) (Q31–Q40)\n- [Facade Pattern](#facade-pattern) (Q41–Q50)\n- [Flyweight Pattern](#flyweight-pattern) (Q51–Q60)\n- [Proxy Pattern](#proxy-pattern) (Q61–Q70)\n\n## Adapter Pattern\n\n1. **What is the Adapter pattern and what problem does it solve?**\n   - Converts the interface of a class into another interface clients expect\n   - Lets incompatible classes collaborate without modifying their source\n   - Useful for legacy systems and third-party integrations\n   - Focus is interface compatibility, not new behavior\n   - Reasoning / scenario (detailed):\n     - Scenario (like your README): new code expects `ModernPaymentProcessor.processPayment(double)` but a legacy library exposes `LegacyPaymentSystem.makePayment(float, String)`.\n     - Instead of rewriting the legacy library or changing all client code, you wrap the legacy object in an adapter that translates the call and data types.\n\n2. **What are the key participants in Adapter (Target, Adaptee, Adapter, Client)?**\n   - Target: interface the client expects\n   - Adaptee: existing class with incompatible interface\n   - Adapter: wrapper that implements Target and delegates to Adaptee\n   - Client: uses the Target interface\n   - Reasoning / scenario (detailed):\n     - Target keeps the rest of the system stable.\n     - Adapter is the only place where “translation glue” lives (parameter conversion, method mapping), preventing such logic from leaking across the codebase.\n\n3. **Class Adapter vs Object Adapter: what is the difference?**\n   - Class adapter uses inheritance (extends Adaptee, implements Target)\n   - Object adapter uses composition (holds Adaptee instance)\n   - Object adapter is more flexible (can adapt subclasses, swap adaptee)\n   - Class adapter limited by single inheritance (in Java)\n   - Reasoning / scenario (detailed):\n     - In Java, object adapters are common because you can wrap any existing instance (including different concrete adaptees) and choose at runtime.\n     - Class adapters can be simpler but are constrained by inheritance and are harder to combine with other base classes.\n\n4. **When should you use Adapter vs rewriting/modernizing the component?**\n   - Use Adapter when you cannot modify the adaptee (third-party/legacy)\n   - Use Adapter for incremental migration (old + new coexist)\n   - Rewrite when the legacy API is unstable, unsafe, or a long-term liability\n   - Consider cost/risk and time-to-deliver\n   - Reasoning / scenario (detailed):\n     - Adapter is a “bridge” for integration, not a permanent excuse to keep a broken API forever.\n     - It’s ideal during phased migrations: new code speaks Target; adapter translates to old code until old code is replaced.\n\n5. **How does Adapter support Open/Closed Principle?**\n   - Client code remains closed for modification (still uses Target)\n   - New adapters can be added for new incompatible components\n   - Existing adaptees remain unchanged\n   - Reasoning / scenario (detailed):\n     - You can add `StripeAdapter`, `PayPalAdapter`, `LegacyPaymentAdapter` as new integrations without rewriting checkout logic, because checkout depends on `PaymentProcessor` (Target).\n\n6. **What are common pitfalls of Adapter?**\n   - Over-adapting can hide design issues\n   - “Leaky” adapter that exposes adaptee types defeats the purpose\n   - Hard-to-debug translation logic if not well-scoped\n   - Adapter chains can become messy\n   - Reasoning / scenario (detailed):\n     - If client code starts depending on `LegacyPaymentSystem` again (type casts, special cases), you lose interface isolation.\n     - Keep adapters small and ensure the rest of the system only sees Target types.\n\n7. **Adapter vs Facade: how are they different?**\n   - Adapter: makes one interface look like another (compatibility)\n   - Facade: provides a simplified interface over a subsystem (simplicity)\n   - Adapter usually wraps a single class; Facade often wraps many\n   - Reasoning / scenario (detailed):\n     - Adapter solves “interfaces don’t match”.\n     - Facade solves “API is too complex to use directly”.\n\n8. **Adapter vs Bridge: how are they different?**\n   - Adapter: retrofit compatibility for existing code\n   - Bridge: design-time separation so abstraction and implementation can vary independently\n   - Adapter is often used after-the-fact; Bridge is typically planned\n   - Reasoning / scenario (detailed):\n     - Adapter is a patch for mismatch.\n     - Bridge is an architecture choice to avoid a combinatorial explosion of subclasses (e.g., shapes × platforms).\n\n9. **How do you test an Adapter?**\n   - Unit-test translation logic (method mapping, conversions)\n   - Use fake adaptee to assert delegation\n   - Verify edge cases (nulls, ranges, error mapping)\n   - Reasoning / scenario (detailed):\n     - If the adapter converts currency, types, or exception types, tests should pin that mapping.\n     - Tests protect against subtle integration bugs (e.g., float rounding, wrong currency).\n\n10. **Give real-world examples of Adapter usage.**\n    - Legacy system integration\n    - Third-party library integration with mismatched interfaces\n    - Hardware/driver adapters (USB dongles)\n    - API version migration (v1 → v2 adapter)\n    - Reasoning / scenario (detailed):\n      - The key indicator is: “I need two things to work together but their interfaces don’t match.”\n\n## Bridge Pattern\n\n11. **What is the Bridge pattern and what problem does it solve?**\n    - Decouples an abstraction from its implementation so both can vary independently\n    - Splits a class hierarchy into two dimensions (abstraction + implementor)\n    - Avoids subclass explosion when you have many combinations\n    - Reasoning / scenario (detailed):\n      - Scenario (like your README): `Shape` is an abstraction and `DrawingAPI` is an implementor (Windows vs Mac).\n      - Without Bridge, you might create `WindowsCircle`, `MacCircle`, `WindowsSquare`, `MacSquare`… (combinatorial growth).\n      - With Bridge, `Circle` works with any `DrawingAPI` implementation.\n\n12. **What are the key participants in Bridge?**\n    - Abstraction (high-level interface)\n    - Refined Abstraction (specialized abstractions)\n    - Implementor interface (low-level operations)\n    - Concrete Implementors (platform/device specifics)\n    - Reasoning / scenario (detailed):\n      - The abstraction holds a reference to an implementor and delegates low-level work.\n      - This lets you extend abstractions and implementations independently.\n\n13. **Bridge vs Strategy: how are they different?**\n    - Bridge: structural separation of two hierarchies (abstraction vs implementation)\n    - Strategy: interchangeable algorithms plugged into one context\n    - Bridge is about architecture of types; Strategy is about behavior choice\n    - Reasoning / scenario (detailed):\n      - A Bridge implementor is usually a long-lived “platform driver” dependency.\n      - Strategies are often swapped more freely to change an algorithm.\n\n14. **When should you use Bridge?**\n    - When you have two (or more) independent axes of variation\n    - When implementations may change at runtime (platform/device)\n    - When you want to avoid permanent binding between abstraction and implementation\n    - Reasoning / scenario (detailed):\n      - Axis 1: shapes (circle, square). Axis 2: platform (Windows, Mac).\n      - Bridge prevents `N × M` subclasses by composing axes instead of inheriting all combinations.\n\n15. **How does Bridge improve extensibility?**\n    - Add new abstractions without touching implementors\n    - Add new implementors without touching abstractions\n    - Keeps changes localized to one hierarchy\n    - Reasoning / scenario (detailed):\n      - Add a new `LinuxDrawingAPI` without changing `Circle`.\n      - Add a new `Triangle` without changing Windows/Mac implementors.\n\n16. **What are the trade-offs of Bridge?**\n    - More classes/indirection than a single hierarchy\n    - Requires careful API boundary design between abstraction and implementor\n    - Slight runtime overhead from delegation\n    - Reasoning / scenario (detailed):\n      - Bridge pays off when combinations grow; if there are only 2–3 combinations and no growth expected, it can feel like over-engineering.\n\n17. **How do you choose what belongs in Abstraction vs Implementor?**\n    - Abstraction: high-level operations and domain concepts\n    - Implementor: platform-specific or low-level primitives\n    - Avoid leaking platform details into abstraction\n    - Reasoning / scenario (detailed):\n      - `Shape.draw()` is high-level; `DrawingAPI.drawCircle(x,y,r)` is low-level.\n      - If the abstraction starts depending on platform-specific types, the separation weakens.\n\n18. **Can Bridge be combined with dependency injection (DI)?**\n    - Yes; DI can supply the implementor based on environment\n    - Enables selecting implementation at runtime cleanly\n    - Reasoning / scenario (detailed):\n      - In production on Windows, DI wires `WindowsDrawingAPI`; on Mac, wires `MacDrawingAPI`.\n      - Your `Shape` classes remain unchanged.\n\n19. **Bridge vs Adapter: what’s the key difference?**\n    - Bridge is designed up-front to separate dimensions\n    - Adapter retrofits compatibility for existing classes\n    - Reasoning / scenario (detailed):\n      - Bridge prevents the problem (subclass explosion).\n      - Adapter fixes interface mismatch after the fact.\n\n20. **Give real-world examples of Bridge usage.**\n    - Cross-platform UI frameworks\n    - Device control systems (remote control → devices)\n    - Database APIs (abstraction) vs drivers (implementations)\n    - Reasoning / scenario (detailed):\n      - The indicator is: “I need to extend two independent dimensions without multiplying subclasses.”\n\n## Composite Pattern\n\n21. **What is the Composite pattern and what problem does it solve?**\n    - Composes objects into tree structures to represent part-whole hierarchies\n    - Lets clients treat individual objects and compositions uniformly\n    - Common in recursive structures (file systems, UI trees)\n    - Reasoning / scenario (detailed):\n      - Scenario: a file system where `File` and `Folder` should both support `size()` and `delete()`.\n      - Composite lets client code call `node.size()` without caring whether the node is a file (leaf) or folder (composite).\n\n22. **What are the key participants in Composite?**\n    - Component interface (common operations)\n    - Leaf (no children)\n    - Composite (has children; implements child management)\n    - Client (uses component interface)\n    - Reasoning / scenario (detailed):\n      - The component interface enables uniform treatment.\n      - Composite contains children and applies operations recursively.\n\n23. **How does Composite enable uniform treatment of leaf and composite?**\n    - Both implement the same interface\n    - Client depends on the interface only\n    - Recursive behavior is hidden inside composite\n    - Reasoning / scenario (detailed):\n      - Client doesn’t need `if (node is folder) iterate children`.\n      - The folder itself handles iteration and aggregation (sum of sizes).\n\n24. **Transparent vs Safe Composite: what’s the difference?**\n    - Transparent: child management methods on Component (uniform API, but leaves may throw)\n    - Safe: child management only on Composite (type safety, but less uniform)\n    - Reasoning / scenario (detailed):\n      - Transparent makes the interface consistent but can allow invalid operations (adding child to a leaf).\n      - Safe avoids illegal operations but requires knowing when something is a composite.\n\n25. **What are common use cases for Composite?**\n    - File systems (folders/files)\n    - UI widget trees (containers/components)\n    - Organization charts (manager/employee)\n    - Expression trees (ASTs)\n    - Reasoning / scenario (detailed):\n      - The common theme is a recursive “contains” relationship and the need for uniform operations across the tree.\n\n26. **What are the trade-offs/pitfalls of Composite?**\n    - Can make it harder to restrict what can be contained (validation rules)\n    - Deep recursion may impact performance/stack\n    - Over-generalization if you force everything into a tree\n    - Reasoning / scenario (detailed):\n      - Scenario: only certain nodes may legally contain certain children (UI layout rules).\n      - You may need extra validation logic to prevent invalid trees.\n\n27. **How does Composite relate to Iterator and Visitor?**\n    - Composite provides the structure (tree)\n    - Iterator provides traversal over the structure\n    - Visitor provides operations over many node types\n    - Reasoning / scenario (detailed):\n      - You often combine them:\n        - Composite defines node relationships,\n        - Iterator/recursion walks the tree,\n        - Visitor applies operations (render, export, compute metrics).\n\n28. **How do you implement add/remove children in Composite?**\n    - Composite maintains a list of children\n    - Expose `add/remove/getChild` depending on transparent vs safe design\n    - Ensure parent-child integrity (no cycles)\n    - Reasoning / scenario (detailed):\n      - Avoid cycles (folder containing itself indirectly) or you’ll get infinite recursion in operations like `size()`.\n\n29. **How do you compute aggregated results in Composite?**\n    - Composite operation calls same operation on children and aggregates results\n    - Leaves return base values\n    - Reasoning / scenario (detailed):\n      - Example: folder size = sum of child sizes; leaf size = file size.\n      - This is the main power of Composite: recursion is encapsulated inside composites.\n\n30. **Give real-world examples of Composite usage.**\n    - DOM trees in browsers\n    - GUI component containers\n    - Menu structures (menu/menu item)\n    - Reasoning / scenario (detailed):\n      - The indicator is: “I have a tree where leaves and branches should be treated the same way.”\n\n## Decorator Pattern\n\n31. **What is the Decorator pattern and what problem does it solve?**\n    - Attaches additional responsibilities to an object dynamically\n    - Alternative to subclassing for extending behavior\n    - Supports combining behaviors at runtime\n    - Reasoning / scenario (detailed):\n      - Scenario: coffee ordering where base drink can be wrapped with add-ons (milk, caramel, whip) and you want any combination.\n      - Subclassing leads to explosion (`CoffeeWithMilkWithCaramel...`).\n      - Decorator wraps a base object and adds behavior incrementally.\n\n32. **What are the key participants in Decorator?**\n    - Component interface (common operations)\n    - ConcreteComponent (base object)\n    - Decorator base (wraps a component)\n    - ConcreteDecorators (add responsibilities)\n    - Reasoning / scenario (detailed):\n      - The decorator implements the same interface, so it can be used anywhere the component is expected.\n      - Wrapping preserves substitutability.\n\n33. **Decorator vs Inheritance: what’s the core benefit?**\n    - Avoids subclass explosion\n    - Supports runtime composition of features\n    - Keeps each feature in a small focused class\n    - Reasoning / scenario (detailed):\n      - Adding a new feature becomes adding a new decorator class, not adding new subclasses for every combination.\n\n34. **Decorator vs Proxy: how are they different?**\n    - Decorator adds behavior/responsibilities\n    - Proxy controls access to an object (lazy load, security, remote)\n    - Both wrap an object, but intent differs\n    - Reasoning / scenario (detailed):\n      - Decorator is “enhance”.\n      - Proxy is “control/stand-in”.\n\n35. **Can decorators be stacked? What are the implications?**\n    - Yes, multiple decorators can wrap the same component\n    - Order can matter (e.g., compression then encryption vs encryption then compression)\n    - Debugging may be harder due to deep wrapper chains\n    - Reasoning / scenario (detailed):\n      - Stacking is the main power: you can build pipelines.\n      - But you should be mindful of readability and provide tooling/logging if chains get complex.\n\n36. **How does Decorator maintain the Liskov Substitution Principle?**\n    - Decorator implements the same interface as the component\n    - Client can treat decorated and undecorated objects the same\n    - Should not break expected contract (pre/post conditions)\n    - Reasoning / scenario (detailed):\n      - If a decorator changes semantics unexpectedly (e.g., `read()` now returns different data), clients can break.\n      - Good decorators add behavior while preserving the component’s contract.\n\n37. **What are disadvantages of Decorator?**\n    - Many small objects (more allocations)\n    - Harder debugging/stack traces\n    - Can complicate equality/type checks\n    - Reasoning / scenario (detailed):\n      - Wrapper chains can make it harder to know “what object do I really have?”.\n      - Prefer programming to interfaces and avoid type-based logic.\n\n38. **Decorator vs Composite: how are they different?**\n    - Decorator wraps one object to add behavior\n    - Composite aggregates many children to form a tree\n    - Both use composition, but structure differs\n    - Reasoning / scenario (detailed):\n      - Decorator is a linear chain; Composite is a branching tree.\n\n39. **How do you implement decorators in Java libraries?**\n    - Java I/O streams are classic decorators (`BufferedInputStream`, `GZIPInputStream`)\n    - Each wrapper implements the same stream interface and delegates\n    - Reasoning / scenario (detailed):\n      - You can wrap a `FileInputStream` with buffering, compression, encryption, etc. without changing the underlying stream implementation.\n\n40. **Give real-world examples of Decorator usage.**\n    - Java I/O streams\n    - Logging wrappers (add timing/metrics)\n    - Feature flags (wrap behavior conditionally)\n    - Reasoning / scenario (detailed):\n      - The indicator is: “I need to add features dynamically without creating an explosion of subclasses.”\n\n## Facade Pattern\n\n41. **What is the Facade pattern and what problem does it solve?**\n    - Provides a unified, simplified interface to a complex subsystem\n    - Reduces coupling between clients and subsystem internals\n    - Makes subsystems easier to use and evolve\n    - Reasoning / scenario (detailed):\n      - Scenario: starting a car involves ignition, fuel pump, sensors, engine control.\n      - A facade provides one method like `car.start()` hiding many calls and ordering constraints.\n\n42. **What are the key participants in Facade?**\n    - Facade class (simple API)\n    - Subsystem classes (complex internals)\n    - Client (uses facade)\n    - Reasoning / scenario (detailed):\n      - Client depends on facade, not on dozens of subsystem classes.\n      - Subsystem can change internally without forcing client changes.\n\n43. **Facade vs Adapter: how are they different?**\n    - Facade simplifies an interface\n    - Adapter changes an interface to match what client expects\n    - Reasoning / scenario (detailed):\n      - Facade: “too complex”; Adapter: “doesn’t match.”\n\n44. **Facade vs Mediator: how are they different?**\n    - Facade is usually one-way (client → subsystem)\n    - Mediator coordinates peer-to-peer interactions\n    - Reasoning / scenario (detailed):\n      - Facade is a front door to a subsystem.\n      - Mediator is a traffic controller among components.\n\n45. **How does Facade improve maintainability?**\n    - Reduces dependency surface area\n    - Centralizes usage patterns and correct call ordering\n    - Allows internal refactoring with fewer client changes\n    - Reasoning / scenario (detailed):\n      - If subsystem requires “call A then B then C”, the facade encodes that workflow once.\n      - Without facade, every client duplicates that sequence (bug-prone).\n\n46. **What are the risks/pitfalls of Facade?**\n    - Facade can become a “god API” if it grows without boundaries\n    - Can hide important capabilities if oversimplified\n    - Reasoning / scenario (detailed):\n      - You may need multiple facades for different client types (admin vs end-user) rather than one mega-facade.\n\n47. **Can a subsystem have multiple facades?**\n    - Yes, different facades can serve different use cases\n    - Helps keep each facade focused\n    - Reasoning / scenario (detailed):\n      - Scenario: payment subsystem has `CheckoutFacade` for normal flow and `ReconciliationFacade` for finance operations.\n\n48. **How does Facade relate to layering/architecture?**\n    - Facades often form boundaries between layers\n    - They help enforce dependency direction (upper layer depends on facade)\n    - Reasoning / scenario (detailed):\n      - A service layer can expose facades over repositories and lower-level components, keeping controllers thin.\n\n49. **Does Facade prevent clients from using subsystem classes directly?**\n    - Not necessarily (it’s not an enforcement mechanism)\n    - Use module boundaries/access modifiers to enforce if needed\n    - Reasoning / scenario (detailed):\n      - In many codebases, the facade is a recommended entrypoint, but strict enforcement is done via packaging/module rules.\n\n50. **Give real-world examples of Facade usage.**\n    - SDK clients wrapping many API calls\n    - Libraries offering “easy mode” APIs\n    - Framework bootstrapping APIs (one call to configure/start)\n    - Reasoning / scenario (detailed):\n      - The indicator is: “The subsystem is too complex; I need a simpler way to use it.”\n\n## Flyweight Pattern\n\n51. **What is the Flyweight pattern and what problem does it solve?**\n    - Uses sharing to support large numbers of fine-grained objects efficiently\n    - Splits state into intrinsic (shared) and extrinsic (per-use) state\n    - Reduces memory consumption\n    - Reasoning / scenario (detailed):\n      - Scenario: a text editor has millions of characters; most share the same font/style.\n      - Instead of storing font data per character, you share a `TextStyle` flyweight and store only position/character as extrinsic state.\n\n52. **What is intrinsic vs extrinsic state?**\n    - Intrinsic: invariant, shareable state stored in the flyweight (e.g., font name, size)\n    - Extrinsic: context-specific state passed in at use time (e.g., x/y position)\n    - Reasoning / scenario (detailed):\n      - Intrinsic state enables sharing; extrinsic state prevents sharing from breaking correctness.\n      - If you put per-instance state into flyweight, objects interfere with each other.\n\n53. **What are the key participants in Flyweight?**\n    - Flyweight interface\n    - ConcreteFlyweight (stores intrinsic state)\n    - FlyweightFactory (creates/caches flyweights)\n    - Client/context (provides extrinsic state)\n    - Reasoning / scenario (detailed):\n      - Factory is the heart of Flyweight: it returns existing shared instances when possible.\n      - Client must supply extrinsic state on each operation call.\n\n54. **When should you use Flyweight?**\n    - When you have very large numbers of similar objects\n    - When memory usage is a bottleneck\n    - When object identity is not critical\n    - When much of state can be made intrinsic (shareable)\n    - Reasoning / scenario (detailed):\n      - If you only have hundreds of objects, Flyweight complexity likely isn’t worth it.\n      - If you have millions, sharing can be the difference between running and crashing.\n\n55. **What are the trade-offs of Flyweight?**\n    - More complexity (separating state, factory/cache)\n    - Potential runtime overhead (lookups, passing extrinsic state)\n    - Harder debugging (objects are shared)\n    - Reasoning / scenario (detailed):\n      - You trade memory for CPU and design complexity.\n      - Bugs often happen when extrinsic state is accidentally stored inside shared objects.\n\n56. **How do you implement a Flyweight factory?**\n    - Use a map from intrinsic key → flyweight instance\n    - Return existing instance if present; otherwise create and cache\n    - Consider concurrency and cache eviction policies\n    - Reasoning / scenario (detailed):\n      - Key design matters: it must uniquely represent intrinsic state (e.g., `(fontName, size, weight)`).\n      - In multithreaded code, use concurrent maps or synchronize initialization.\n\n57. **How does Flyweight relate to caching and pooling?**\n    - Flyweight is a specialized cache of shared objects by intrinsic state\n    - Different from pooling which manages temporary object reuse\n    - Reasoning / scenario (detailed):\n      - Flyweights are typically immutable and shared widely.\n      - Pools often reuse mutable objects temporarily and return them, requiring careful lifecycle management.\n\n58. **What are common pitfalls in Flyweight implementations?**\n    - Accidentally storing extrinsic state in the shared flyweight\n    - Using flyweight when object identity matters\n    - Cache growing unbounded without eviction\n    - Reasoning / scenario (detailed):\n      - If the cache keys are too granular, you don’t get sharing.\n      - If keys are too coarse, you share objects that shouldn’t be shared.\n\n59. **How do you make Flyweights safe to share?**\n    - Keep flyweights immutable\n    - Ensure all mutable/context state is extrinsic\n    - Avoid exposing mutable internal structures\n    - Reasoning / scenario (detailed):\n      - Immutability is the easiest way to guarantee thread-safety and correctness for shared objects.\n\n60. **Give real-world examples of Flyweight usage.**\n    - Text rendering (shared glyphs/styles)\n    - Game engines (shared meshes/textures metadata)\n    - String interning (conceptually similar sharing)\n    - Reasoning / scenario (detailed):\n      - The indicator is: “I’m creating huge numbers of similar objects and running out of memory.”\n\n## Proxy Pattern\n\n61. **What is the Proxy pattern and what problem does it solve?**\n    - Provides a surrogate/placeholder for another object\n    - Controls access to the real object (security, caching, logging)\n    - Can defer expensive object creation (lazy loading)\n    - Can represent remote objects locally\n    - Reasoning / scenario (detailed):\n      - Scenario (classic): a `CreditCard` acts as a proxy for a bank account—access is mediated, not direct.\n      - In software, a proxy can delay creating a heavy object until it’s actually used.\n\n62. **What are common types of proxies?**\n    - Virtual proxy (lazy initialization)\n    - Protection proxy (access control)\n    - Remote proxy (RPC/remote objects)\n    - Caching proxy (memoization)\n    - Logging/monitoring proxy (metrics, tracing)\n    - Reasoning / scenario (detailed):\n      - Virtual proxy: load an image only when it becomes visible.\n      - Protection proxy: check user roles before calling the real service.\n      - Remote proxy: local stub talks to remote server.\n\n63. **What are the key participants in Proxy?**\n    - Subject interface (common API)\n    - RealSubject (does the real work)\n    - Proxy (implements Subject and controls access to RealSubject)\n    - Client (uses Subject)\n    - Reasoning / scenario (detailed):\n      - The subject interface lets client code remain unaware whether it talks to proxy or real.\n      - Proxy decides when/how to delegate.\n\n64. **Proxy vs Decorator: what’s the key difference?**\n    - Proxy controls access; Decorator adds responsibilities/behavior\n    - Proxy may prevent/redirect calls; Decorator generally forwards while enhancing\n    - Reasoning / scenario (detailed):\n      - A protection proxy can deny access entirely.\n      - A decorator typically shouldn’t deny; it should add behavior while preserving semantics.\n\n65. **Proxy vs Adapter: what’s the key difference?**\n    - Proxy preserves the same interface as the real subject\n    - Adapter changes the interface to make it compatible\n    - Reasoning / scenario (detailed):\n      - Proxy is a stand-in with the same API.\n      - Adapter is a translator with a different API.\n\n66. **How does a virtual proxy implement lazy loading?**\n    - Proxy holds a reference that starts as null/uninitialized\n    - On first method call, proxy creates/loads the real object\n    - Subsequent calls delegate directly\n    - Reasoning / scenario (detailed):\n      - Scenario: `ImageProxy.display()` loads the image from disk/network only when needed.\n      - This reduces startup time and memory if many images are never viewed.\n\n67. **What are trade-offs of using proxies?**\n    - Adds indirection and complexity\n    - Can hide performance costs (remote calls look like local calls)\n    - Can complicate debugging and error handling\n    - Reasoning / scenario (detailed):\n      - Remote proxies are notorious for this: a simple method call might actually be a network request with latency/failures.\n      - Good designs make “remote-ness” visible through timeouts, retries, and clear contracts.\n\n68. **How do dynamic proxies work in Java (conceptually)?**\n    - Runtime-generated proxy implements interfaces\n    - Intercepts method calls via an invocation handler\n    - Used for AOP (logging, transactions, security)\n    - Reasoning / scenario (detailed):\n      - Frameworks like Spring can wrap your service interface and inject cross-cutting concerns without modifying your class.\n      - This is Proxy pattern applied via runtime generation.\n\n69. **How do you test code that uses Proxy?**\n    - Test through the Subject interface (black-box)\n    - Verify proxy-specific behavior (lazy init, access control, caching)\n    - Use fakes/mocks for RealSubject when needed\n    - Reasoning / scenario (detailed):\n      - For caching proxy, tests should ensure repeated calls don’t hit the real subject more than once.\n      - For protection proxy, tests should ensure unauthorized access is blocked.\n\n70. **Give real-world examples of Proxy usage.**\n    - ORM lazy-loaded entities (virtual proxies)\n    - Access control wrappers (protection proxies)\n    - HTTP client stubs (remote proxies)\n    - Caching layers in front of services\n    - Reasoning / scenario (detailed):\n      - The indicator is: “I need to control when/how an object is created or accessed.”\n\n",
  "Behavioral": "# Top 110 Interview Questions on Behavioral Design Patterns\n\n## Overview / Index\n\n- [Chain of Responsibility Pattern](#chain-of-responsibility-pattern) (Q1–Q10)\n- [Command Pattern](#command-pattern) (Q11–Q20)\n- [Interpreter Pattern](#interpreter-pattern) (Q21–Q30)\n- [Iterator Pattern](#iterator-pattern) (Q31–Q40)\n- [Mediator Pattern](#mediator-pattern) (Q41–Q50)\n- [Memento Pattern](#memento-pattern) (Q51–Q60)\n- [Observer Pattern](#observer-pattern) (Q61–Q70)\n- [State Pattern](#state-pattern) (Q71–Q80)\n- [Strategy Pattern](#strategy-pattern) (Q81–Q90)\n- [Template Method Pattern](#template-method-pattern) (Q91–Q100)\n- [Visitor Pattern](#visitor-pattern) (Q101–Q110)\n\n## Chain of Responsibility Pattern\n\n1. **What is the Chain of Responsibility pattern and when should you use it?**\n   - A behavioral pattern that passes requests along a chain of handlers. Each handler decides either to process the request or pass it to the next handler. Use it when multiple objects can handle a request and the handler isn't known in advance, or when you want to issue a request to one of several objects without specifying the receiver explicitly.\n   - Reasoning / scenario (detailed):\n     - Scenario: You’re processing an HTTP request and want to apply steps like `AuthFilter → RateLimitFilter → ValidationFilter → BusinessHandler`.\n     - The client (server entrypoint) should not hardcode “call auth, then call rate limit, then call validation…”.\n     - With Chain of Responsibility, each handler either:\n       - Handles the request (and stops), or\n       - Passes it forward to the next handler.\n     - You use it when:\n       - The exact handler isn’t known ahead of time, or\n       - You want to reorder/add/remove handlers without changing the client code.\n\n2. **How does the Chain of Responsibility pattern differ from the Decorator pattern?**\n   - Chain of Responsibility: Only one handler processes the request, and it may not be processed at all. Focus is on responsibility delegation.\n   - Decorator: All decorators in the chain execute, adding behavior. Focus is on adding responsibilities to objects.\n   - Reasoning / scenario (detailed):\n     - Chain of Responsibility is about “find someone to handle this”:\n       - Scenario: exception handlers: `SqlExceptionHandler → NetworkExceptionHandler → DefaultHandler`.\n       - Only one handler is expected to handle the exception (or none).\n     - Decorator is about “everyone wraps and contributes behavior”:\n       - Scenario: stream wrappers: `BufferedInputStream(new GzipInputStream(new FileInputStream(...)))`.\n       - Every decorator participates; the final behavior is the sum of wrappers.\n\n3. **What are the main components of the Chain of Responsibility pattern?**\n   - Handler (interface): Defines interface for handling requests and optionally implements successor link\n   - ConcreteHandler: Handles requests it's responsible for, otherwise forwards to successor\n   - Client: Initiates the request to a handler in the chain\n   - Reasoning / scenario (detailed):\n     - Handler interface exists so the client can depend on one type (`handle(request)`), not on every concrete handler.\n     - The “successor link” (or a list) is what turns independent handlers into a chain.\n     - Concrete handlers each represent one responsibility (auth, logging, validation), keeping each class small and focused.\n     - The client only needs an entry point (the head of the chain), which supports loose coupling.\n\n4. **How do you prevent infinite loops in a Chain of Responsibility?**\n   - Ensure each handler only forwards to its successor, never backwards\n   - Avoid circular references when setting up the chain\n   - Implement a maximum chain depth counter\n   - Use a visited set to track processed handlers\n   - Reasoning / scenario (detailed):\n     - The loop happens when the chain becomes a cycle (A → B → C → A).\n     - In real systems, chains are often configured dynamically (DI, config files), which increases the risk of accidental cycles.\n     - Practical safeguards:\n       - Validate chain wiring at startup (detect cycles once).\n       - Add a hop counter / TTL on the request for defensive runtime safety.\n       - Use a visited-set for complex routing graphs (rare, but useful in rule engines).\n\n5. **Can a handler in the chain modify the request before passing it to the next handler?**\n   - Yes, handlers can modify, enrich, or transform the request before forwarding it. This is useful for preprocessing, validation, or adding context information.\n   - Reasoning / scenario (detailed):\n     - Scenario: an authentication handler verifies a token and attaches `userId/roles` onto the request context.\n     - A logging handler might add a correlation id, or normalize headers.\n     - This is powerful, but it’s also why you should keep the request/context structure well-defined:\n       - Otherwise handlers start depending on hidden side effects from earlier handlers.\n\n6. **What happens if no handler in the chain can process the request?**\n   - The request goes unhandled (reaches end of chain)\n   - Implement a default handler at the end to handle unprocessed requests\n   - Throw an exception or return a default response\n   - Log the unhandled request for debugging\n   - Reasoning / scenario (detailed):\n     - Chain of Responsibility deliberately allows “nobody handles it” unless you design otherwise.\n     - Scenario: in validation, if no validator recognizes a field, you may want a default rule:\n       - Fail closed (throw) for safety-critical systems, or\n       - Fail open (default behavior) for non-critical flows.\n     - A default handler makes this explicit and keeps the behavior consistent.\n\n7. **How do you implement a dynamic chain where handlers can be added or removed at runtime?**\n   - Use a List to store handlers instead of fixed successor links\n   - Provide add/remove methods in a chain manager class\n   - Iterate through the list when processing requests\n   - Use dependency injection to configure chains\n   - Reasoning / scenario (detailed):\n     - Fixed “next” pointers are great for static chains, but dynamic systems need a data structure you can edit.\n     - Scenario: feature flags enable/disable `RateLimitHandler` at runtime.\n     - A chain manager (or DI container) can:\n       - Build the list in order,\n       - Swap a handler implementation, or\n       - Insert/remove handlers without touching client code.\n\n8. **What are the performance implications of using a long chain of handlers?**\n   - Linear time complexity O(n) where n is chain length\n   - Each handler adds overhead even if it doesn't process the request\n   - Consider using a map/registry for direct handler lookup if performance is critical\n   - Long chains can make debugging difficult\n   - Reasoning / scenario (detailed):\n     - Every request potentially pays the cost of traversing handlers until it’s handled.\n     - Scenario: 30 middleware steps in a high-QPS API can add noticeable latency even if each step is “small”.\n     - Optimizations (when needed):\n       - Combine trivial handlers,\n       - Short-circuit early (cheap checks first),\n       - Replace linear search with routing (map by request type) if the goal is “pick one handler”.\n\n9. **How does the Chain of Responsibility pattern promote loose coupling?**\n   - Sender doesn't need to know which handler will process the request\n   - Handlers don't need to know about other handlers except their successor\n   - New handlers can be added without modifying existing code\n   - Reduces dependencies between sender and receiver objects\n   - Reasoning / scenario (detailed):\n     - The sender depends only on the handler interface, not on concrete classes.\n     - Each handler is isolated:\n       - It only knows how to do its responsibility and how to forward.\n     - This reduces ripple effects:\n       - Adding `FraudDetectionHandler` doesn’t require editing the request sender logic.\n\n10. **Can you give real-world examples where Chain of Responsibility is used in Java or other frameworks?**\n    - Java Servlet Filters (FilterChain)\n    - Java Exception handling (try-catch blocks)\n    - Logging frameworks (log level handlers)\n    - Event bubbling in GUI frameworks\n    - Middleware in web frameworks (Express.js, Spring Interceptors)\n    - Reasoning / scenario (detailed):\n      - Servlet filters: each filter can handle/short-circuit (e.g., auth failure) or call `chain.doFilter()` to continue.\n      - Logging: a logger can decide whether to handle a message (based on level/category) or pass it along.\n      - GUI event bubbling: events propagate up the component tree until something consumes them.\n      - Web middleware: same concept—each step can respond immediately or pass to the next middleware.\n\n## Command Pattern\n\n11. **What problem does the Command pattern solve?**\n    - Decouples the object that invokes an operation from the one that knows how to perform it\n    - Enables parameterization of objects with operations\n    - Supports queuing, logging, and undoable operations\n    - Allows structuring systems around high-level operations\n    - Reasoning / scenario (detailed):\n      - Scenario: A UI button, CLI command, or API endpoint triggers actions like `TurnLightOn`, `PlaceOrder`, `SendEmail`.\n      - Without Command, the invoker (button/controller) must directly know the receiver and call specific methods, causing tight coupling.\n      - With Command:\n        - The invoker only calls `command.execute()`.\n        - The command object encapsulates *what to do* and *which receiver to do it to*.\n      - This indirection unlocks:\n        - Queues/scheduling (commands become work items),\n        - Logging/auditing (store executed commands),\n        - Undo/redo (reverse a command).\n\n12. **What are the four main components of the Command pattern (Command, Receiver, Invoker, Client)?**\n    - Command: Interface declaring execute() method\n    - ConcreteCommand: Implements execute() and binds Receiver with action\n    - Receiver: Knows how to perform the actual work\n    - Invoker: Asks command to carry out request\n    - Client: Creates ConcreteCommand and sets its Receiver\n    - Reasoning / scenario (detailed):\n      - Scenario: A `RemoteControl` (Invoker) has buttons that can control different devices (Receivers) like `Light`, `Fan`, `GarageDoor`.\n      - Roles explained:\n        - **Receiver** contains real business logic (`light.turnOn()`).\n        - **ConcreteCommand** adapts receiver logic into a uniform action (`execute()` calls `light.turnOn()`).\n        - **Invoker** is dumb: it stores a command and triggers it (button press).\n        - **Client** wires everything together (chooses which command goes on which button).\n      - This separation is what prevents the invoker from being coupled to receiver classes.\n\n13. **How does the Command pattern support undo/redo operations?**\n    - Add unexecute()/undo() method to Command interface\n    - Store command history in a stack\n    - For undo: pop from history and call undo()\n    - For redo: maintain a separate redo stack\n    - Commands must store state needed to reverse their actions\n    - Reasoning / scenario (detailed):\n      - Scenario: A text editor supports `Type`, `Delete`, `Paste` with undo/redo.\n      - Each command records enough information to reverse itself:\n        - Insert command stores inserted text + position.\n        - Delete command stores deleted text + original position.\n      - Undo/redo becomes generic:\n        - On execute: push command to `undoStack`, clear `redoStack`.\n        - Undo: pop from `undoStack`, call `undo()`, push to `redoStack`.\n        - Redo: pop from `redoStack`, call `execute()`, push back to `undoStack`.\n      - Key idea: history is a history of *objects* (commands), not a history of scattered ad-hoc state.\n\n14. **What is the difference between Command pattern and Strategy pattern?**\n    - Command: Encapsulates a request as an object, focuses on action execution, supports undo/redo\n    - Strategy: Encapsulates algorithms, focuses on how something is done, typically stateless\n    - Command has a Receiver, Strategy doesn't necessarily\n    - Reasoning / scenario (detailed):\n      - Command is about “represent an action/request”:\n        - Example: `TransferMoneyCommand(from, to, amount)` can be queued, retried, logged, undone.\n      - Strategy is about “choose an algorithm”:\n        - Example: `PricingStrategy` changes how the total is calculated, but you typically don’t queue “calculate price” as a request.\n      - Practical hint:\n        - If you need undo/redo, scheduling, or auditing of actions → Command.\n        - If you need interchangeable ways to compute/decide something → Strategy.\n\n15. **How do you implement a macro command that executes multiple commands?**\n    - Create a MacroCommand class implementing Command interface\n    - Store a list of commands internally\n    - execute() iterates and executes all commands\n    - undo() iterates in reverse and undoes all commands\n    - Reasoning / scenario (detailed):\n      - Scenario: “Morning routine” button: turn on lights, start coffee machine, open blinds.\n      - MacroCommand is basically Composite + Command:\n        - `execute()` runs sub-commands in order.\n        - `undo()` runs sub-commands in reverse order to correctly roll back.\n      - This lets you treat a multi-step workflow as one command (for UI, scheduling, undo, logging).\n\n16. **Can commands be queued and executed asynchronously? How?**\n    - Yes, add commands to a queue (BlockingQueue in Java)\n    - Use a thread pool or executor service to process commands\n    - Commands become work items in a task queue\n    - Useful for job scheduling, thread pools, and request processing\n    - Reasoning / scenario (detailed):\n      - Scenario: an API request triggers `GenerateInvoiceCommand`, which is slow and should run in background.\n      - Instead of executing immediately:\n        - Enqueue the command into a `BlockingQueue`.\n        - Worker threads take commands and call `execute()`.\n      - Why Command fits well:\n        - A command object is a self-contained unit of work (all data needed to run).\n        - The queue/executor doesn’t need to know business details—just “run execute()”.\n\n17. **What are the advantages of using Command pattern over direct method calls?**\n    - Supports undo/redo functionality\n    - Commands can be logged, queued, and scheduled\n    - Enables macro commands (composite commands)\n    - Decouples sender from receiver\n    - Commands can be persisted and replayed\n    - Reasoning / scenario (detailed):\n      - Direct calls are “fire and forget” and disappear after execution.\n      - Commands can be treated like data:\n        - Log them (audit trail),\n        - Persist them (store in DB),\n        - Replay them (rebuild state / run jobs later).\n      - This is why Command is common in:\n        - UI frameworks (actions),\n        - Job systems (background tasks),\n        - Event sourcing (store intent as events/commands).\n\n18. **How does the Command pattern help in implementing transactional behavior?**\n    - Group commands into a transaction\n    - Execute all commands in sequence\n    - If any fails, call undo() on all executed commands\n    - Implements all-or-nothing semantics\n    - Can implement two-phase commit\n    - Reasoning / scenario (detailed):\n      - Scenario: A “Place Order” workflow:\n        - Reserve inventory → charge payment → create shipment.\n      - If shipping creation fails after payment succeeds, you need to roll back:\n        - Refund payment, release inventory.\n      - With Command:\n        - Each step is a command with `execute()` and `undo()`.\n        - A transaction runner executes commands; on failure, it undoes executed ones in reverse order.\n      - This is a practical approach for “sagas” in distributed systems (compensating transactions).\n\n19. **What is a null command or no-op command and why is it useful?**\n    - A command that does nothing when executed\n    - Eliminates need for null checks in invoker\n    - Provides default behavior\n    - Simplifies code by avoiding conditionals\n    - Example: EmptyCommand with empty execute() method\n    - Reasoning / scenario (detailed):\n      - Scenario: A remote control has 10 buttons, but you only configured 3.\n      - Without Null Command, pressing an unassigned button might cause `NullPointerException` or require `if (command != null)` everywhere.\n      - With Null Command:\n        - Unassigned buttons are set to `NoOpCommand`.\n        - The invoker always calls `execute()` safely, simplifying the invoker logic.\n\n20. **How would you implement command logging and replay functionality?**\n    - Serialize commands to a log file/database\n    - Store command type and parameters\n    - For replay: deserialize commands and execute in order\n    - Useful for crash recovery, auditing, and debugging\n    - Can implement event sourcing pattern\n    - Reasoning / scenario (detailed):\n      - Scenario: You want an audit log: “who did what, when” and also the ability to replay in a test environment.\n      - Because commands encapsulate intent, a log entry can store:\n        - command name/type (e.g., `CreateUserCommand`)\n        - parameters (userId, email, timestamp)\n        - metadata (actor, correlation id)\n      - Replay:\n        - Read log entries, reconstruct command objects, call `execute()` in order.\n      - Trade-off:\n        - You must ensure commands are deterministic and versioned (schema changes over time).\n\n## Interpreter Pattern\n\n21. **What is the Interpreter pattern and what problem does it solve?**\n    - Defines a representation for a language's grammar and an interpreter to process it\n    - Solves the problem of interpreting sentences in a language\n    - Useful for implementing domain-specific languages (DSL)\n    - Evaluates expressions based on defined grammar rules\n    - Reasoning / scenario (detailed):\n      - Scenario: you want users to write simple rules like: `age > 18 AND country == \"IN\"`.\n      - Instead of hardcoding every possible combination with `if/else`, you:\n        - Define a grammar (what expressions are allowed),\n        - Build an object model (AST) representing the expression,\n        - Evaluate/interpret the AST against some context (a user record).\n      - Interpreter is best when:\n        - The language is small and stable,\n        - You repeatedly evaluate many expressions over data.\n\n22. **What are terminal and non-terminal expressions in the Interpreter pattern?**\n    - Terminal expressions: Leaf nodes that don't contain other expressions (e.g., numbers, variables)\n    - Non-terminal expressions: Composite nodes containing other expressions (e.g., operators, functions)\n    - Terminal expressions return values directly\n    - Non-terminal expressions recursively interpret their sub-expressions\n    - Reasoning / scenario (detailed):\n      - Scenario: expression `5 + (x * 2)`\n      - Terminals are leaves:\n        - `Number(5)`, `Variable(\"x\")`, `Number(2)`\n      - Non-terminals combine other expressions:\n        - `Multiply(Variable(\"x\"), Number(2))`\n        - `Add(Number(5), Multiply(...))`\n      - This structure is what makes recursive evaluation natural: each node knows how to interpret itself.\n\n23. **When should you NOT use the Interpreter pattern?**\n    - When grammar is complex (leads to many classes)\n    - When performance is critical (interpretation is slow)\n    - When the grammar changes frequently\n    - When existing parser generators (ANTLR, JavaCC) would be better\n    - For production-level compilers or parsers\n    - Reasoning / scenario (detailed):\n      - Interpreter often creates:\n        - Many small classes (one per grammar rule),\n        - Many small objects (AST nodes).\n      - If the language is large (full SQL, full JavaScript), this becomes unmanageable and slow.\n      - Scenario: building a full SQL engine with Interpreter classes is a huge reinvention—better to use mature parsers/engines.\n      - If performance is critical:\n        - Prefer compiling expressions once (bytecode/JIT) or using a proven engine.\n\n24. **How does the Interpreter pattern relate to the Composite pattern?**\n    - Interpreter uses Composite pattern for its structure\n    - Abstract syntax tree is a composite structure\n    - Terminal expressions are leaves, non-terminal are composites\n    - Both use recursive composition\n    - interpret() method is like Composite's operation()\n    - Reasoning / scenario (detailed):\n      - Composite is about representing “part-whole” trees uniformly.\n      - An AST is exactly a tree:\n        - Leaves = terminals,\n        - Internal nodes = operators.\n      - Because every node shares the same interface (`interpret(context)`), clients can treat the whole tree and subtrees the same way.\n      - That uniformity is the Composite idea applied to language expressions.\n\n25. **What is the difference between Interpreter pattern and Visitor pattern?**\n    - Interpreter: Each class knows how to interpret itself (interpret() in each node)\n    - Visitor: Interpretation logic is externalized to visitor classes\n    - Interpreter: Adding new operations requires modifying all classes\n    - Visitor: Adding new operations is easy, adding new node types is hard\n    - Reasoning / scenario (detailed):\n      - Interpreter “puts behavior on the nodes”:\n        - Each node implements `interpret()` (or `evaluate()`).\n        - Adding a new operation (e.g., pretty-print, type-check) means adding methods or changing nodes.\n      - Visitor “pulls operations out of the nodes”:\n        - Nodes accept a visitor; operations live in visitors.\n        - Adding a new operation is adding a new visitor class (easy).\n        - Adding a new node type forces updating all visitors (hard).\n      - Choose based on what changes more:\n        - If operations change frequently → Visitor helps.\n        - If the node types and evaluation are core and stable → Interpreter can be simpler.\n\n26. **How do you handle operator precedence in an expression interpreter?**\n    - Build the abstract syntax tree respecting precedence during parsing\n    - Use parentheses to enforce evaluation order\n    - Parse higher precedence operators first\n    - Use a precedence table in the parser\n    - Example: multiplication before addition\n    - Reasoning / scenario (detailed):\n      - Precedence is a parsing concern, not an evaluation concern.\n      - Scenario: `3 + 5 * 2`\n        - Correct meaning is `3 + (5 * 2)` not `(3 + 5) * 2`.\n      - You handle this by building the AST so that:\n        - `Multiply(5, 2)` becomes a subtree,\n        - Then `Add(3, Multiply(...))` becomes the root.\n      - Parentheses override precedence by forcing subtree grouping.\n\n27. **What are the performance limitations of the Interpreter pattern?**\n    - Recursive interpretation is slower than compiled code\n    - Creates many small objects (memory overhead)\n    - No optimization of expressions\n    - Repeated interpretation of same expression is inefficient\n    - Consider caching or compilation for better performance\n    - Reasoning / scenario (detailed):\n      - Interpreting means repeatedly walking the AST and doing dynamic dispatch per node.\n      - Scenario: you evaluate the same pricing rule for 1 million carts:\n        - Re-parsing and rebuilding AST each time is very expensive.\n      - Typical mitigations:\n        - Parse once → reuse the AST (cache compiled form).\n        - Precompute constant sub-expressions (constant folding).\n        - Compile AST to bytecode/lambdas for faster repeated runs.\n\n28. **Can you explain the Abstract Syntax Tree (AST) in the context of Interpreter pattern?**\n    - Tree representation of the abstract syntactic structure\n    - Internal nodes are operators/non-terminals\n    - Leaf nodes are operands/terminals\n    - Built during parsing phase\n    - Traversed during interpretation phase\n    - Example: \"3 + 5\" becomes AddExpression(Number(3), Number(5))\n    - Reasoning / scenario (detailed):\n      - AST separates “syntax” from “execution”:\n        - After parsing, you have a structured tree, not a raw string.\n      - Benefits:\n        - Easier evaluation (recursive),\n        - Easier transformations (optimize, simplify),\n        - Easier tooling (pretty-print, lint, validate).\n      - Scenario: for `\"3 + 5\"`, the AST root is `Add`, with children `Number(3)` and `Number(5)`.\n\n29. **How would you implement a simple SQL query interpreter using this pattern?**\n    - Create terminal expressions: TableExpression, ColumnExpression, ValueExpression\n    - Create non-terminal expressions: SelectExpression, WhereExpression, AndExpression\n    - Parse SQL string into AST\n    - interpret() method executes the query\n    - Context holds database connection and result set\n    - Reasoning / scenario (detailed):\n      - Scenario: You support a tiny subset of SQL: `SELECT name FROM users WHERE age > 18 AND city = 'Pune'`.\n      - Parsing builds an AST like:\n        - `Select(columns=[name], table=users, where=And(Greater(age, 18), Equals(city, 'Pune')))`\n      - Interpretation:\n        - The `SelectExpression` asks the context for the table data.\n        - The `WhereExpression` filters rows by evaluating the predicate subtree.\n      - Trade-off:\n        - Good for small DSLs; for real SQL, use a full parser/engine.\n\n30. **What are alternatives to the Interpreter pattern for complex grammars?**\n    - Parser generators (ANTLR, Yacc, Bison)\n    - Compiler-compilers\n    - Regular expressions for simple patterns\n    - Scripting language engines (JavaScript engine, Python interpreter)\n    - Domain-specific language frameworks\n    - Reasoning / scenario (detailed):\n      - When grammars get complex, hand-writing AST classes and parsers is error-prone and slow to evolve.\n      - Parser generators give:\n        - A formal grammar file,\n        - Automatically generated parsers,\n        - Better error reporting and maintainability for big languages.\n      - If you only need simple matching (like log parsing), regex may be sufficient.\n      - If a mature scripting engine exists, embedding it can be cheaper than building your own language.\n\n## Iterator Pattern\n\n31. **What is the Iterator pattern and why is it important?**\n    - Provides a way to access elements sequentially without exposing underlying representation\n    - Separates traversal logic from collection\n    - Enables multiple simultaneous traversals\n    - Provides uniform interface for different collection types\n    - Fundamental to collection frameworks\n    - Reasoning / scenario (detailed):\n      - Scenario: You have different collections (array, linked list, tree) but want a consistent way to traverse them.\n      - Without Iterator, clients must know the internal structure:\n        - arrays use indexes, lists use nodes, trees use recursion.\n      - Iterator provides a uniform contract (`hasNext()/next()`), so traversal logic doesn’t leak collection internals.\n      - This improves:\n        - Encapsulation (collection hides representation),\n        - Reuse (same traversal code works across collections),\n        - API clarity (clients don’t need special-case traversal).\n\n32. **What is the difference between internal and external iterators?**\n    - External: Client controls iteration (hasNext(), next()). More flexible, client-driven.\n    - Internal: Iterator controls iteration (forEach with callback). Simpler for client, less control.\n    - External: Java Iterator, C++ iterators\n    - Internal: Java Stream.forEach(), Ruby each\n    - Reasoning / scenario (detailed):\n      - External iterator gives control to the client:\n        - Client can pause, break, skip, or interleave multiple iterations.\n        - Example: iterate until you find the first match, then stop.\n      - Internal iterator gives control to the collection/framework:\n        - You provide a function/callback and the framework drives iteration.\n        - Example: `stream.forEach(...)` is concise but it’s harder to “break early” or manage complex control flow.\n      - Trade-off: internal is simpler syntax; external is more flexible.\n\n33. **How does the Iterator pattern support multiple simultaneous traversals?**\n    - Each iterator maintains its own traversal state\n    - Multiple iterator instances can exist for same collection\n    - Each tracks its own position independently\n    - Collection remains unchanged during iteration\n    - Reasoning / scenario (detailed):\n      - Scenario: Two parts of your code iterate the same list at the same time:\n        - one computes a sum, another searches for an item.\n      - Each iterator object stores its own cursor/index.\n      - Because state is inside iterators (not in the collection), multiple traversals can happen independently without interfering.\n\n34. **What is the fail-fast behavior in iterators and how is it implemented?**\n    - Iterator throws ConcurrentModificationException if collection is modified during iteration\n    - Collection maintains a modCount (modification counter)\n    - Iterator stores expectedModCount at creation\n    - Before each operation, checks if modCount == expectedModCount\n    - Prevents unpredictable behavior from concurrent modifications\n    - Reasoning / scenario (detailed):\n      - Problem: If you modify a list while iterating, the iterator’s internal position can become invalid (skip elements, duplicate, out-of-bounds).\n      - Fail-fast chooses safety:\n        - detect “someone changed the collection” and throw immediately.\n      - In Java collections this is commonly done with:\n        - `modCount` in the collection,\n        - `expectedModCount` stored in the iterator,\n        - check on `next()` / `remove()`.\n      - Note: fail-fast is not a concurrency guarantee; it’s a best-effort bug detector.\n\n35. **How do you implement a bidirectional iterator?**\n    - Add previous() and hasPrevious() methods\n    - Maintain current position that can move forward or backward\n    - Example: Java ListIterator\n    - Requires collection to support backward traversal\n    - Useful for doubly-linked lists\n    - Reasoning / scenario (detailed):\n      - Scenario: You build a text editor and want to move a cursor forward/backward through tokens.\n      - A bidirectional iterator needs:\n        - a position pointer,\n        - ability to step back (which is efficient when the underlying structure supports it, e.g., array index or doubly-linked list).\n      - That’s why Java provides `ListIterator` for lists, but not for all collections.\n\n36. **What is the difference between Iterator and Enumeration in Java?**\n    - Iterator: hasNext(), next(), remove(). Fail-fast. Modern.\n    - Enumeration: hasMoreElements(), nextElement(). No remove. Legacy.\n    - Iterator allows element removal during iteration\n    - Iterator has better method names\n    - Enumeration is in legacy classes (Vector, Hashtable)\n    - Reasoning / scenario (detailed):\n      - Enumeration is an older API (pre-Collections Framework) and is intentionally minimal.\n      - Iterator modernizes traversal by adding:\n        - consistent naming (`hasNext/next`),\n        - safe removal via `iterator.remove()` (when supported),\n        - fail-fast checks in most implementations.\n      - In interviews, think of Enumeration as legacy compatibility, Iterator as the standard.\n\n37. **How does the Iterator pattern support different traversal algorithms?**\n    - Different iterator implementations for different traversals\n    - Examples: PreOrderIterator, PostOrderIterator for trees\n    - BreadthFirstIterator, DepthFirstIterator for graphs\n    - FilteredIterator for conditional traversal\n    - Each encapsulates specific traversal logic\n    - Reasoning / scenario (detailed):\n      - Scenario: A tree can be traversed in preorder, inorder, postorder, BFS.\n      - Instead of hardcoding traversal logic inside the tree class (or duplicating it in clients),\n        you implement different iterator classes that encapsulate traversal state/algorithm.\n      - This keeps the data structure stable while letting traversal behavior vary independently.\n\n38. **What are the thread-safety concerns when using iterators?**\n    - Iterators are generally not thread-safe\n    - Concurrent modification causes ConcurrentModificationException\n    - Use ConcurrentHashMap, CopyOnWriteArrayList for concurrent access\n    - Synchronize on collection during iteration\n    - Use concurrent iterators that support weak consistency\n    - Reasoning / scenario (detailed):\n      - Scenario: Thread A iterates a list while Thread B adds/removes elements.\n      - Outcomes can include:\n        - fail-fast exceptions,\n        - missed elements,\n        - inconsistent reads.\n      - Options:\n        - Synchronize iteration and modification on a lock (simple, can block).\n        - Use concurrent collections:\n          - `CopyOnWriteArrayList` is great for many reads/few writes.\n          - `ConcurrentHashMap` iterators are weakly consistent (don’t throw, may not see all updates).\n      - Choose based on read/write patterns and correctness requirements.\n\n39. **How do you implement a filtered iterator that skips certain elements?**\n    - Wrap existing iterator\n    - Override next() to skip elements not matching predicate\n    - hasNext() advances to next valid element\n    - Store predicate/filter condition\n    - Example: FilteredIterator with Predicate<T>\n    - Reasoning / scenario (detailed):\n      - Scenario: Iterate only “active” users from a list.\n      - A filtered iterator is a wrapper:\n        - It holds a base iterator and a predicate.\n        - `hasNext()` moves forward until it finds a match.\n        - `next()` returns the cached match.\n      - This keeps filtering logic out of client code and makes iteration reusable/composable.\n\n40. **What is the relationship between Iterator pattern and the for-each loop in Java?**\n    - for-each loop uses Iterator internally\n    - Collection must implement Iterable interface\n    - Iterable.iterator() returns an Iterator\n    - Compiler translates for-each to iterator calls\n    - Syntactic sugar over Iterator pattern\n    - Reasoning / scenario (detailed):\n      - The enhanced for-loop:\n        - `for (T x : collection) { ... }`\n        is compiled roughly into:\n        - `for (Iterator<T> it = collection.iterator(); it.hasNext(); ) { T x = it.next(); ... }`\n      - That’s why any custom collection becomes “for-each compatible” by implementing `Iterable` and returning an `Iterator`.\n\n## Mediator Pattern\n\n41. **What is the Mediator pattern and what problem does it solve?**\n    - Defines an object that encapsulates how objects interact\n    - Reduces coupling by preventing objects from referring to each other explicitly\n    - Centralizes complex communications and control logic\n    - Promotes loose coupling between colleagues\n    - Reasoning / scenario (detailed):\n      - Scenario: A dialog box has many controls: textbox, checkbox, button, dropdown.\n      - Without Mediator, controls talk to each other directly:\n        - textbox enables button, checkbox disables dropdown, etc.\n      - This creates many-to-many connections and tangled logic.\n      - Mediator introduces one coordinator (the dialog/controller):\n        - Controls notify the mediator about events,\n        - Mediator decides what other controls should do.\n      - Net result: fewer dependencies between controls and centralized interaction rules.\n\n42. **How does the Mediator pattern reduce coupling between objects?**\n    - Objects communicate through mediator instead of directly\n    - Colleagues only know about the mediator, not each other\n    - Changes to interaction logic only affect mediator\n    - Reduces many-to-many relationships to one-to-many\n    - Reasoning / scenario (detailed):\n      - Scenario: `UserList`, `MessageInput`, and `SendButton` in a chat UI.\n      - Without Mediator, each component must hold references to the others and coordinate state changes.\n      - With Mediator:\n        - Each component only references the mediator.\n        - When `UserList` changes selection, it notifies mediator.\n        - Mediator tells `SendButton` whether it should be enabled and updates `MessageInput` placeholder, etc.\n      - When you change the rules, you change mediator logic, not every pair of components.\n\n43. **What is the difference between Mediator and Observer patterns?**\n    - Mediator: Bidirectional communication, centralized control, colleagues know mediator\n    - Observer: Unidirectional notification, decentralized, observers don't know each other\n    - Mediator: Coordinates behavior between objects\n    - Observer: Distributes notifications to multiple observers\n    - Reasoning / scenario (detailed):\n      - Observer is “broadcast”:\n        - Scenario: `NewsAgency` notifies many subscribers when news changes.\n        - Observers react independently; the subject doesn’t coordinate them.\n      - Mediator is “orchestration”:\n        - Scenario: UI controls must coordinate behavior (enable/disable, validation).\n        - The mediator contains interaction rules and triggers actions across colleagues.\n      - Quick test:\n        - If you need a central brain to coordinate many components → Mediator.\n        - If you need a subject to notify many listeners of changes → Observer.\n\n44. **How do you prevent the Mediator from becoming a \"god object\"?**\n    - Keep mediator focused on coordination, not business logic\n    - Split large mediators into smaller, specialized mediators\n    - Use multiple mediators for different subsystems\n    - Delegate complex logic to colleagues\n    - Follow Single Responsibility Principle\n    - Reasoning / scenario (detailed):\n      - Risk: once all interactions go through mediator, it can accumulate too much logic and become hard to maintain.\n      - Scenario: A single “AppMediator” handles chat, payments, notifications, settings—too broad.\n      - Mitigations:\n        - Keep mediators scoped (e.g., `CheckoutMediator`, `ChatRoomMediator`).\n        - Move domain logic into services; mediator should mostly route events and call services.\n        - Use multiple mediators and compose them rather than one mega-mediator.\n\n45. **Can you have multiple mediators in a system? When would you do this?**\n    - Yes, for different subsystems or contexts\n    - Example: ChatRoomMediator for chat, GameMediator for game logic\n    - Prevents single mediator from becoming too complex\n    - Each mediator handles a specific domain\n    - Mediators can be hierarchical\n    - Reasoning / scenario (detailed):\n      - Scenario: A large app has independent modules:\n        - Chat, Checkout, Admin dashboard.\n      - Each module can have its own mediator because the interaction rules are different.\n      - Hierarchical mediators can coordinate:\n        - local mediator handles local UI interactions,\n        - higher-level mediator coordinates between modules (navigation, global events).\n\n46. **How does the Mediator pattern affect testability of components?**\n    - Colleagues become easier to test in isolation\n    - Mock the mediator for unit testing colleagues\n    - Mediator itself needs comprehensive testing\n    - Reduces test complexity by centralizing interactions\n    - Can test interaction logic separately from business logic\n    - Reasoning / scenario (detailed):\n      - Without Mediator, each component requires many collaborators in tests (lots of mocking/wiring).\n      - With Mediator:\n        - You can unit-test a colleague by mocking the mediator interface (“when clicked, notify mediator”).\n        - You can separately test the mediator’s interaction rules with fake colleagues.\n      - Trade-off: mediator tests may be broader, but the system becomes more modular overall.\n\n47. **What are the trade-offs between using Mediator vs direct object communication?**\n    - Mediator: Less coupling, centralized control, potential bottleneck\n    - Direct: Simpler for few objects, tighter coupling, distributed logic\n    - Mediator adds indirection and complexity\n    - Direct communication is more efficient\n    - Use Mediator when interactions are complex\n    - Reasoning / scenario (detailed):\n      - Direct communication is great when there are only 2–3 objects and interactions are simple.\n      - As interactions grow, direct communication creates:\n        - tangled dependencies,\n        - duplicated rules (multiple classes implementing the same coordination).\n      - Mediator trades:\n        - extra indirection and a central point,\n        for:\n        - cleaner dependencies and single place for interaction rules.\n      - Bottleneck risk is mostly about design quality (scope mediators properly).\n\n48. **How do you implement a mediator that handles asynchronous communication?**\n    - Use message queues or event bus\n    - Implement async methods with callbacks or Promises\n    - Use ExecutorService for async processing\n    - Store pending requests and process asynchronously\n    - Example: mediator.sendAsync(message, callback)\n    - Reasoning / scenario (detailed):\n      - Scenario: In a chat system, sending a message involves network I/O.\n      - The mediator can:\n        - enqueue an action on an executor,\n        - or publish events to an event bus/message queue.\n      - This keeps colleagues responsive and avoids blocking UI threads.\n      - You often model this as:\n        - mediator methods returning `Future/CompletableFuture` (Java) or using callbacks.\n\n49. **What is the difference between Mediator and Facade patterns?**\n    - Mediator: Bidirectional, colleagues know mediator, coordinates behavior\n    - Facade: Unidirectional, subsystem doesn't know facade, simplifies interface\n    - Mediator: Colleagues communicate through it\n    - Facade: Provides unified interface to subsystem\n    - Reasoning / scenario (detailed):\n      - Facade is for simplifying usage of a subsystem:\n        - Scenario: `VideoConverterFacade.convert()` hides many underlying classes.\n        - Subsystem classes are unaware of the facade.\n      - Mediator is for coordinating peer objects:\n        - Scenario: UI components or services that must collaborate.\n        - Colleagues know the mediator and report events to it.\n      - Facade reduces complexity for the caller; Mediator reduces coupling between peers.\n\n50. **Can you give examples of Mediator pattern in GUI frameworks?**\n    - Dialog boxes coordinating form controls\n    - MVC Controller mediating between Model and View\n    - Event dispatchers in JavaScript frameworks\n    - Java Swing's Action mechanism\n    - Air traffic control system (classic example)\n    - Reasoning / scenario (detailed):\n      - Dialog boxes: the dialog (mediator) decides which controls are enabled based on inputs.\n      - MVC controller: coordinates between view events and model updates.\n      - Event dispatchers: central place to route UI events and orchestrate reactions.\n      - Air traffic control: planes don’t coordinate with each other directly; they coordinate via the tower (mediator).\n\n## Memento Pattern\n\n51. **What is the Memento pattern and when should you use it?**\n    - Captures and externalizes object's internal state for later restoration\n    - Use for undo/redo functionality\n    - Use when you need snapshots of object state\n    - Use when direct access to state would violate encapsulation\n    - Reasoning / scenario (detailed):\n      - Scenario: a text editor wants “undo” without exposing all internal fields of the editor to other objects.\n      - The editor (Originator) can create a snapshot (Memento) of its state before changes.\n      - Later, it restores from that snapshot to go back in time.\n      - Use Memento when:\n        - You need rollbacks/undo,\n        - Directly exposing internal state would break encapsulation,\n        - Snapshots are acceptable in memory/time cost.\n\n52. **How does the Memento pattern preserve encapsulation?**\n    - Memento stores state but doesn't expose it publicly\n    - Only Originator can access memento's internal state\n    - Caretaker holds memento but can't examine or modify it\n    - Uses narrow and wide interfaces (public vs package-private)\n    - Reasoning / scenario (detailed):\n      - The caretaker must be able to store mementos, but shouldn’t be able to read/modify them.\n      - Scenario: `History` keeps snapshots of `TextEditor`, but `History` should not see editor internals.\n      - So:\n        - Memento is opaque to the caretaker (narrow interface).\n        - Originator can read/write the internal state inside the memento (wide access).\n      - This keeps “who can see state” controlled, even though state is being stored externally.\n\n53. **What are the three main roles in the Memento pattern (Originator, Memento, Caretaker)?**\n    - Originator: Creates memento with its current state, restores from memento\n    - Memento: Stores internal state of Originator, protects against access by others\n    - Caretaker: Responsible for memento's safekeeping, never operates on memento\n    - Reasoning / scenario (detailed):\n      - Scenario mapping:\n        - Originator = `TextEditor` (has internal content/cursor/formatting).\n        - Memento = `EditorSnapshot` (captures those details).\n        - Caretaker = `History` (push/pop snapshots, but doesn’t interpret them).\n      - This separation avoids:\n        - Caretaker becoming coupled to originator’s internal representation,\n        - “Snapshot logic” leaking across the codebase.\n\n54. **How do you implement undo/redo functionality using the Memento pattern?**\n    - Maintain two stacks: undoStack and redoStack\n    - Before change: push current state to undoStack\n    - Undo: pop from undoStack, push current to redoStack, restore\n    - Redo: pop from redoStack, push current to undoStack, restore\n    - Clear redoStack when new change is made\n    - Reasoning / scenario (detailed):\n      - Two stacks model time travel:\n        - undoStack = past states,\n        - redoStack = states you “undid” and can go forward to.\n      - Workflow:\n        - Before mutating the originator, capture a memento and push to undo.\n        - On undo: save current state to redo, restore previous from undo.\n        - On redo: save current state to undo, restore from redo.\n        - When a new action happens after undo, redo history becomes invalid → clear redo stack.\n\n55. **What are the memory implications of storing multiple mementos?**\n    - Each memento consumes memory proportional to state size\n    - Many mementos can cause memory issues\n    - Consider limiting history size\n    - Use incremental mementos for large objects\n    - Implement memento cleanup/expiration policy\n    - Reasoning / scenario (detailed):\n      - A memento is effectively a copy of state.\n      - Scenario: large document with images and formatting—full snapshots can be big.\n      - If you store hundreds of snapshots, memory grows quickly.\n      - Typical strategies:\n        - cap history (last N states),\n        - compress snapshots,\n        - store diffs (incremental mementos),\n        - offload to disk for long histories (slower).\n\n56. **How do you implement incremental mementos to save memory?**\n    - Store only changes (deltas) instead of full state\n    - First memento stores full state, subsequent store diffs\n    - Restoration requires applying deltas in sequence\n    - More complex but memory-efficient\n    - Example: store \"changed field X from A to B\"\n    - Reasoning / scenario (detailed):\n      - Scenario: a drawing app where each action changes only a small part of the canvas.\n      - Instead of snapshotting the entire canvas every time, store deltas:\n        - “added shape S”, “moved shape A from (x1,y1) to (x2,y2)”.\n      - Undo applies the inverse delta; restore replays deltas from a base snapshot.\n      - Trade-off:\n        - much less memory,\n        - more complexity (need to ensure deltas are correct and composable).\n\n57. **What is the difference between Memento and Command patterns for undo operations?**\n    - Memento: Stores state snapshots, restores previous state\n    - Command: Stores operations, executes inverse operations\n    - Memento: Better for complex state, simpler undo logic\n    - Command: Better for simple state, more flexible\n    - Can combine both patterns\n    - Reasoning / scenario (detailed):\n      - Memento undo = “restore old snapshot”:\n        - Great when state is complex and hard to reverse step-by-step.\n      - Command undo = “do the opposite operation”:\n        - Example: `AddItemCommand.undo()` removes the item that was added.\n        - Great when operations are clear and inverses are easy.\n      - Combine:\n        - Use Command for high-level user actions,\n        - Use Memento internally for complex objects where inverse logic is painful.\n\n58. **How do you serialize mementos for persistence?**\n    - Implement Serializable interface\n    - Use ObjectOutputStream to write to file/database\n    - Use JSON/XML serialization libraries\n    - Store in database as BLOB or structured data\n    - Ensure all state fields are serializable\n    - Reasoning / scenario (detailed):\n      - Scenario: You want “restore session after crash” for an editor.\n      - You can persist snapshots:\n        - binary serialization for speed (but versioning is tricky),\n        - JSON for readability and cross-language (but bigger/slower).\n      - Practical concerns:\n        - version your snapshot schema,\n        - avoid storing sensitive data in plain text,\n        - ensure backward compatibility when state structure evolves.\n\n59. **Can mementos be shared between different originators?**\n    - Generally no, mementos are specific to their originator\n    - Sharing violates encapsulation\n    - Different originators have different state structures\n    - Exception: if originators share same state structure\n    - Reasoning / scenario (detailed):\n      - A memento is meaningful only to the originator that created it.\n      - Scenario: `TextEditorSnapshot` contains fields specific to `TextEditor` (cursor position, formatting).\n      - If another originator consumes it, it either:\n        - can’t interpret it, or\n        - must understand internals (breaking encapsulation).\n      - Sharing is only safe when originators truly share the same state model and semantics (rare in practice).\n\n60. **How do you implement a memento with limited history (e.g., only last 10 states)?**\n    - Use a bounded queue or circular buffer\n    - When limit reached, remove oldest memento\n    - LinkedList with size check\n    - Example: if (history.size() > 10) history.removeFirst();\n    - Consider LRU cache for more sophisticated eviction\n    - Reasoning / scenario (detailed):\n      - Limiting history is a practical memory control.\n      - Scenario: mobile app note editor—keeping unlimited snapshots can OOM the app.\n      - A circular buffer gives:\n        - constant memory usage,\n        - predictable performance.\n      - Trade-off:\n        - you can’t undo beyond the last N operations (acceptable in many products).\n\n## Observer Pattern\n\n61. **What is the Observer pattern and what problem does it solve?**\n    - Defines one-to-many dependency where state changes notify all dependents\n    - Solves the problem of keeping related objects synchronized\n    - Enables loose coupling between subject and observers\n    - Supports broadcast communication\n    - Reasoning / scenario (detailed):\n      - Scenario: `NewsAgency` publishes news; many `NewsReader` subscribers want updates.\n      - Without Observer, the agency must know every dependent and call them directly (tight coupling).\n      - With Observer:\n        - Observers subscribe/unsubscribe dynamically.\n        - The subject broadcasts updates without knowing concrete observer types.\n      - This keeps components synchronized (views update when model changes) while remaining loosely coupled.\n\n62. **What is the difference between push and pull models in Observer pattern?**\n    - Push: Subject sends detailed data to observers in update(data)\n    - Pull: Subject sends minimal notification, observers query for data\n    - Push: Observers get data they may not need\n    - Pull: Observers get only what they need, requires subject reference\n    - Hybrid: Send notification with subject reference\n    - Reasoning / scenario (detailed):\n      - Scenario: Stock price updates.\n      - Push:\n        - Subject sends `update(price, timestamp, volume, ...)` to all observers.\n        - Simple for observers but can send unnecessary data.\n      - Pull:\n        - Subject sends “changed” notification; observers call `subject.getPrice()` if they care.\n        - Less wasted data but observers must hold a subject reference and do extra calls.\n      - Hybrid:\n        - Send minimal event plus a reference, so observers can decide what to pull.\n\n63. **How do you prevent memory leaks in Observer pattern implementations?**\n    - Always unsubscribe observers when no longer needed\n    - Use weak references (WeakHashMap, WeakReference)\n    - Implement explicit cleanup/dispose methods\n    - Avoid anonymous inner classes that hold implicit references\n    - Use lifecycle-aware observers (Android LiveData)\n    - Reasoning / scenario (detailed):\n      - The common leak: Subject holds strong references to observers.\n      - Scenario: a UI screen subscribes to a global event bus, then is closed.\n        - If it doesn’t unsubscribe, the bus still references it → it can’t be garbage-collected.\n      - Mitigations:\n        - Explicit unsubscribe in lifecycle hooks.\n        - Weak references for “best-effort” auto-cleanup.\n        - Framework support (lifecycle-aware subscriptions) to avoid manual mistakes.\n\n64. **What is the difference between Observer and Pub-Sub (Publish-Subscribe) patterns?**\n    - Observer: Subject knows observers, tight coupling, synchronous\n    - Pub-Sub: Publishers don't know subscribers, message broker in between, can be async\n    - Observer: Direct reference between subject and observers\n    - Pub-Sub: Decoupled through event channel/message queue\n    - Reasoning / scenario (detailed):\n      - Observer is direct:\n        - Subject has a list of observers and calls them.\n        - Works well inside one process/module.\n      - Pub-Sub is brokered:\n        - Publisher emits to a topic; subscribers listen to the topic via a broker (Kafka, RabbitMQ, event bus).\n        - Publisher and subscriber don’t know each other at all.\n      - Pub-Sub is better for distributed/asynchronous systems; Observer is simpler for in-memory object graphs.\n\n65. **How do you handle exceptions thrown by observers during notification?**\n    - Catch exceptions in notification loop, continue notifying others\n    - Log exceptions for debugging\n    - Optionally remove failing observers\n    - Use try-catch around each observer.update()\n    - Consider async notification to isolate failures\n    - Reasoning / scenario (detailed):\n      - Scenario: 10 observers listen for “price changed”. One observer has a bug and throws.\n      - If you don’t isolate exceptions, one bad observer can prevent all others from receiving updates.\n      - Best practice:\n        - wrap each `update()` call in try/catch,\n        - log errors,\n        - optionally quarantine/remove repeatedly failing observers.\n      - Async notification can further isolate failures but introduces concurrency/ordering concerns.\n\n66. **What are the thread-safety concerns in Observer pattern?**\n    - Concurrent modifications to observer list during notification\n    - Use CopyOnWriteArrayList for observer list\n    - Synchronize subscribe/unsubscribe operations\n    - Consider using concurrent collections\n    - Avoid deadlocks when observers call back to subject\n    - Reasoning / scenario (detailed):\n      - Scenario: One thread is notifying observers while another thread subscribes/unsubscribes.\n      - Risks:\n        - `ConcurrentModificationException`,\n        - missed notifications,\n        - inconsistent observer list.\n      - Techniques:\n        - snapshot the observer list before notifying,\n        - use `CopyOnWriteArrayList` (great for many reads/few writes),\n        - synchronize carefully to avoid deadlocks when observers call back into the subject.\n\n67. **How do you implement priority-based notification in Observer pattern?**\n    - Store observers with priority in PriorityQueue or sorted list\n    - Sort observers by priority before notification\n    - Notify high-priority observers first\n    - Example: observers implement Comparable with priority\n    - Use TreeSet with custom comparator\n    - Reasoning / scenario (detailed):\n      - Scenario: In a trading system, risk checks must run before UI updates.\n      - Priorities allow ordering:\n        - high priority observers validate/guard,\n        - lower priority observers update views/logs.\n      - Implementation approaches:\n        - maintain observers in a sorted structure,\n        - or sort a snapshot before notifying (simpler, but sorting each time has cost).\n\n68. **What is the difference between Observer pattern and Event Listener pattern?**\n    - Essentially the same pattern\n    - Event Listener: Java/GUI terminology (ActionListener, MouseListener)\n    - Observer: GoF terminology\n    - Event Listener often uses event objects\n    - Both implement same concept with different naming\n    - Reasoning / scenario (detailed):\n      - In practice, GUI “listeners” are observers.\n      - The difference is mostly convention:\n        - Event Listener style often passes an event object (`ActionEvent`) and registers via `addXListener`.\n        - Observer pattern is described more generally (subject/observers), sometimes without explicit event classes.\n      - Both are about one-to-many notification when something happens.\n\n69. **How do you prevent cascading updates in Observer pattern?**\n    - Set a flag during notification to prevent re-entry\n    - Use version numbers to detect if already updated\n    - Batch updates and notify once\n    - Implement update coalescing\n    - Use dependency graph to determine update order\n    - Reasoning / scenario (detailed):\n      - Cascading update problem:\n        - Observer A updates Subject B, which notifies observers, which updates Subject A again… (feedback loop).\n      - Scenario: Two UI models observe each other (bidirectional binding) → infinite ping-pong.\n      - Solutions:\n        - re-entrancy guard flags,\n        - change versioning (ignore if already processed),\n        - batch/coalesce updates (notify once after multiple changes),\n        - explicit dependency graph for complex systems.\n\n70. **Can you explain how Observer pattern is used in MVC architecture?**\n    - Model is the Subject\n    - Views are Observers\n    - Model notifies Views when data changes\n    - Views update their display automatically\n    - Enables multiple views of same data\n    - Reasoning / scenario (detailed):\n      - MVC goal: keep UI views in sync with the underlying model without tight coupling.\n      - Scenario: A `UserModel` changes its `name`.\n        - The model notifies observers.\n        - A “profile view” updates the label.\n        - A “header view” updates the greeting.\n      - Multiple views can subscribe to the same model, enabling consistent UI updates with minimal wiring.\n\n## State Pattern\n\n71. **What is the State pattern and when should you use it?**\n    - Allows object to alter behavior when internal state changes\n    - Use when object behavior depends on state with many conditional statements\n    - Use when state transitions are explicit and numerous\n    - Eliminates large switch/if-else blocks\n    - Reasoning / scenario (detailed):\n      - Scenario: A vending machine behaves differently depending on state: `NoCoin`, `HasCoin`, `ProductSelected`.\n        - If no coin: `dispense()` should refuse.\n        - If has coin: `selectProduct()` should advance the flow.\n      - Without State pattern, you typically write big `if (state == ...)` checks in every operation.\n      - With State pattern, behavior is split into state classes, and the context delegates to the current state.\n      - Use it when “what an object does” depends heavily on internal state and transitions are well-defined.\n\n72. **How does the State pattern eliminate conditional statements?**\n    - Each state is a separate class with its own behavior\n    - Context delegates to current state object\n    - State-specific behavior is in state classes, not conditionals\n    - Polymorphism replaces conditionals\n    - Example: instead of if(state==OPEN), use currentState.handle()\n    - Reasoning / scenario (detailed):\n      - Scenario: `Order.cancel()` has different outcomes:\n        - `New` → cancelled,\n        - `Shipped` → cannot cancel,\n        - `Delivered` → cannot cancel.\n      - Without State, `cancel()` becomes a conditional branching on the order’s state.\n      - With State, each state class implements `cancel()` behavior; polymorphism chooses the right behavior automatically.\n\n73. **What is the difference between State and Strategy patterns?**\n    - State: Context behavior changes based on internal state, states know about transitions\n    - Strategy: Client chooses algorithm, strategies are independent\n    - State: States can trigger transitions\n    - Strategy: Strategies don't change each other\n    - State: Typically not set by client\n    - Reasoning / scenario (detailed):\n      - Both patterns can look similar (context holds an interface), but the intent differs.\n      - State:\n        - Scenario: `TCPConnection` moves through `Closed → Listening → Established`.\n        - The object transitions as events happen; states often decide the next state.\n      - Strategy:\n        - Scenario: `CompressionStrategy` is chosen (ZIP vs GZIP) and usually doesn’t transition on its own.\n      - Quick test:\n        - If transitions are core and automatic → **State**.\n        - If it’s a plug-in algorithm choice → **Strategy**.\n\n74. **Should state transitions be controlled by the Context or the State objects?**\n    - Both approaches are valid:\n    - Context-controlled: Centralized transition logic, context knows all states\n    - State-controlled: Distributed logic, more flexible, states know successors\n    - State-controlled is more common and flexible\n    - Context-controlled is better for complex transition rules\n    - Reasoning / scenario (detailed):\n      - Context-controlled transitions:\n        - Scenario: complex workflow rules best kept in one place for auditing.\n        - Risk: context can become a big switch statement.\n      - State-controlled transitions:\n        - Scenario: `HasCoinState.selectProduct()` naturally sets the next state.\n        - Benefit: behavior and transition are cohesive in the state class.\n      - Both are valid; choose the one that keeps complexity manageable in your codebase.\n\n75. **How do you share state objects between multiple contexts?**\n    - Make state objects stateless (Flyweight pattern)\n    - Use singleton instances for each state\n    - State objects should not store context-specific data\n    - Pass context as parameter to state methods\n    - Example: state.handle(context)\n    - Reasoning / scenario (detailed):\n      - If state objects are stateless, they can be shared safely across many contexts.\n      - Scenario: Many `CircuitBreaker` instances can reuse the same `OpenState` object.\n      - Keep per-instance data in the context (breaker), not inside the shared state instance.\n\n76. **What are the trade-offs between using State pattern vs state machines?**\n    - State pattern: OOP, flexible, easier to extend, more classes\n    - State machine: Table-driven, compact, harder to extend, less code\n    - State pattern: Better for complex state behavior\n    - State machine: Better for simple state transitions\n    - State pattern: Compile-time checking\n    - Reasoning / scenario (detailed):\n      - Table-driven state machines shine when transitions are simple and data-driven: (state, event) → nextState.\n      - State pattern shines when each state has substantial behavior (not just transitions) and you want OO decomposition.\n      - Trade-off:\n        - State pattern introduces more classes and indirection.\n        - Tables can become hard to understand when behavior per state grows.\n\n77. **How do you implement state history and rollback in State pattern?**\n    - Maintain a stack of previous states in context\n    - Push current state before transition\n    - Rollback: pop and restore previous state\n    - Can combine with Memento pattern for state data\n    - Example: stateHistory.push(currentState)\n    - Reasoning / scenario (detailed):\n      - Scenario: a wizard UI where users click Back to return to the previous step.\n      - Before changing state, push the current state onto a stack.\n      - On rollback, pop and restore the previous state.\n      - If you need to restore context data too, combine with Memento for snapshotting fields.\n\n78. **Can a state object maintain its own internal state?**\n    - Yes, but reduces shareability\n    - If state has internal state, can't use Flyweight\n    - Each context needs its own state instance\n    - Increases memory usage\n    - Better to store state-specific data in context\n    - Reasoning / scenario (detailed):\n      - If a state object stores mutable per-context data (timers/counters), sharing it across contexts mixes data (bugs).\n      - Scenario: `OpenState` stores “time opened”; if shared, one breaker overwrites another.\n      - Prefer keeping per-context data in the context and keeping state objects stateless.\n\n79. **How do you handle invalid state transitions?**\n    - Throw IllegalStateException\n    - Log warning and ignore transition\n    - Return error code or boolean\n    - Implement validation before transition\n    - Use state transition table to validate\n    - Reasoning / scenario (detailed):\n      - Scenario: `Order.ship()` called while order is unpaid.\n      - Options depend on desired UX and safety:\n        - Throw (fail fast) in domain logic.\n        - Return false/error for user-driven flows where invalid actions are expected.\n        - Ignore/log for idempotent operations.\n      - For complex workflows, a transition table can enforce allowed transitions consistently.\n\n80. **What is the difference between State pattern and Chain of Responsibility?**\n    - State: One state handles request, state changes\n    - Chain: Request passes through chain until handled\n    - State: Focus on changing behavior based on state\n    - Chain: Focus on finding appropriate handler\n    - State: Context knows current state\n    - Chain: Sender doesn't know which handler processes\n    - Reasoning / scenario (detailed):\n      - State:\n        - Exactly one active state determines behavior at a time.\n        - Calls go to the current state; you don’t “try other states”.\n      - Chain of Responsibility:\n        - Multiple handlers are candidates; the request flows until someone handles it.\n      - Scenario: Vending machine state flow → State. Middleware pipeline → Chain.\n\n## Strategy Pattern\n\n81. **What is the Strategy pattern and what problem does it solve?**\n    - Defines family of algorithms, encapsulates each, makes them interchangeable\n    - Solves problem of selecting algorithm at runtime\n    - Eliminates conditional statements for algorithm selection\n    - Enables algorithm to vary independently from clients\n    - Reasoning / scenario (detailed):\n      - Scenario: `ShoppingCart.checkout()` needs to support multiple payment methods (Card, UPI, PayPal, Bitcoin).\n      - Without Strategy, `checkout()` often becomes an `if/else` or `switch` on “payment type”, and every new payment method forces you to modify `ShoppingCart` (higher risk, violates Open/Closed).\n      - With Strategy:\n        - `PaymentStrategy` is an interface (the “family of algorithms”).\n        - `CreditCardPayment`, `PayPalPayment`, `BitcoinPayment`, etc. are interchangeable implementations.\n        - `ShoppingCart` depends only on `PaymentStrategy`, not on specific payment types.\n      - Result: you can change the algorithm (payment behavior) without changing the client/context (`ShoppingCart`).\n\n82. **How does the Strategy pattern support the Open/Closed Principle?**\n    - Open for extension: Add new strategies without modifying existing code\n    - Closed for modification: Context and existing strategies unchanged\n    - New algorithms added as new strategy classes\n    - No need to modify context when adding strategies\n    - Reasoning / scenario (detailed):\n      - OCP means: add new behavior by adding new code, not by editing stable code.\n      - Scenario: you already have `CreditCardPayment` and `PayPalPayment`. Later you add `ApplePayPayment`.\n      - With Strategy:\n        - You add one new class `ApplePayPayment implements PaymentStrategy`.\n        - `ShoppingCart` stays unchanged because it only calls `paymentStrategy.pay(amount)`.\n      - Compare to a `switch (paymentType)` in `checkout()`:\n        - you must edit `ShoppingCart` to add a new `case`, re-test it, and risk breaking existing paths.\n\n83. **What is the difference between Strategy and State patterns?**\n    - Strategy: Client chooses algorithm, strategies independent, focus on how\n    - State: Context changes state automatically, states may trigger transitions, focus on what\n    - Strategy: Typically set explicitly by client\n    - State: Changes based on internal logic\n    - Strategy: Algorithms are interchangeable\n    - Reasoning / scenario (detailed):\n      - Both patterns can look similar (a context holds a reference to an interface), but the intent differs.\n      - Strategy scenario (client-driven choice):\n        - `ShippingCostStrategy` can be `StandardShipping`, `ExpressShipping`, `Pickup`.\n        - The client/user picks which one to use (“I want express”), and the strategy doesn’t change itself automatically.\n      - State scenario (system-driven transitions):\n        - `Order` can be in `New`, `Paid`, `Shipped`, `Delivered`.\n        - The order changes state because events happen (`pay()`, `ship()`, `deliver()`), and the state object may decide the next state.\n      - A quick test:\n        - If the object “moves through” states over time and transitions are part of the design → **State**.\n        - If you simply “plug in” one of many algorithms to compute/do something → **Strategy**.\n\n84. **How do you choose the appropriate strategy at runtime?**\n    - Based on configuration/properties\n    - Based on input parameters\n    - Using factory pattern to select strategy\n    - Based on environment conditions\n    - Client decision based on requirements\n    - Reasoning / scenario (detailed):\n      - Scenario: You have multiple pricing rules: `RegularPrice`, `FestivalDiscount`, `EmployeeDiscount`, `PremiumMemberDiscount`.\n      - At runtime you pick a strategy based on *current facts*:\n        - Configuration: feature flags (“enable festival discount in region X”).\n        - Input parameters: cart total, user segment, coupon code.\n        - Environment: dev vs prod, country, A/B experiment bucket.\n      - Common implementation approach:\n        - A factory/registry maps inputs → a `PricingStrategy` instance (e.g., `StrategyFactory.from(user, cart, config)`).\n        - The context (`CheckoutService`) stays simple: it just calls `strategy.calculate(cart)`.\n\n85. **What are the performance implications of using Strategy pattern?**\n    - Slight overhead from indirection/polymorphism\n    - Object creation overhead if strategies not reused\n    - Generally negligible compared to algorithm complexity\n    - Can cache/reuse strategy instances\n    - Benefits outweigh performance cost in most cases\n    - Reasoning / scenario (detailed):\n      - Strategy adds an extra level of indirection: `context` calls `strategy.doWork()` rather than calling a concrete method directly.\n      - The overhead is typically:\n        - One virtual call (polymorphic dispatch), which is very small.\n        - Potential extra allocations if you create new strategy objects repeatedly.\n      - Scenario: `TaxStrategy` chosen per country.\n        - Good: store a single instance per country in a map and reuse it (strategies are often stateless).\n        - Risk: constructing a strategy per request may add GC pressure in high-throughput systems.\n      - Practical takeaway: optimize only if profiling shows it’s hot; structure/maintainability is usually the bigger win.\n\n86. **How does Strategy pattern differ from Template Method pattern?**\n    - Strategy: Uses composition, runtime selection, multiple algorithms\n    - Template Method: Uses inheritance, compile-time, single algorithm skeleton\n    - Strategy: More flexible, can switch at runtime\n    - Template Method: Simpler, less overhead\n    - Strategy: Favors composition over inheritance\n    - Reasoning / scenario (detailed):\n      - Template Method is “one algorithm skeleton, subclasses override steps”.\n        - Scenario: `ReportGenerator.generate()` is fixed (load → transform → render), subclasses only override `transform()` or `render()`.\n        - Choice is effectively compile-time (which subclass you instantiate).\n      - Strategy is “swap the whole algorithm object”.\n        - Scenario: `CompressionStrategy` can be `ZipCompression` or `GzipCompression`; you can change it per file at runtime.\n      - If you need to *switch behavior frequently* or *combine behaviors dynamically*, Strategy is usually a better fit than inheritance.\n\n87. **Can strategies maintain internal state? What are the implications?**\n    - Yes, but reduces reusability\n    - Stateful strategies can't be shared between contexts\n    - Need new instance for each use\n    - Increases memory usage\n    - Better to pass state as parameters or use stateless strategies\n    - Reasoning / scenario (detailed):\n      - Scenario (stateful strategy): `RetryStrategy` that tracks how many retries have already happened.\n        - If you reuse the same strategy instance across multiple requests, one request could “consume” retries and affect another request (bug).\n      - That’s why stateful strategies:\n        - Are harder to share safely.\n        - Often require a new instance per context/request (more allocations).\n      - Stateless alternative:\n        - Make the strategy pure (no internal counters) and pass required data in parameters, e.g., `retry(attemptNumber, exception)`.\n        - Or keep the evolving state in the context object and let the strategy remain reusable.\n\n88. **How do you implement a default strategy?**\n    - Initialize context with default strategy in constructor\n    - Use null object pattern for no-op default\n    - Check for null and use default if strategy not set\n    - Example: strategy = strategy != null ? strategy : new DefaultStrategy()\n    - Reasoning / scenario (detailed):\n      - Scenario: `ShippingCostStrategy` is optional; if the client doesn’t choose one, you still want deterministic behavior.\n      - Options:\n        - Default in constructor: `this.strategy = new StandardShipping();` ensures context is always usable.\n        - Null Object: `NoDiscountStrategy` returns “no discount” so you avoid `if (strategy == null)` checks everywhere.\n        - Lazy defaulting: if not set, choose a default on first use (useful when the default depends on runtime config).\n      - Goal: avoid `NullPointerException` and keep the context’s API easy to use.\n\n89. **What is the difference between Strategy pattern and Dependency Injection?**\n    - Strategy: Design pattern for algorithm selection\n    - DI: Technique for providing dependencies\n    - DI can be used to inject strategies\n    - Strategy is what, DI is how\n    - DI is broader concept, Strategy is specific pattern\n    - Reasoning / scenario (detailed):\n      - Strategy answers a design question: “How do I represent multiple interchangeable algorithms?”\n        - Example: `PaymentStrategy` with multiple implementations.\n      - Dependency Injection answers a wiring question: “How does an object get the dependency it needs?”\n        - Example: Spring injects `CreditCardPayment` into `ShoppingCart`.\n      - They combine naturally:\n        - DI constructs/registers all strategies and provides a selected one (or a `Map<String, PaymentStrategy>`).\n        - Your application code still benefits from Strategy’s clean polymorphism.\n      - Key point: DI doesn’t replace Strategy; it helps *assemble* and *select* strategies.\n\n90. **How do you pass context information to strategies efficiently?**\n    - Pass context object as parameter\n    - Pass only required data as parameters\n    - Use context interface to limit exposure\n    - Strategy holds reference to context (if needed frequently)\n    - Use callback interfaces for context access\n    - Reasoning / scenario (detailed):\n      - Scenario: `PricingStrategy` needs `cartTotal`, `customerTier`, and `country`.\n      - Passing options:\n        - Pass only required values: `strategy.price(cartTotal, tier, country)` keeps strategies decoupled and easier to test.\n        - Pass a context object: `strategy.price(CheckoutContext ctx)` can be convenient if there are many inputs, but risks “god context” (strategy can depend on too much).\n        - Use a narrow interface: `PricingContext { money cartTotal(); Tier tier(); Country country(); }` so strategies only see what they’re allowed to use.\n        - Hold context reference: useful if the strategy is long-lived and repeatedly queries the same context, but increases coupling and can complicate lifecycle/thread-safety.\n      - Practical rule: start by passing only what’s needed; introduce a narrow context interface when parameter lists get unwieldy.\n\n## Template Method Pattern\n\n91. **What is the Template Method pattern and when should you use it?**\n    - Defines algorithm skeleton in base class, subclasses override specific steps\n    - Use when you have invariant algorithm structure with variable steps\n    - Use to avoid code duplication in similar algorithms\n    - Use to control extension points in framework\n    - Reasoning / scenario (detailed):\n      - Scenario: A document generator always does: `createHeader → createContent → createFooter`.\n      - The overall flow is fixed (template), but the content differs by document type (report vs contract).\n      - Without Template Method, you often duplicate the common flow across subclasses or clients.\n      - With Template Method, the base class defines the skeleton once and subclasses override only the varying steps.\n      - Use it when you want a stable algorithm structure and controlled customization points.\n\n92. **What is the difference between template methods and hook methods?**\n    - Template method: Defines algorithm skeleton, usually final, calls other methods\n    - Hook method: Optional step with default (often empty) implementation, can be overridden\n    - Template method: Must not be overridden\n    - Hook method: May be overridden\n    - Example: generateDocument() is template, addMetadata() is hook\n    - Reasoning / scenario (detailed):\n      - Template method is the “main recipe” you don’t want subclasses to change (to preserve invariants).\n      - Hook methods are optional customization points.\n      - Scenario: `generateDocument()` always prints header/content/footer, but `addMetadata()` is optional:\n        - Most subclasses don’t care and use the default.\n        - Some override it to add extra info.\n      - Hooks prevent forcing all subclasses to implement every step.\n\n93. **How does the Template Method pattern support code reuse?**\n    - Common code in base class, executed once\n    - Subclasses only implement varying parts\n    - Eliminates duplicate code across similar algorithms\n    - Promotes DRY (Don't Repeat Yourself) principle\n    - Framework provides structure, subclasses provide specifics\n    - Reasoning / scenario (detailed):\n      - Scenario: Multiple report types share the same export pipeline:\n        - load data → validate → format → write output.\n      - Common steps live once in the base class.\n      - Subclasses only implement variable steps (e.g., `format()` for PDF vs HTML).\n      - This reduces bugs because shared steps are fixed in one place and improvements benefit all subclasses.\n\n94. **What is the Hollywood Principle in the context of Template Method?**\n    - \"Don't call us, we'll call you\"\n    - Base class calls subclass methods, not vice versa\n    - Inversion of control\n    - Framework controls flow, calls into subclass code\n    - Subclasses don't control when their methods are called\n    - Reasoning / scenario (detailed):\n      - In a framework, you don’t orchestrate the full flow; the framework does.\n      - Scenario: a test framework runs `setUp → test → tearDown`.\n        - Your subclass provides `setUp()` and `test()` implementations.\n        - The framework calls you at the right time.\n      - Template Method is one concrete mechanism to implement this inversion of control.\n\n95. **How does Template Method pattern differ from Strategy pattern?**\n    - Template Method: Inheritance, compile-time, algorithm structure fixed\n    - Strategy: Composition, runtime, entire algorithm replaceable\n    - Template Method: Subclass overrides steps\n    - Strategy: Client provides algorithm\n    - Template Method: Less flexible but simpler\n    - Reasoning / scenario (detailed):\n      - Template Method:\n        - You choose behavior by selecting a subclass.\n        - The skeleton is fixed; variation is in overridden steps.\n        - Changing behavior typically means instantiating a different subclass (compile-time-ish choice).\n      - Strategy:\n        - You choose behavior by plugging in a strategy object.\n        - You can swap strategies at runtime without changing the context class.\n      - Rule of thumb:\n        - Need a fixed pipeline with small step variations → Template Method.\n        - Need to swap algorithms freely and frequently → Strategy.\n\n96. **Can you override the template method itself? Why or why not?**\n    - No, template method should be final\n    - Prevents subclasses from changing algorithm structure\n    - Ensures invariant parts remain unchanged\n    - Subclasses should only override primitive operations\n    - Violating this breaks the pattern's intent\n    - Reasoning / scenario (detailed):\n      - The template method defines the contract/invariants of the algorithm.\n      - Scenario: `generateDocument()` must always call `createHeader()` before `createFooter()`.\n      - If subclasses override `generateDocument()`, they can skip steps or reorder them, breaking expectations and possibly security/validation guarantees.\n      - Marking it `final` enforces correct use: subclasses customize only the intended extension points.\n\n97. **What are the disadvantages of using inheritance in Template Method pattern?**\n    - Tight coupling between base and subclasses\n    - Violates \"favor composition over inheritance\"\n    - Limited to single inheritance in Java\n    - Harder to test in isolation\n    - Subclasses must understand base class implementation\n    - Reasoning / scenario (detailed):\n      - Inheritance couples subclasses to base class behavior and lifecycle.\n      - Changes in the base class can unintentionally break subclasses (fragile base class problem).\n      - Testing:\n        - You often need to instantiate concrete subclasses to test base behavior, or vice versa.\n      - Java constraints:\n        - Single inheritance can make it hard to mix multiple template-style frameworks.\n      - If flexibility becomes a problem, Strategy (composition) may be a better fit.\n\n98. **How do you handle optional steps in a template method?**\n    - Use hook methods with empty default implementation\n    - Subclasses override only if needed\n    - Use boolean flags to enable/disable steps\n    - Example: if (shouldValidate()) validate();\n    - Provide default no-op implementation\n    - Reasoning / scenario (detailed):\n      - Scenario: Some document types require validation; others don’t.\n      - Hook method approach:\n        - base class calls `validate()` which is empty by default.\n        - subclasses override when they need validation.\n      - Flag approach:\n        - base calls `if (shouldValidate()) validate();`\n        - subclass overrides `shouldValidate()` to control behavior without overriding the full template.\n      - Both keep the skeleton intact while allowing optional behavior.\n\n99. **Can you implement Template Method pattern without inheritance?**\n    - Yes, using composition and callbacks\n    - Pass function objects/lambdas for variable steps\n    - Use Strategy pattern instead\n    - Example: template(step1Func, step2Func)\n    - More flexible but loses some structure\n    - Reasoning / scenario (detailed):\n      - Scenario: You want a fixed pipeline but don’t want inheritance coupling.\n      - You can model the “steps” as functions/callbacks:\n        - framework code calls `step1()`, `step2()` callbacks in order.\n      - This is essentially the same idea but using composition:\n        - safer in languages where inheritance is restrictive,\n        - easier to test and assemble dynamically.\n      - Trade-off: you lose some of the explicit subclass structure/documentation.\n\n100. **What is the relationship between Template Method and Factory Method patterns?**\n     - Factory Method is a specialization of Template Method\n     - Factory Method: Template method for object creation\n     - Both use inheritance and method overriding\n     - Factory Method focuses on creating objects\n     - Template Method focuses on algorithm steps\n     - Reasoning / scenario (detailed):\n       - Factory Method is a common “step” inside a template:\n         - Base class defines a workflow, but delegates object creation to subclasses via a factory method.\n       - Scenario: `DocumentCreator.openDocument()` defines flow:\n         - create → open → log.\n         - `createDocument()` is the factory method overridden by subclasses (PDF vs Word).\n       - So: Template Method is the general “fixed skeleton”; Factory Method is a specific kind of override point for instantiation.\n\n## Visitor Pattern\n\n101. **What is the Visitor pattern and what problem does it solve?**\n     - Separates algorithm from object structure it operates on\n     - Solves problem of adding operations without modifying classes\n     - Useful when object structure is stable but operations change\n     - Enables adding new operations easily\n     - Reasoning / scenario (detailed):\n       - Imagine a `DocumentElement` hierarchy: `TextElement`, `ImageElement`, `LinkElement`.\n       - You need multiple *unrelated* operations over the same structure: `HTMLExport`, `MarkdownExport`, `SpellCheck`, `Analytics`, `AccessibilityAudit`.\n       - Without Visitor, you either:\n         - Keep adding methods to every element class (`toHtml()`, `toMarkdown()`, `spellCheck()`, ...), which bloats the model and forces edits across many files for every new operation, or\n         - Centralize logic with `if/instanceof/switch` checks, which is fragile and grows quadratically with types.\n       - With Visitor, you keep the element classes stable and add a new operation by adding **one new visitor class** (e.g., `SpellCheckVisitor`) that knows how to handle each element type.\n\n102. **What is double dispatch and how does Visitor pattern implement it?**\n     - Double dispatch: Method call depends on runtime types of two objects\n     - First dispatch: element.accept(visitor) - based on element type\n     - Second dispatch: visitor.visit(element) - based on visitor type\n     - Simulates multiple dispatch in single-dispatch languages\n     - Allows operation to vary by both visitor and element type\n     - Reasoning / scenario (detailed):\n       - In a single-dispatch language like Java, a call like `visitor.visit(element)` is chosen based on the runtime type of **one** receiver (`visitor`).\n       - Visitor achieves “choose-by-two-types” via two calls:\n         - **Dispatch #1 (element side)**: `element.accept(visitor)` picks the correct `accept(...)` based on the *runtime type of `element`* (`TextElement` vs `ImageElement`).\n         - Inside `accept`, the element calls `visitor.visit(this)`. Here, `this` is the concrete element type.\n         - **Dispatch #2 (visitor side)**: Java picks the right overridden `visit(...)` implementation based on the *runtime type of `visitor`* (e.g., `HTMLExportVisitor` vs `MarkdownExportVisitor`), and the method signature is tied to the element type passed (`visit(TextElement)` vs `visit(ImageElement)`).\n       - Net effect: behavior is specialized by **(ConcreteElementType, ConcreteVisitorType)**.\n\n103. **When should you use Visitor pattern over adding methods to classes?**\n     - When you need many unrelated operations on objects\n     - When object structure classes rarely change\n     - When operations change frequently\n     - When you can't modify existing classes\n     - When operations need to accumulate state\n     - Reasoning / scenario (detailed):\n       - Use Visitor when the “shape” of your object model is stable (e.g., a DOM/AST is fairly fixed) but the set of operations keeps growing:\n         - Week 1: export to HTML\n         - Week 2: export to Markdown\n         - Week 3: generate a table of contents\n         - Week 4: compute reading time, link checks, SEO audit\n       - If you keep adding methods onto elements, every new operation requires editing **every** element class (high churn, higher risk).\n       - If you add a visitor, you add **one class** per operation, keeping elements unchanged.\n       - Visitors can also maintain cross-cutting state cleanly (e.g., count images, collect all links, track heading depth) while traversing the structure.\n\n104. **What are the main disadvantages of the Visitor pattern?**\n     - Adding new element types is difficult (requires changing all visitors)\n     - Breaks encapsulation (visitors need access to element internals)\n     - Circular dependency between elements and visitors\n     - More complex than adding methods directly\n     - Requires element classes to expose accept() method\n     - Reasoning / scenario (detailed):\n       - **Adding a new element type is expensive**:\n         - Suppose you add `VideoElement`.\n         - You must add `visit(VideoElement)` to the visitor interface.\n         - Then you must update every existing concrete visitor (`HTMLExportVisitor`, `MarkdownExportVisitor`, `SpellCheckVisitor`, ...) or the code won’t compile (they no longer satisfy the interface).\n       - **Encapsulation pressure**:\n         - A visitor often needs data from elements (text, URL, attributes).\n         - That data must be exposed via getters or “visitor-friendly” APIs, which can leak internals.\n       - **Tighter coupling / circular knowledge**:\n         - Elements reference the visitor type in `accept(DocumentVisitor visitor)`.\n         - Visitors reference every element type in `visit(TextElement ...)`, `visit(ImageElement ...)`, etc.\n       - **More moving parts**:\n         - For small models (2–3 element types and 1–2 operations), Visitor can be overkill versus adding a method directly.\n\n105. **How does the Visitor pattern violate encapsulation?**\n     - Visitors need access to element's internal state\n     - Elements must expose data through public methods\n     - Breaks information hiding principle\n     - Alternative: provide visitor-specific interfaces\n     - Trade-off between encapsulation and flexibility\n     - Reasoning / scenario (detailed):\n       - Example: `ImageElement` stores `source`, maybe also `altText`, `width/height`, lazy-load flags.\n       - An `HTMLExportVisitor` needs `source` and `altText` to produce `<img ...>`.\n       - If those fields were intended to be private implementation details, you now introduce getters (or widen visibility) so the visitor can read them.\n       - Over time, multiple visitors ask for more internals, and the element’s public surface grows (“tell, don’t ask” gets harder to follow).\n       - Mitigations:\n         - Expose higher-level queries instead of raw fields (e.g., `image.toHtmlAttributes()`).\n         - Use narrow, visitor-specific read interfaces if you can (e.g., `HtmlExportableImageView`), at the cost of more types/complexity.\n\n106. **What is the difference between Visitor and Strategy patterns?**\n     - Visitor: Operates on object structure, double dispatch, multiple element types\n     - Strategy: Encapsulates algorithm, single dispatch, one context type\n     - Visitor: Traverses structure\n     - Strategy: Performs single operation\n     - Visitor: Elements accept visitors\n     - Reasoning / scenario (detailed):\n       - **Strategy** answers: “How should *one kind of thing* do an algorithm?”\n         - Example: `SortStrategy` for a `List<T>`: you swap one sorting algorithm for another, but the *data type you sort* stays conceptually the same.\n       - **Visitor** answers: “How should I apply an operation across a *family of related types*?”\n         - Example: a DOM/AST has many node types; the visitor has one method per node type.\n       - With Strategy, the variation point is usually one method like `strategy.execute(context)`.\n       - With Visitor, the variation point is a matrix: many element types × many operations (visitors), enabled via double dispatch.\n\n107. **How do you handle adding new element types in Visitor pattern?**\n     - Must add visit() method to all existing visitors\n     - Breaks Open/Closed Principle for element types\n     - Consider using reflection or instanceof as workaround\n     - Use default/fallback visit method\n     - This is the main weakness of Visitor pattern\n     - Reasoning / scenario (detailed):\n       - Concrete scenario:\n         - You have `TextElement`, `ImageElement`, `LinkElement`.\n         - Visitors: `HTMLExportVisitor`, `MarkdownExportVisitor`, `AnalyticsVisitor`.\n         - You introduce a new element: `CodeBlockElement`.\n       - What breaks:\n         - You add `visit(CodeBlockElement e)` to `DocumentVisitor`.\n         - Now **every** concrete visitor must implement it (compile-time failure until updated).\n       - Common ways teams cope (trade-offs):\n         - **Default/fallback**: add something like `visitUnknown(DocumentElement e)` and have new element call that until visitors catch up (less type-safe).\n         - **Reflection-based dispatch**: one `visit(DocumentElement e)` that finds a `visit(CodeBlockElement)` method via reflection (more dynamic, harder to refactor safely, slower).\n         - **Type checks**: use `instanceof` inside a visitor as a temporary escape hatch (simpler, but you lose the main benefit of Visitor).\n\n108. **Can you implement Visitor pattern without modifying existing element classes?**\n     - Difficult in statically-typed languages\n     - Use reflection to inspect and operate on elements\n     - Use external visitor framework\n     - In dynamic languages, can add methods at runtime\n     - Generally requires accept() method in elements\n     - Reasoning / scenario (detailed):\n       - In classic Visitor, each element must provide `accept(Visitor v)` so the element can call back `v.visit(this)` (this is the “double dispatch hook”).\n       - If the element classes come from a third-party library and you can’t add `accept`:\n         - In Java, you typically fall back to:\n           - Adapters/wrappers (wrap third-party elements in your own `Visitable` types), or\n           - Reflection / pattern matching / `instanceof` in a central place.\n       - In dynamic languages (e.g., Python/JS), you can sometimes “retrofit” an `accept` method at runtime (monkey patching), but that’s not idiomatic/safe in many codebases.\n       - Bottom line: without an `accept`-like hook, you usually can’t get the clean, compile-time-safe Visitor form.\n\n109. **How does Visitor pattern relate to the Open/Closed Principle?**\n     - Open for extension: Easy to add new operations (new visitors)\n     - Closed for modification: Don't modify element classes\n     - But: Adding new element types requires modifying all visitors\n     - Good when operations change more than structure\n     - Trade-off depends on what changes more frequently\n     - Reasoning / scenario (detailed):\n       - Visitor is “open/closed” in one direction:\n         - Adding a new operation = add a new visitor class (extension) with no changes to elements.\n         - Example: add `PdfExportVisitor` without touching `TextElement/ImageElement/LinkElement`.\n       - It is “open/closed” in the *opposite* direction only if element types never change:\n         - Adding `VideoElement` forces modifications across the visitor interface and all visitor implementations.\n       - So Visitor is ideal when:\n         - Your **type set is stable** (DOM/AST nodes don’t change often)\n         - Your **operations evolve** (new analyses/exports/transformations are frequent)\n\n110. **What are alternatives to Visitor pattern for adding operations to object structures?**\n     - Add methods directly to classes (if you control them)\n     - Use instanceof and type checking (less elegant)\n     - Use reflection and annotations\n     - Use functional programming with pattern matching\n     - Use interpreter pattern for expression evaluation\n     - Use external function libraries that operate on data structures\n     - Reasoning / scenario (detailed):\n       - **Add methods to classes** (best when you own the model and operations are few):\n         - If there are only 1–2 operations and they are core to the object, putting `toHtml()` directly on elements may be simpler than Visitor.\n       - **Type checking (`instanceof`, `switch`)** (best as a pragmatic shortcut):\n         - Centralize logic in one place: `export(element)` does `if (element instanceof TextElement) ...`.\n         - Downsides: grows with each new type, easy to forget a case, less refactor-safe.\n       - **Reflection/annotations** (best when you want “plugin-like” extensibility):\n         - Methods discovered at runtime, e.g., `@Handles(TextElement.class)`.\n         - Downsides: weaker compile-time guarantees; errors move to runtime.\n       - **Pattern matching / algebraic data types** (best in FP-friendly languages):\n         - Use sealed types + exhaustive `match` (Kotlin/Scala/Swift) to get compile-time exhaustiveness without Visitor boilerplate.\n         - Trade-off: adding a new case still forces you to update matches (similar trade-off, different ergonomics).\n       - **Interpreter / evaluation on AST nodes**:\n         - If the “operation” is essentially evaluation/interpretation, putting `evaluate()`/`interpret()` on nodes can be more direct than visitors.\n       - **External function libraries**:\n         - Treat elements as data and keep operations as pure functions (works well when immutability is common and you don’t need OO double dispatch)."
};

if (typeof window !== "undefined") {
  window.MESSAGE_QUEUE_QUESTION_SECTIONS = MESSAGE_QUEUE_QUESTION_SECTIONS;
  window.SPRING_QUESTION_SECTIONS = SPRING_QUESTION_SECTIONS;
  window.DESIGN_PATTERN_QUESTION_SECTIONS = DESIGN_PATTERN_QUESTION_SECTIONS;

  // Backward-compatible aliases for any remaining legacy references.
  window.INTERVIEW_QUESTIONS_SECTIONS = MESSAGE_QUEUE_QUESTION_SECTIONS;
  window.SPRING_INTERVIEW_QUESTIONS_SECTIONS = SPRING_QUESTION_SECTIONS;
}
